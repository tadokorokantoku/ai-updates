---
version: "2.1.124"
date: 2026-04-30T01:39:41+00:00
---

- Added project state management: new command to view and delete Claude Code project data (transcripts, tasks, file history, config entries) for a specific path or across all projects, with `--dry-run` support
- Added safety warnings when `rm` or `rmdir` shell operations are flagged as dangerous
- Added "Fix with Claude" option in IDE integrations
- Added prompt history search across all projects (not available in remote sessions)
- Added `CCR_EGRESS_GATEWAY_ENABLED` environment variable to route traffic through an egress gateway
- Added `CLAUDE_CODE_COLD_COMPACT` environment variable
- Added Remote Control security setting: require explicit approval before `SendMessage` reaches a peer session on another machine
- Added feedback message when installed plugins cache is cleared
- Updated organization error message when Claude subscription access is disabled for Claude Code
- Fixed oversized images being included in messages
