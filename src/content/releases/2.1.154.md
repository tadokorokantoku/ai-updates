---
version: "2.1.154"
date: 2026-05-28T15:40:50+00:00
---

- SDK: `Workflow` tool `args` field documentation clarified — pass arrays/objects as actual JSON values, not as a JSON-encoded string (a stringified list breaks `args.filter`/`args.map` in scripts)
- SDK: Added `gitOperation` structured field to `Bash` tool output; provides client-facing classification of git/gh operations (commit/push/merge/rebase/PR) detected in a command without re-parsing stdout
