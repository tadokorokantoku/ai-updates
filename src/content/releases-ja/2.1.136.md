---
version: "2.1.136"
date: 2026-05-08T16:30:41+00:00
---

- OpenTelemetry 経由でレスポンスをキャプチャしているエンタープライズ向けに、セッション品質サーベイを再有効化する `CLAUDE_CODE_ENABLE_FEEDBACK_SURVEY_FOR_OTEL` を追加
- ユーザーの意図や例外的な許可に関わらず無条件にブロックする auto mode 分類器ルール用の `settings.autoMode.hard_deny` を追加
- VS Code 拡張機能、JetBrains プラグイン、Agent SDK で `/clear` 実行後に `.mcp.json`、プラグイン、claude.ai コネクターで設定された MCP サーバーが黙って消える問題を修正
- 並行した認証情報書き込みが直前にローテーションされた OAuth トークンを上書きして強制再ログインを引き起こすまれなログインループを修正
- 複数サーバーが並行してリフレッシュする際に MCP OAuth リフレッシュトークンが失われる問題を修正 — 複数のリモート MCP サーバーを使用するユーザーは毎日の再認証が不要になる
- ツール呼び出し後に extended thinking が redacted thinking ブロックを出力した際の API エラー（400）を修正
- プロジェクトパスにアンダースコアが含まれる場合に `--resume` / `--continue` がセッションを見つけられない問題を修正
- 一致する `Edit(...)` 許可ルールが存在する場合に plan mode がファイル書き込みをブロックしない問題を修正
- WSL2: xclip/wl-paste が画像データを読み取れない場合に PowerShell フォールバック経由で Windows クリップボードからの画像ペーストが動作するようになった
- 実行中のセッションでまだ使用されているバージョンをキャッシュクリーンアップが削除する際にプラグインの `Stop`/`UserPromptSubmit` フックが失敗する問題を修正
- スラッシュコマンドダイアログ全体の視覚的一貫性を改善: フッターヒント、ダイアログの間隔、矢印キーのスタイルを標準化し、ダイアログフレームがロード後にポップインする代わりに即座に表示されるようになった
- bash コマンド出力とマークダウンコードブロックで色が誤った位置に表示される問題を修正
- ReasonML の差分表示で単語差分の境界に「undefined」という文字化けしたテキストアーティファクトが表示される問題を修正
- ワークツリー削除後のワークツリー終了ダイアログが誤ったディレクトリの未コミットファイルについて警告する問題を修正
- 小さな非 git ディレクトリでセッション中に作成されたファイルを `@` ファイルピッカーが一致させられない問題を修正
- 100 件以上のエントリがあるディレクトリで `@` メンションファイルピッカーがファイルを見つけられない問題を修正
- 出力が切り詰められた場合にフルスクリーンモードで失敗したツール呼び出しをクリックで展開できない問題を修正
- 持続的な拡張キーモードを持つターミナルで Ctrl+G を使って外部エディタを開いた後に Backspace と Ctrl+Backspace が入れ替わる問題を修正
- `/usage` の週次リセットが日付ではなく時刻を表示する問題を修正
- ウェルカムバナーの省略記号が CJK ターミナルでカラムオーバーフローを引き起こす問題を修正
- セッション履歴に不正な入力フィールドを持つツール呼び出しが含まれる場合の `/insights` クラッシュを修正
- ツールの折りたたみ可能性の分類がセッション中に変化した際のレンダラークラッシュを修正
- `plugin.json` の `skills` エントリがプラグインのデフォルト `skills/` ディレクトリを隠す問題を修正し、ファイルパスの一覧表示が黙って失敗する代わりにエラーを表示するようになった
- IDE シェル統合ロックファイルが `CLAUDE_CONFIG_DIR` を尊重しない問題を修正
- ストリーミング中にコピーされたターミナル出力の末尾の空白を修正
- プラグインのアンインストールと有効化/無効化がスラグの大文字小文字を区別しない比較をしない問題を修正
- サロゲートペア文字列でツールエラーの切り詰めマーカーが負の数を表示する問題を修正
- `/resume` または `/clear` 後に `CLAUDE_ENV_FILE` SessionStart フックの環境変数が古くなる問題を修正
- ペーストされた複数行の名前が与えられた場合に `/branch` が複数行のセッションタイトルを保存する問題を修正
- 列境界で折り返されたテキストの 2 行目に余分な先頭スペースが付く問題を修正
- `/install-github-app`、`/desktop`、`/resume`、`/web-setup` のダイアログで Esc が閉じない問題を修正
- `/doctor` の MCP スキーマエラーが不足しているフィールド名やソースファイルパスを表示しない問題を修正
- Bash 権限プロンプトがユーザーが読める説明の代わりに内部パーサー診断を表示する問題を修正
- スペースを含むプラグインスラッシュコマンド（例: `/myplugin review`）が名前空間付き形式に解決されない問題を修正
- `AskUserQuestion` が配列として渡されたマルチセレクト回答を破棄する問題を修正
- `/clear <name>` が `/resume` 用にクリアされたセッションにラベルを付けない問題を修正
- `CronList` の出力に qualifier とスケジュールされたプロンプトが表示されない問題を修正
- フルスクリーンモードで「Jump to bottom」オーバーレイが CJK 文字にカラーアーティファクトを残す問題を修正
- ストリーミング中に広いマークダウンテーブルがターミナルスクロールバックに古い枠線付きレンダリングを残す問題を修正
- ペーストされたテキストのプレースホルダーを含む長いプロンプトが自動切り詰めされた際にペーストされたテキストが黙って破棄される問題を修正
- changelog リフレッシュに失敗した後に `/release-notes` が古いバージョンで止まる問題を修正
- ターミナルに収まる以上のサーバーがある場合に `/mcp` サーバーリストがスクロールしない問題を修正
- 最初のスラッシュコマンドの後に入力中のスラッシュコマンドオートコンプリートが動作しない問題を修正
- `autoScrollEnabled: false` の状態で一番下にスクロールすると自動追従が再び有効になる問題を修正
- 空の入力で Enter によりプロンプトサジェストが自動送信される問題を修正（Tab または矢印キーで選択が必要に）
- キーボードショートカットのヒントが `keybindings.json` でリバインドされたキーを反映しない問題を修正
- `/settings` で言語変更後に Escape で確認が取り消される問題を修正
- `/terminal-setup` が完全一致のみでオートコンプリートに表示され部分一致で出ない問題を修正
- `AskUserQuestion` ダイアログで「Chat about this」を選択すると質問テキストが消える問題を修正
- サーバーがコンテンツブロックを返す場合に MCP ツールの結果が表示されない問題を修正
- `--worktree` が既存またはステールなワークツリーと衝突した際のエラーメッセージを改善
- プラグインマーケットプレイスの削除キーを `r`（リトライと競合していた）から `d`（他の削除操作と統一）に変更
