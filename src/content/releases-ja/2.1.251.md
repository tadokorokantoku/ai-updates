---
version: "2.1.251"
date: 2026-08-28T18:19:32+00:00
---

- `PreModelSwitch` と `PostModelSwitch` フックイベントを追加(モデル切り替えのブロック・確認・注釈が可能に)。`SessionStart` の resume フックがセッションの鮮度と再キャッシュの推定コストを受け取れるように
- フォアグラウンドのサブエージェントのツール呼び出しと結果を Remote Control クライアントにライブストリーミングする機能を追加(バックグラウンドのサブエージェントはデフォルトのまま、ステータスのみ表示)
- Claude apps gateway 経由で spend limit を持つ開発者向けに、`/usage` に Spend limit バーと、ステータスライン用の `rate_limits.spend_limit` フィールドを追加
- `/cost` にセッションごとのプロンプトキャッシュ行(ヒット率、ミス数、再キャッシュされたトークン数、warm/cold)を追加し、ステータスラインスクリプト向けに対応する `prompt_cache` オブジェクトを追加
- `claude --help` に `attach`、`logs`、`stop`、`respawn`、`rm` を追加。実行中のバックグラウンドセッションに対する `--resume` メッセージが、具体的な `claude attach <id>` コマンドを示すように
- 権限チェック後に作業ディレクトリ内でシンボリックリンクが差し替えられた場合、ファイルツール(Read、Write、Edit)がそれを辿ってしまい、承認範囲外の読み書きが可能だった問題を修正
- マーケットプレイスのエントリで宣言されたプラグインコマンドがプラグインディレクトリ外を指せてしまう問題を修正。そのようなパスは path-traversal エラーとして拒否されるように
- プロジェクト設定から詳細な beta トレーシングや raw API ボディのログ記録を有効化できてしまう問題、および低スコープの beta トレーシングエンドポイントが managed settings やホストアプリによって固定された OTLP コレクターを迂回できてしまう問題を修正
- Workflow ツールが権限チェック実行前に、セッションが読み取り許可されていない範囲の `scriptPath` を読み取り(かつエラー内で引用し)てしまう問題を修正
- Grep と Glob が、シンボリックリンクされた検索パス経由で到達したファイルに `Read(...)` の deny ルールを適用していなかった問題を修正
- モデルが thinking のみを出力したターンの後、「text content blocks must be non-empty」エラーで会話が止まってしまう問題を修正
- 起動時デフォルトが auto モードのアカウントで、新規インストール後の初回起動が default モードで始まってしまう問題を修正
- effort が xhigh/max で thinking が無効な場合に Opus 5 のリクエストが「effort … is not supported when thinking is disabled」で失敗していた問題を修正。この場合 effort は `high` として送信されるように
- Claude Desktop が別セッションから配信したメッセージへの返信時、そのセッション ID への `SendMessage` が「not reachable」で失敗せず、Claude Desktop 経由で配信されるように修正
- 多数の並列サブエージェントがある場合の TUI のもたつきを修正。1秒ごとの進捗更新がトランスクリプトに積み重なるのではなく、直前の表示を置き換えるように
- agent teams において、チームメイトの最終回答がチームリードに届かない問題を修正。中身のない「available」通知ではなく、idle 通知の中に届くように
- バックグラウンドのサブエージェントが、名前のない兄弟/親エージェントからのメッセージに返信できなかった問題を修正(`from` がアドレスではなくエージェントタイプになっていたため)
- セッション途中で届いた managed-settings の `disableAutoMode` が、既に実行中の auto モードセッションを default モードへ戻さなかった問題を修正
- 現在の Opus モデルが既に 1M コンテキストウィンドウを持っている場合でも「switch to Opus 1M for 5x more context」というヒントが表示されてしまう問題を修正
- Claude apps gateway セッションが、保存された Anthropic プロファイル(例: Console サインイン)をアクティブ扱いしてしまう問題を修正。リクエストで実際には使われないにもかかわらず `/status` に表示し、gateway の 401 をそのプロファイルで再試行していた
- クラウドセッションが、ホストがセッションの初期モデルを設定しただけの場合でもモデルが変更されたと Claude に伝えてしまう問題を修正
- 組織のポリシーで Remote Control が無効化されている場合に失敗として報告されていた問題を修正。単一の控えめな通知を表示するように
- 別セッションでサーバーが無効化されていた場合に、`/mcp reconnect` が Remote Control 上で実際の対処法ではなく汎用的な詳細非表示エラーを表示していた問題を修正
- `--input-format stream-json`: メッセージ ID なしで送信されたクライアント注入のアシスタントツール呼び出しが最初の呼び出しにマージされ、結果が失われる問題を修正(古いセッションの resume 時も含む)
- ディレクトリ変更によりセッションが既存の同一 ID トランスクリプトに再配置され、トランスクリプトが黙って上書きされてしまう問題を修正
- バックグラウンドセッションとそのサブエージェントが、`git worktree add` で作成した git worktree 内のファイルを編集できなかった問題を修正
- 別の Claude Code プロセスがプラグインマーケットプレイスを同時に更新していた場合、バックグラウンドセッションがプラグインスキルなしで起動し、そのままになってしまうことがあった問題を修正
- tmux 経由の SSH で開いたバックグラウンドセッション内でのテキスト選択を修正。OSC 52 にフォールバックするのではなく、フォアグラウンドセッションと同様に tmux バッファへコピーされるように
- SDK MCP サーバーのハンドシェイク確認応答が失われた場合に SDK およびクラウドセッションが無期限にハングしていた問題を修正。待機は70秒でタイムアウトし、該当サーバーのみを失敗扱いにするように
- セッションが強制停止された後、self-hosted runner が停止したセッションの Bash ツールプロセスを起動したままにしていた問題を修正
- 管理者が組織の usage-credit 上限を $0 に設定した Team/Enterprise メンバー向けの `/usage-credits` を修正。上限に達したと表示する代わりに、管理者に確認するよう案内するように
- gitlab.com オリジンでの merge-request 番号を伴う `--worktree --tmux` が、GitLab の ref を直接フェッチせず失敗確実な GitHub 形式のフェッチを先に試みていた問題を修正
- `emacs -nw` や `micro` など `/dev/tty` を開くエディタで、バックグラウンドセッション内の Ctrl+G が「Emacs quit unexpectedly」で失敗していた問題を修正
- null バイトを含む `additionalDirectories` のエントリが起動をクラッシュさせたり、SDK ホスト・IDE・フックから来た場合に `/add-dir` やその後の設定更新を壊していた問題を修正。現在はスキップされるように
- MCP サーバーメニューのコピー用ショートカットを修正。常に成功したと主張するのではなく、サインイン URL がどのようにコピーされたかを表示するように
- GNU screen および `screen` ターミナルタイプを使う tmux セッションで、イタリック体のテキスト(セッションの recap 行など)がハイライトブロックとして表示されてしまう問題を修正
- `claude mcp add --header` と `claude mcp add-json` のヘルプテキストが誤ったトランスポートを表示していた問題を修正
- クラウドセッションの起動に失敗した際に `claude ultrareview` と `/ultrareview` が30分間まるごと待機してしまう問題を修正。早期に停止し理由を報告するように
- 整数のシェル変数への算術式代入(例: `OPTIND=1/0`、`RANDOM=2+2`)を Bash の権限チェックが自動承認してしまう問題を修正。現在は承認を求めるように
- バックグラウンド化されたセッション(`←`、`/background`、`--bg`)が、シェルでエクスポートされた Vertex/Bedrock gateway(`ANTHROPIC_*_BASE_URL` + `CLAUDE_CODE_SKIP_*_AUTH`)を失い、すべてのリクエストが失敗していた問題を修正
- Max プランでの `claude --bg --model fable` が、同一アカウントの対話セッションにまだ Fable の割り当てがあるにもかかわらず、usage credits を求めて停止していた問題を修正
- 未対応の状態(agent-team のチームメイトのペインなど)で「make auto mode your default」の一度きりの提案が表示され、意図しないキー入力で未読のまま承諾されてしまう問題を修正
- 設定に変更がないにもかかわらず、同じ Claude apps gateway に再サインインした際に managed-settings の承認プロンプトが再表示されていた問題を修正
- `/bug` と `/share` が無効化されている場合に `/feedback` が無効と報告していた問題を修正。組織ポリシーや環境変数でオフになっている場合、ヒント・`/help`・拒否メッセージが `/feedback` を提案しないように
- 一時的な GitHub 接続エラーの後、クラウドセッション作成が GitHub のセットアップを促していた問題を修正。現在は再試行するようメッセージを表示
- 対話セッションのターン中の不要な UI 再描画を削減し、CPU使用率を改善
- インストールサイズを改善: ネイティブバイナリが約5MB小さくなりました
- クラウドセッションを改善: Bash コマンド実行中にセッションのネットワークプロキシが接続を切断した場合、ツールの結果に「connection reset」だけでなくホスト名と理由が表示されるように
- `/schedule` を改善: 「No MCP connectors」という簡素なメッセージの代わりに、Claude Code で設定した MCP サーバーはクラウドの routine にアタッチできないことを説明するように
- 自分のサブエージェントからのメッセージのフレーミングを改善: 送信者が無関係な別の Claude セッションではなく、このセッション内のワーカーであると Claude に伝えるように
- サブエージェントパネルや `/tasks` から開いたバックグラウンドサブエージェント/フォークのトランスクリプトを表示中、プロンプトのプレースホルダーが「Message @name…」と表示されるように改善
- エラーメッセージ、メニュー、コマンド結果における MCP サーバー名のサニタイズを改善
- `CLAUDE_CODE_PROVIDER_MANAGED_BY_HOST`(例: Claude Desktop)下での Amazon Bedrock セッション開始を改善: Bedrock のモデル ID や ARN が指定されたセッションが、推論プロファイルの検出を待たなくなりました
- managed settings の承認ダイアログを改善: 前回承認した以降に変更された設定のみを表示するように
- モデルのツール呼び出しが不正な形式だった場合の再試行を改善: 壊れた出力は再試行コンテキストから除外されるように(Bedrock、Vertex、Foundry を含む)
- `/radio` を Bedrock、Vertex AI、Foundry、Claude Platform on AWS、およびテレメトリが無効な場合でも利用できるように変更
- Claude in Chrome を変更し、テレメトリが無効なセッションを含め、ブラウザの操作が常に Claude Code の権限チェックを通るように(以前は Chrome 拡張機能独自のプロンプトが使われていました)
- `CLAUDE_CODE_SUBAGENT_MODEL` を変更し、すべてを上書きするのではなくデフォルトのサブエージェントモデルを設定するように。エージェント定義の `model:` と明示的な per-spawn モデルがこれより優先されるように
- 有効なモデルが認識可能な Claude モデルでない場合(例: カスタム `ANTHROPIC_BASE_URL` 経由のサードパーティモデル)、デフォルトのコミットトレーラーを `Co-Authored-By: Claude Code` に変更
- シート制の Enterprise サブスクリプションのデフォルトモデルを、他のプレミアムプランと同様に Opus 5 に変更
- `/effort` を変更し、モデルごとにデフォルトの effort レベルを保存するように。モデルを切り替えても各モデルの設定が保持されます
- managed settings が gateway ログインを強制する(または読み取れない)というだけの理由でサインイン前に analytics がオフにならないように変更。gateway または `DISABLE_TELEMETRY` でサインインした後にのみオフのままになります
- Bedrock、Vertex、Foundry、およびテレメトリがオフの場合のフッターの PR バッジを変更し、`gh pr view` の代わりに GitHub API を直接呼び出すように(`gh auth token`、`GH_TOKEN`、`GITHUB_TOKEN` 経由)
- サンドボックス内でコマンドを実行する際の Bash コマンド出力ファイルの作成・読み取り方法を変更し、サンドボックス化されたコマンドがそれらをリダイレクトや置換できないように
- プラグイン/LSP のインストール提案と auto モードのデフォルト提案が、入力中のテキストを送信またはクリアするまで待つように変更。送信用の Enter キーがこれらに意図せず回答してしまうことを防止
- サンドボックスの TLS を終端したり、サンドボックストラフィックを自分のプロキシ経由にルーティングしたり、認証情報を注入したり、サンドボックスの分離を弱めたりする server-managed 設定を、適用前に承認が必要になるよう変更
- managed settings または project settings からの `ANTHROPIC_CUSTOM_HEADERS` が、認証情報・組織/テナント・ルーティング・API 動作に関するヘッダー(例: `Authorization`、`Host`)を設定する場合に承認が必要になるよう変更
- プロジェクトレベルの `.claude/settings.json` の `env` が `CLAUDE_CONFIG_DIR`、`CLAUDE_CODE_TMPDIR`、`TMPDIR`/`TMP`/`TEMP` を設定しないように変更。これらはシェル、ユーザー、または managed settings で設定してください
- あまり使われない6言語(1c、gml、isbl、mathematica、maxima、sqf)のシンタックスハイライトを削除。バイナリが2.5MB小さくなりました
- [VSCode] サインイン画面の「Bedrock, Foundry, or Vertex」ボタンが、サードパーティプロバイダーのセットアップ箇所ではなくドキュメントの先頭を開いてしまう問題を修正
- [VSCode] Remote Control バナーをフッターのピルに変更(Remote Control が有効または失敗している間に表示)。claude.ai/code 上のセッションを開き、`/remote-control` でオン/オフを切り替えられます
