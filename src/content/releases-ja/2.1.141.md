---
version: "2.1.141"
date: 2026-05-13T23:19:16+00:00
---

- hook の JSON 出力に `terminalSequence` フィールドを追加: コントローリングターミナルなしでもデスクトップ通知・ウィンドウタイトル・ベルを発行可能に
- `CLAUDE_CODE_PLUGIN_PREFER_HTTPS` を追加: GitHub SSH キーのない環境で GitHub プラグインソースを SSH ではなく HTTPS でクローン
- `ANTHROPIC_WORKSPACE_ID` 環境変数を追加: ワークロードアイデンティティフェデレーション用 — フェデレーションルールが複数のワークスペースを対象とする場合に発行トークンを特定ワークスペースにスコープ
- `claude agents --cwd <path>` を追加: セッション一覧をディレクトリでフィルタリング
- `/feedback` で直近のセッション（過去 24 時間または 7 日間）を含められるように: 現在のセッションをまたぐ問題の報告に対応
- Rewind メニューに「ここまでを要約」を追加: 直近のターンを保持しながら以前のコンテキストを圧縮
- Auto モードのパーミッションダイアログで `permissions.ask` ルールがプロンプトを引き起こした場合にその旨を説明するように
- IDE 接続時のファイル編集パーミッションプロンプトに「IDE で差分を表示」オプションを復元
- `/bg` または `←←` で起動したバックグラウンドエージェントが、デフォルトに戻さず現在のパーミッションモードを引き継ぐように
- `claude agents`: 作業完了後もバックグラウンドシェルが残っているエージェントを「Working」のままにせず「Completed」に移動するように
- 長い思考中のスピナーフィードバックを改善: 10 秒後にスピナーが琥珀色に変わり Claude が作業中であることを通知
- プラグインメニューのナビゲーションを改善: `→`/Tab でタブ切り替え、`↑` でタブストリップに移動、フルスクリーンモードでタブヘッダーと検索ボックスがクリック可能に
- `ANTHROPIC_SMALL_FAST_MODEL` オーバーライドなしの Bedrock/Vertex/Foundry/gateway で、バックグラウンドサイドクエリが利用できない Haiku モデル ID を送信する問題を修正: メインループのモデルにフォールバックするように
- デーモンパイプキーファイルがロックまたは読み取り不能な場合に Windows で `claude daemon status` と `/doctor` が例外を投げる問題を修正: 不透明なエラーの代わりに根本原因を表示
- フラグを追加するラッパー経由で起動した場合に `claude agents` がダッシュボードではなくエージェントタイプ一覧を表示する問題を修正
- 作業ディレクトリが削除されたクラッシュセッションを `claude agents` で開いた際に冗長なディスパッチが発生する問題を修正
- カスタム `ANTHROPIC_BASE_URL` ゲートウェイのバックグラウンドジョブが自動命名されない問題を修正: Haiku モデル未設定時はメインモデルを使用
- 一方のセッションで `/model` を使うと他の並行セッションの autocompact しきい値がサイレントに変更される問題を修正
- ツールパーミッションプロンプト表示中にパーミッションモードを切り替えても、新設定がツールを許可している場合にプロンプトが自動的に閉じない問題を修正
- パーミッション/ダイアログプロンプト表示中に Enter を押すと入力ボックスのテキストも送信される問題を修正
- `EnterWorktree` が作業ディレクトリを変更した後、hook が存在しない `transcript_path` を受け取る問題を修正
- セルの折り返しがあるマークダウンテーブルが枠付きグリッドではなく縦のキーバリューレイアウトにフォールバックする問題を修正（2.1.136 のリグレッション）
- 入力ボックスに自動復元されたキャンセル済みプロンプトが上矢印履歴から削除され、重複エントリが生じる問題を修正
- 何も応答がないうちに Ctrl+C/Esc でキャンセルされたプロンプトが上矢印履歴から消える問題を修正
- vim の INSERT/VISUAL モード中に Ctrl+C が実行中のターンを中断しない問題を修正
- `enter` が `chat:newline` にリバインドされている場合に `meta+enter`・`ctrl+enter` などの代替 `chat:submit` キーバインドが機能しない問題を修正
- 出力スタイルが設定されている場合にプロンプトサジェストがサイレントに無効化される問題を修正
- ターン完了メッセージで `spinnerVerbs` 設定が適用されない問題を修正
- AskUserQuestion ポップアップが直前のチャットコンテンツの最終行を隠す問題を修正
- 検索でエラーが返った場合に Web Search ステータスが「Did 0 searches」と表示される問題を修正
- 行がターミナル幅を超えた場合に複数行のステータスライン出力が行を落としたり破損させたりする問題を修正
- light-ansi テーマでライト背景の差分コンテキスト行に不可視の白が使われる問題を修正: 黒を使用するように
- エラーオーバーレイが元のエラーメッセージを隠すミニファイ済みバンドルソースを出力する問題を修正
- フィードバックサーベイの評価数字を入力後に Enter を押すと評価ではなくチャットメッセージとして送信される問題を修正
- エージェントパネルで選択中のサブエージェントに `x` を押すと、エージェントを停止するのではなくプロンプトに入力される問題を修正
- ユーザーの最初のプロンプト前にプラグインモニター通知からセッションタイトルが生成される問題を修正
- 折りたたまれた読み取り/検索グループ配下で「Allowed by PermissionRequest hook」がツール呼び出しごとに繰り返される問題を修正
- `/tui` が実行中のバックグラウンドシェルやサブエージェントをサイレントに終了させる問題を修正: 代わりに終了を待つよう要求するメッセージを表示
- Bedrock・Vertex・Foundry などのサードパーティプロバイダーでウェルカムバナーが「API Usage Billing」と表示される問題を修正: プロバイダー名を表示するように
- 短いターミナルのフルスクリーンモードで `/mcp` サーバーリストがフォーカス中のサーバーを表示範囲内に保たない問題を修正
- `/feedback` バンドルのリダクションでセッション ID などの引用値に対して無効な JSON が生成される問題を修正
- デスクトップおよびサードパーティプロバイダーのセッションがホストの managed-settings から `apiKeyHelper`/`ANTHROPIC_AUTH_TOKEN` を誤って継承する問題を修正
- ロガー初期化前に発行された初期アナリティクスイベントがサイレントに破棄される問題を修正
- `sha` も指定されている場合にマーケットプレイスの `ref` が上流に存在しないプラグインで `claude plugin install` が失敗する問題を修正
- `.mcp.json` 経由で MCP サーバーを宣言しているプラグインのプラグイン詳細ペインが 0 台と表示される問題を修正
- 未設定の設定変数を持つプラグイン MCP サーバーが修正ヒント付きの「config issue」ではなく汎用的な接続エラーを表示する問題を修正: 不正な `.mcp.json` エントリが他の MCP サーバーを削除しないように
- POSIX シェルのパラメーター展開（例: `${var%pattern}`）を使用した MCP サーバー設定が環境変数の未設定として誤ってフラグされる問題を修正
- 接続時に 403 を返す MCP HTTP/SSE サーバーが「failed」ではなく「needs auth」として表示されるように修正
- オプションの server-events ストリームの再接続失敗時にリモート MCP サーバーが不必要に切断される問題を修正: ツール呼び出しは POST 経由で継続
- ワーカーセッショントークンがセッション途中でローテートした際に Remote Control MCP コネクターがすべて 401 で失敗する問題を修正
- サーバーが古いトークンを拒否した際に Remote Control が `/login` でループする代わりに信頼済みデバイスを自動再登録する問題を修正
- SDK/ヘッドレスモードでベータトレースが有効な場合に初期 OTel スパンがサイレントに破棄されるレースを修正
- カスタム `voice:pushToTalk` キーバインドと `"space": null` のアンバインドがサイレントに無視される問題を修正
- Windows で Alt+V による画像貼り付けがクリップボードにスクリーンショットがある場合でも「no image found」と報告する問題を修正
- glibc と musl 両方のプラットフォームパッケージがインストールされている Linux で SDK が「Claude Code native binary not found」を報告する問題を修正
- Bedrock: アンビエント AWS 認証情報が解決できる場合でも `awsCredentialExport` が設定されていれば常に実行するように: クロスアカウントアクセスの認証を修正
- [VSCode] マイクが無音のみを出力した場合にチャット内マイクがフィードバックを表示しない問題を修正: 「No audio detected」を表示するように
- [VSCode] ボイスモード: WSLg ユーザー向けに WSL エラーで `sox libsox-fmt-pulse` のインストールを提案するように
- `claude agents`: プリウォームされたバックグラウンドワーカーが不健全な場合にセッション起動が失敗する問題を修正: 新規起動にフォールバック
- `claude agents` が新しい REPL をバックグラウンド化した際に残る空のプレースホルダーセッションを非表示に: `←` 経由で入ったときに他のエージェントがない場合はオンボーディングテキストを表示
- `←` で残った空のアイドルバックグラウンドセッションをデーモンが 5 分後に自動退場させるように
