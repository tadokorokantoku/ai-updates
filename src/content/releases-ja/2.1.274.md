---
version: "2.1.274"
date: 2026-09-16T22:36:09+00:00
---

- メモリ使用量が危機的な状態になった場合に、メモリを解放するか安全に再起動する手順とともに目立つ警告を表示するように追加
- 最初の非対話ターンが接続中の MCP サーバーを待つ時間の上限を設定する `CLAUDE_CODE_MCP_STARTUP_WAIT_MS` を追加（`0` は待たない）
- `claude_code.llm_request` OpenTelemetry トレーススパンに、`api_request` イベントと同様の `effort` 属性を追加
- `claude_code.managed_settings_resolved` OTel イベントを追加: managed-settings のソースとポリシーヘルパーの状態。`OTEL_LOG_MANAGED_SETTINGS=1` で設定内容とダイジェストを redacted 形式で出力
- Claude apps ゲートウェイの設定に `store.connect_timeout_seconds` を追加し、Postgres の接続タイムアウト（デフォルト 5 秒）を延長可能に。データベースに到達できない場合の起動エラーが `store.postgres_url` と設定済みタイムアウトを案内するように改善
- Claude Desktop と Cowork が Claude apps ゲートウェイ経由で送信するテレメトリに、IdP のサブジェクトである `enduser.sub` を追加
- レプリカが一度に上流へ送るリクエスト数の上限 256 を超えて処理を抱えている場合の Claude apps ゲートウェイ警告と、その上限を示す起動時ログ行を追加
- フルスクリーンモードで折りたたまれたチームメイトやエージェントのメッセージをクリックで展開できるように追加
- 「unexpected tool_use_id」400 エラーを延々とリトライし続けてセッションが動かなくなる問題を修正: 破損したトランスクリプトは可能な場合自己修復し、それ以外の場合は `/rewind` のヒントとともに明確なエラーでループを終了
- 最初のリクエストに 422 やその他の 4xx エラーで応答する、レガシーな HTTP+SSE のみに対応した `http` 設定の MCP サーバーへの接続が失敗していた問題を修正
- サーバーごとに長めの `timeout` を設定していても、Streamable HTTP の MCP ツール呼び出しが約 5 分でタイムアウトしていた問題を修正
- サーバーが `listChanged` を宣言せずに list-changed 通知を送信した場合に MCP のプロンプトとリソースが更新されなかった問題を修正
- 403 insufficient_scope で拒否された MCP ツール呼び出しが期限切れのサインインとして報告されていた問題を修正: エラーは不足している権限を明示し、`/mcp` での再認証を案内するように
- コンテキストがリアクティブなコンパクション後に再びオーバーフローした際、（アクティブな `/goal` などの）フック駆動セッションがコンパクションではなく「Prompt is too long」で終了していた問題を修正
- コンパクション済みセッションを再開（`--continue` / `--resume`）した際にアクティブな `/goal` が失われる問題を修正
- 自動更新による再起動後に `claude agents` が `--model`、`--effort`、`--permission-mode`、`--allow-dangerously-skip-permissions`、`--agent` を失っていた問題を修正
- 言語サーバーが数千ファイル分のプロジェクト全体の診断情報を発行した際にターンごとの速度低下が発生していた問題を修正
- Bedrock、Vertex、Foundry 上で `model: "opus"` のサブエージェントが、モデル ID からモデルファミリーを認識できない場合にセッションのモデルのままになっていた問題を修正（`ANTHROPIC_DEFAULT_OPUS_MODEL` が設定されている場合を除く）
- トークン更新の失敗が数回続くと、次の予定された更新まで自己ホスト型ランナーのセッションが毎ターン 401 で失敗していた問題を修正。ランナーはリトライを続け、401 の後に新しいトークンを取得するように
- VS Code や `file://` URI を必要とするその他のターミナルで、ローカルファイルパスへのクリック可能なリンクが何も起こさなかった問題を修正
- 自分のメッセージ内で番号付きリストが振り直されていた問題（「3. 2. 1.」と入力すると「3. 4. 5.」と表示される）を修正。番号や「N)」マーカーは入力した通りに表示されるように
- AskUserQuestion のプレビューノートが、ハイライトされている選択肢ではなく以前に選んだ選択肢に付与されていた問題を修正
- AskUserQuestion のプレビューモードで、Enter でノートを送信するとハイライトされた選択肢が失われていた問題を修正
- 一部の呼び出しがメッセージ付きで承認された場合に、再開されたバックグラウンドエージェントが中断されたツールバッチの半分を保持し続けていた問題を修正
- `CLAUDE_CODE_RESUME_INTERRUPTED_TURN` で起動したローカルの `claude -p --resume` が、以前のプロセスが未完了のまま残したバックグラウンドタスクを報告していなかった問題を修正
- クラウドセッションの最初のターンが、まだ接続中の SDK ホスト型 MCP サーバーのツールなしで開始することがあった問題を修正
- バックグラウンドエージェントが自身のバックグラウンドタスクを待機していて再開する予定であるにもかかわらず、通知が「稼働中のバックグラウンド作業はない」と主張していた問題を修正
- Claude Desktop セッションのエラーヒントが、そこでは使用できない CLI フラグではなく `/usage-credits` のようなスラッシュコマンドを提案するように修正
- Claude がルーチンをリスト表示形式で書き込む際、`/schedule` がメッセージロールなしでルーチンのプロンプトを保存していた問題を修正
- `/status` が、その自身のエラーバナーが確認を促していた `apiKeyHelper` の失敗を表示していなかった問題を修正
- 非対話セッションでの `/fast on` が、組織の管理された fast mode ポリシー下で一旦オンと報告した後にオフになっていた問題を修正。今後は組織が無効化していることを表示するように
- Artifact ツールが、セッションが最新バージョンを読み込んでいなかったために結局拒否する更新について承認を求めていた問題を修正
- ネットワークアクセスが有効な Cowork と claude.ai のクラウドセッションが、チームメイトの artifact の読み取りをネットワークアクセスが無効であるかのように扱っていた問題を修正
- 自身の Git リポジトリを持たないプラグインやマーケットプレイスのディレクトリが、Git 管理された `~/.claude` のような外側のリポジトリからバージョンを取得していた問題を修正
- 空の `--mcp-config` を伴う `--strict-mcp-config` が、付随的な MCP サーバーのために最初の非対話ターンを `MCP_TIMEOUT` まで保持していた問題を修正
- Stop プロンプトフックが会話中のブロックのたびにプロンプト全体を再送信していた問題を修正。繰り返されるブロックは 500 文字のラベルで条件を示すように
- Cursor や VS Code のターミナル内で実行している Wayland 上の Linux で、起動時に余分な空のエディタウィンドウが開いていた問題を修正
- Postgres がスペンドチェック中に接続を切断した際に Claude apps ゲートウェイで処理されない Promise の拒否が発生していた問題を修正
- Claude apps ゲートウェイが SIGTERM で開いているストリームをすべて切断していた問題を修正: 終了前に進行中のリクエストを最大 25 秒待って完了させるように（`CLAUDE_GATEWAY_DRAIN_TIMEOUT_MS`）
- プラグインポリシーがリモートの managed settings から取得される場合に `installed_plugins.json` がほぼ毎回の起動で書き換えられ、Claude Desktop が開いているすべてのセッションのプラグインを再読み込みしていた問題を修正
- ヘッドレスおよび SDK セッションが、完了したバックグラウンドタスクごとに個別のモデル呼び出しを行っていた問題を修正。キューに溜まった完了は今後まとめて 1 回の呼び出しで応答されるように
- プラグインのリロードのたびに Bash ツールがシェルプロファイルを再ソースしていた（次のコマンドで数秒の遅延が発生する）問題を修正。今後はプラグインの `bin/` ディレクトリが変更された場合のみ再ソースするように
- `hooks/hooks.json` にトップレベルの `$schema` を持つプラグインで「unknown key」通知が表示されていた問題を修正
- MCP の接続エラーと MCP ログインツールの説明で、MCP 設定内の `${VAR}` プレースホルダーから解決されたシークレットが表示されていた問題を修正
- 特定の特殊なシェル変数をループまたは代入するコマンドに対する Bash の権限チェックを修正。これらのコマンドは今後権限を求めるように
- worktree で隔離されたセッションが、特定のネストしたシェル展開を含む Bash コマンドを受け入れていた問題を修正。今後は拒否されるように
- マルチバイト文字を含むファイルで、Edit の権限プロンプトのプレビューが承認された編集と異なる場所を表示することがあった問題を修正
- 軽度のメモリ逼迫状態のマシンで、バックグラウンドコマンドが 30 分アイドルの後に停止されていた問題を修正。今後はメモリが危機的な状態の場合のみ停止され、デバッグログに理由が記録されるように
- サブエージェントがメインセッションに送信したメッセージが、再起動後に Claude Desktop のトランスクリプトから消えていた問題を修正
- 複数回のリロードが重なった後、`.zip` から読み込まれたプラグインが古い展開内容から提供され続けていた問題を修正
- サブエージェントの進捗サマリーが暴走した複数段落の返信に置き換えられていた問題を修正
- `--input-format stream-json` セッションの起動を改善: 最初のターンは、ツール検索が遅延している接続中の MCP サーバーを最大 2 秒待たなくなり、後のターンで到着するように
- Monitor ツールの通知を改善: スクリプトの最終出力とその終了が、2 回の通知ではなく 1 回の通知にまとめられ、モデルのターンを節約
- Artifact ツールのエラーを改善: claude.ai にサインインしていない場合、最初の試行でターミナルがその旨を表示し、Claude には拒否された呼び出しをすぐにリトライしないよう伝える
- artifact の公開を改善: 古いバージョンをベースにした公開は送信前に停止され、マージすべき新しいページが示される
- サブモジュールのチェックアウトを含むエージェント worktree を削除する前の安全確認を改善
- `OTEL_LOG_RAW_API_BODIES=file:<dir>` の出力を改善: 新しい `index.jsonl` と `request_body_id` / `message.id` イベント属性により、各レスポンスをそのリクエストファイルとトランスクリプトメッセージにリンク
- Claude apps ゲートウェイの起動を改善: 最初の Postgres 接続を終了前に最大 3 回試行するようになり、数秒遅れて到達可能になるデータベースで起動が失敗しなくなった
- 負荷時の Claude apps ゲートウェイの spend-limit チェックを改善: データベースへのラウンドトリップが 4 回ではなく 1 回になり、混雑したゲートウェイでのタイムアウトが減少
- Claude apps ゲートウェイのサインインレート制限エラーを改善: `/login` が拒否理由を説明し、ゲートウェイログにはどの制限に達したか、どの設定を変更すべきかが記録されるように
- Bedrock、Vertex、Foundry、テレメトリ無効のインストールが、他のインストールと同様にデフォルトで v2 MCP クライアントと MCP 2026-07-28 ネゴシエーションを直接 HTTP サーバーに対して使用するように変更（オプトアウト: `MCP_SDK_GENERATION=v1` または `MCP_PROTOCOL_NEGOTIATION=legacy`）
- `/code-review` を変更し、独自のチューニング済み設定を持たないすべてのモデルで、多数のレビューサブエージェントを起動する代わりに軽量なインラインレビュープロンプトを使用するように
- `.mcp.json`、設定、プラグイン、エージェントファイル内の `"type": "sdk"` の MCP エントリを、警告付きでスキップするように変更: インプロセスサーバーの登録は SDK ホストアプリケーションのみが行える
- ローカルセッションでの artifact ウォッチングを変更: 他の場所で公開された新しいバージョンはターンを開始しなくなり、Claude は後の Artifact ツールの結果からそれを知るように
- プラグインとマーケットプレイスのクローンを変更し、Git LFS ファイルをダウンロードせずポインタのまま残すように。チェックアウト内で `git lfs pull` を実行すると取得される
- 読み取り専用リポジトリをアクセスチェックで git ホストが拒否した場合、セッション開始を失敗させる代わりにそのリポジトリをスキップするように自己ホスト型ランナーを変更
- `/status` の GitHub 行を「Cloud sessions」と表示するように、また `/web-setup`、`/ultrareview`、テレポートのメッセージが「Claude Code on the web」ではなく「cloud session」と表示するように変更
- [VSCode] ウィンドウのリロードが中断したステップの続行を追加。チャット内にラベル表示され、Claude Code: Continue After Reload 設定でオフにできる
- [VSCode] Customize メニューに Memory と Instructions の項目を追加: Memory は自動メモリのトグル、保存済みメモリ、メモリフォルダを表示し、Instructions は CLAUDE.md ファイルを編集
- [VSCode] Claude が開くエディタグループのロックを止める `claudeCode.lockEditorGroups` 設定を追加
- [VSCode] 新しい会話の最初の数秒で尋ねた `/btw` のサイドクエスチョンが、別セッションのサイドクエスチョン履歴を表示することがあった問題を修正
- [VSCode] 拡張機能が最初にグローバル gitignore ファイルを検索する際に一瞬フリーズしていた問題を修正
- [VSCode] Claude がツールを実行している間に送信されたメッセージが、ウィンドウのリロード後に会話から消えていた問題を修正
- [VSCode] Manage Plugins の有効化トグルと MCP サーバーダイアログの行がキーボードで操作できなかった問題を修正
- [VSCode] Environment Variables 設定で `CLAUDE_CONFIG_DIR` を変更した後、ターミナルで行ったサインインとサインアウトがリロードするまで反映されなかった問題を修正
- [VSCode] チャット内の Edit 差分が、一部のパネル幅や長い折り返し行で下部が切れていた問題を修正。差分ボックスは表示される行に合わせて調整されるように
- [VSCode] 拡張機能からの重複した設定書き込みが `~/.claude/settings.json` をパースできない状態にしたり、設定を消失させたりしていた問題を修正
- [VSCode] Open in New Tab（Ctrl/Cmd+Shift+Esc）で新しいタブのメッセージボックスにフォーカスが当たらず、クリックするまで入力できないことがあった問題を修正
- [VSCode] 閉じた Claude タブを再度開くと、そのロックされたグループが別の Claude タブとファイルを保持している場合にエディタレイアウトが分割されていた問題を修正
- [VSCode] ファイルタブが Claude タブとグループを共有している場合に、New session が別のロックされたエディタグループを開いていた問題を修正
- [VSCode] セッション名が、セッションピッカーで検索クエリを入力している間に横にずれていた問題を修正
- [VSCode] プランレビューカードが、プランに複数のコメントがある場合に Send feedback ボタンと理由欄を切り取っていた問題を修正。コメントリストはスクロールするように
- [VSCode] チャット返信内のインラインコードとコードブロックが High Contrast テーマで読みにくかった問題を修正
- [VSCode] 会話のスクリーンリーダーナビゲーションを改善: 各メッセージが「You」または「Claude」として、ツールステップにはツール名とともにアナウンスされるように
- [VSCode] `XDG_CONFIG_HOME` が絶対パスの場合、デフォルトのグローバル gitignore ファイルを `$XDG_CONFIG_HOME/git/ignore` に変更
- [Claude Code on the web] クラウドセッションの diff ビューに「Compare against」ブランチピッカーを追加し、ベースブランチだけでなく任意のブランチと比較できるように
- [Claude Code on the web] GitHub のトークン更新が一時的にエラーになった際に、クラウドセッションの git 操作が「service unavailable」で失敗していた問題を修正
- [Claude Code on the web] ルーチンの編集が、二重に発火したり一時停止したばかりのルーチンを再度有効化したりすることがあった問題を修正
- [Claude Code on the web] セッションの認証情報が更新された直後の数分間、クラウドセッションでのコミットが署名エラーで失敗することがあった問題を修正
- [Claude Code on the web] GitHub トリガーをリンクできなかったルーチンを保存した後のトーストが、「edit to retry」だけでなくリポジトリごとのトリガー上限などの理由を表示するように修正
- [Claude Code on the web] セッションが既読にした直後に未読に戻ってしまうことがあった問題を修正
- [Claude Code on the web] オーナーの GitHub 接続が見つからない場合、最初の失敗したチェックでルーチンをオフにする代わりに、最大 72 時間実行をスキップしてリトライするように変更
- [Claude Code on the web] ルーチンの保留通知を変更: サブスクリプションが一時停止されている場合、自動的な再開を約束する代わりに自分で再度オンにするよう案内するように
- [Claude Tag] Claude Tag 管理設定の Add channel と Add workspace フォームに Guests 設定を追加し、オーナーが最初から Inherit、Allow、Channel only、Restrict を選択できるように
- [Claude Tag] 別の Slack アプリやボットが @mention した際に Claude が応答していなかった問題を修正。タグは今後返信を受け取り、数日間非アクティブで追跡を停止していたチャンネルでも Claude を起こすように
- [Claude Tag] 新しい Slack チャンネルが作成された直後に @Claude をタグ付けした別アプリのメッセージを Claude が見逃していた問題を修正。Claude がチャンネルに参加した時点で配信されるように
- [Claude Tag] 最後の Slack メッセージから数分後に送られたフォローアップを、通知のないサイレントな編集として折りたたんでいた問題を修正。ブロッカーなどの遅れた更新は今後、通知される新しい返信として投稿される
- [Claude Tag] 単一チャンネル内で検索した場合に Claude の Slack 検索がエラーで失敗していた問題を修正。今後はそのチャンネル内の一致するメッセージを返すように
- [Claude Tag] 誰も待っていない状態でセーフティフィルターの停止が Slack スレッドのコンテキストを静かにリセットしていた問題を修正。Claude は今後常にその旨を伝え、実行中のバックグラウンド作業をキャンセルしないように
- [Claude Tag] Claude の Slack 返信内のメールアドレスが `mailto:` プレフィックス付きで表示されていた問題を修正。今後はプレーンでクリック可能なアドレスとして表示される
- [Claude Tag] 別のワークスペースから依頼された場合、組織全体で共有された Enterprise Grid チャンネルの監視を Claude が拒否していた問題を修正
- [Claude Tag] Claude Tag 管理設定の Environment ピッカーが、アーカイブ済みまたはアプリが作成した環境について環境名ではなく生の環境 ID を表示していた問題を修正
- [Claude Tag] Slack での Claude のライブ進捗チェックリストを改善: 2,000 文字を上限とし、活発なスレッドでは最大 15 分ごとに再投稿。古い「Latest task list」リンクは更新される
- [Claude Tag] 「Channel only」のゲスト設定を使用するチャンネルで Slack キャンバスを編集するたびに Claude が付加していた繰り返しのゲスト帰属メモを削除
- [Code Review] 新しいレビューが同じスレッドの下に低優先度の注記も記録した場合に、修正済みの指摘のスレッドが開いたままになっていた問題を修正
- [Code Review] GitHub や内部サービスの起動時の一時的な失敗でレビューが「Code review encountered an error」で終了することがあった問題を修正。今後は待機してリトライするように
- Code Review が投稿する各指摘の文言を改善: 誰が影響を受けるか、コードのどこが間違っているか、修正方法を先頭で述べる短く平易な文に
- [Code Review] 組織の上限が原因でレビューがスキップされた場合の check-run カードと PR コメントを改善: 各原因が、それを解決する管理者ページにリンクするように
