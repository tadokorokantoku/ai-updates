---
version: "2.1.50"
date: 2026-02-20T23:48:06+00:00
---

- LSP サーバーの `startupTimeout` 設定のサポートを追加
- エージェント worktree 隔離により worktree が作成/削除される際のカスタム VCS セットアップとティアダウンを可能にする `WorktreeCreate` と `WorktreeRemove` フックイベントを追加
- 作業ディレクトリにシンボリックリンクが含まれる場合、セッションストレージパスが起動中の異なる時点で解決されるため、再開されたセッションが見えなくなるバグを修正。グレースフルシャットダウンシーケンスでフックと分析の前にセッションデータをフラッシュすることで、SSH 切断時のセッションデータ損失も修正
- Linux: glibc が 2.30 より古いシステム（例: RHEL 8）でネイティブモジュールがロードされない問題を修正
- 完了したチームメイトタスクがセッション状態からガベージコレクションされないエージェントチームのメモリリークを修正
- `CLAUDE_CODE_SIMPLE` でスキル、セッションメモリ、カスタムエージェント、CLAUDE.md のトークンカウントを完全に削減するよう修正
- 存在しないサーバー名を与えたときに `/mcp reconnect` が CLI をフリーズさせる問題を修正
- 完了したタスク状態オブジェクトが AppState から削除されないメモリリークを修正
- エージェント定義で `isolation: worktree` をサポート。エージェントを隔離された git worktree で宣言的に実行可能に
- ツール検索が有効で launch 引数としてプロンプトが渡されたときに MCP ツールが検出されないバグを修正
- コンパクション後に内部キャッシュをクリアして長いセッション中のメモリ使用量を改善
- 設定済みの全エージェントをリスト表示する `claude agents` CLI コマンドを追加
- 処理された後の大きなツール結果をクリアして長いセッション中のメモリ使用量を改善
- LSP 診断データが配信後にクリーンアップされず、長いセッションで無限のメモリ増加を引き起こすメモリリークを修正
- 完了したタスク出力がメモリから解放されないメモリリークを修正。多くのタスクを持つ長いセッションでメモリ使用量を削減
- Yoga WASM と UI コンポーネントのインポートを遅延させ、ヘッドレスモード（`-p` フラグ）の起動パフォーマンスを改善
- キャッシュヒット率を低下させていたプロンプト候補キャッシュのリグレッションを修正
- ファイル履歴スナップショットに上限を設け、長いセッションでの無限メモリ増加を修正
- 1M コンテキストウィンドウサポートを無効化する `CLAUDE_CODE_DISABLE_1M_CONTEXT` 環境変数を追加
- Opus 4.6（fast モード）が完全な 1M コンテキストウィンドウを含むように
- VSCode: VS Code セッションで `/extra-usage` コマンドサポートを追加
- TaskOutput がクリーンアップ後も最近の行を保持するメモリリークを修正
- CircularBuffer で削除されたアイテムがバッキング配列に保持されるメモリリークを修正
- クリーンアップ後に ChildProcess と AbortController の参照が保持されるシェルコマンド実行のメモリリークを修正
