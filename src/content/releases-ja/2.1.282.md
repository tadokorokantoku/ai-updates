---
version: "2.1.282"
date: 2026-09-24T18:38:05+00:00
---

- 幅の広いターミナルで Claude の文章の表示幅を制限する `maxProseWidth` 設定を追加。テーブルとコードブロックは引き続き全幅で表示される
- プロジェクトの設定ファイルで無視された、またはテレメトリを無効化した telemetry 変数を一覧表示する起動時通知、`/status`、`claude doctor` の項目を追加
- `claude --chrome` を排他的な `managed-mcp.json` と併用できるようにする管理設定 `allowClaudeInChromeWithManagedMcp` を追加。Chrome がブロックされた際のエラーにその設定名が表示されるようになった
- Postgres の短時間の障害（データベースのフェイルオーバーなど）の間も `/readyz` を ready 状態に保てるよう、Claude apps gateway に `store.readiness_grace_seconds` を追加
- フルスクリーンモードの `/feedback` の下書き一覧にスクロールバーを追加。マウスが一覧上にあるときに表示される
- 会話履歴に API が復号できない Web 検索結果が含まれる場合（サードパーティのゲートウェイ経由で回答されたターンなど）に、すべてのリクエストが 400 エラーで失敗していた問題を修正
- 継続または再開したセッション（`--continue`、`--resume`）が以前のメッセージを変更された形で再送信し、API が Claude の以前の推論を破棄してしまうケースをさらに修正
- Claude が作業中に `/model`、`/rename`、`/artifacts` などの即時実行スラッシュコマンドを使用すると、それまでの extended thinking が破棄されていた問題を修正
- 会話の以前のターンで提供されていた組み込みツールを除外した `--tools` リストで再開すると、継続または再開した会話の以前の extended thinking が失われていた問題を修正
- 「Invalid `data` in `redacted_thinking` block」という API エラーで毎ターン失敗していたセッションを修正。Claude Code は会話の thinking ブロックを破棄して一度だけ再試行するようになった
- 要約リクエストが拒否されると compaction が失敗していた問題を修正。フォールバックモデルで再試行するようになった
- thinking がオフで effort が high を超えるセッションで、安全性に関するモデル切り替え後にターンが失敗する（「Effort 'xhigh' isn't available with thinking turned off」）問題を修正
- Claude Desktop など SDK ホスト型セッションで Fable の使用クレジットのプロンプトに応答がないままモデルが切り替わっていた問題を修正。今はターンが終了するようになり、Remote Control クライアントにもモデル切り替え通知が表示されるようになった
- プランが未有効化の使用クレジットを必要とする場合に、完全な Fable モデル ID を指定した `/model` が使用クレジットのプロンプトを開かずに API エラーで停止していた問題を修正
- ログイン更新中のプロセスが終了またはキルされた後、「another Claude Code process is refreshing it」というログインエラーで最大 1 分間リクエストが失敗していた問題を修正
- 別の Claude Code ウィンドウがサインインを更新中に開始したセッション（複数の VS Code ウィンドウでよく発生）が、組織ポリシーの取得を再試行しなかった問題を修正
- macOS の `/Network` に `..` や `/.vol` 形式のカーネルパスで到達するリポジトリのシンボリックリンク、または macOS の `/home` へのルールへのリンクが一覧表示される経由で、起動時に CLAUDE.md やルールが読み込まれてしまう問題を修正
- 設定ファイル内のパターン途中に `:*` を含む Bash 権限ルールがスキップされる一方で `--allowedTools` では有効になっていた問題を修正。すべてのソースで動作するようになり、マッチ方法について起動時に警告が表示される
- リモートセッションのワーカーが再起動した際、復元された権限プロンプトで承認済みのコマンドが二重実行されていた問題を修正
- `disableClaudeAiConnectors` や `allowManagedPermissionRulesOnly` などの真偽値ロックキーに誤った型の値が設定されていると、管理設定がそれを無視していた問題を修正。ロックが適用されるようになり、起動時に該当キー名が表示される
- ネストした値の一つが無効な場合に管理対象の `permissions`、`autoMode`、`worktree`、`attribution` 設定全体が無視されていた問題を修正。残りの設定は引き続き適用されるようになった
- リポジトリ、ユーザー、`--add-dir` の skills、commands、skills ディレクトリのプラグインマニフェストが、管理対象の `allowManagedPermissionRulesOnly` 配下で `allowed-tools` により自身のツールを事前承認できてしまっていた問題を修正
- Amazon Bedrock および Bedrock Mantle のセーフガードブロックメッセージにリクエスト ID が表示されていなかった問題を修正。ブロックメッセージにメッセージ ID も表示されるようになった
- Vertex AI: Claude Code がまだ認識していないモデル（新しくリリースされたものなど）で Web 検索が提供されなかった問題を修正
- Bash と PowerShell がディスククォータの上限を「Exit code 1」の裏に隠し、大きな出力ファイルを一時領域に残していた問題を修正
- 同じ呼び出し内の他のパラメータも無効な場合に、ツール入力の検証エラーが不明・欠落・誤入力のパラメータのうち一つしか表示していなかった問題を修正。それらもすべて表示されるようになった
- セッション途中でターミナルの bracketed paste モードがリセットされた後、貼り付けた複数行テキストが 1 行ずつ送信されてしまっていた問題を修正
- `SessionStart` フックのあるプロジェクトで、起動時にプロンプトのサンプルテキストが点滅して消えてしまう問題を修正
- フルスクリーンモードで起動した際、最初のフレームの前に一瞬空白の画面が表示される問題を修正
- 画面がターミナルより高いまま短くなった場合（例: シェルコマンドが出力をストリーミングしている間にプロンプト行を削除するなど）に、非フルスクリーンレンダラーの行が乱れて表示位置がずれる問題を修正
- 再描画された行の CJK 文字や絵文字が次の行に折り返された際、差分の最後の列に古い文字が残ってしまう問題を修正
- タブを含む履歴から呼び出したプロンプトで、カーソルが末尾より手前に配置される問題を修正
- 改行として送信するターミナル（1.25 より前の Windows Terminal）で送信ヒントが ctrl+enter と表示されていた問題を修正。そのようなターミナルでは ctrl+x ctrl+s と表示されるようになった
- Remote Control 自体の対象外エラーで `--debug` を付けて実行するよう案内しているにもかかわらず、`claude remote-control --debug` が「Unknown argument: --debug」で失敗していた問題を修正
- `/install-github-app` が「cancelled」と表示した後もブランチの push や API キーシークレットの保存を続けていた問題を修正。中断すると残りのステップが停止し、それまでに完了した内容が報告されるようになった
- Bedrock、Vertex などのサードパーティプロバイダで、/feedback、/bug、/share が保存中にキャンセルしてもレポートファイルを保存してしまっていた問題を修正
- 設定ファイルでまだ有効化されている、または読み込めなかった場合に、プラグインのアンインストールが成功と報告してプラグインの保存済みオプションを削除してしまっていた問題を修正。今は停止し、該当ファイル名が表示される
- 削除後にインストール済みプラグインの一覧が読み込めない場合、プラグインのアンインストールが保存済みオプションとシークレットを削除してしまっていた問題を修正。今はそれらが保持され、アンインストール側でその旨が表示される
- `/skills` で `/` の直後に入力したキーが検索ボックスではなくスキル一覧を動かしてしまっていた問題を修正
- `/skills` の検索ボックスからスキル一覧に入力中にターミナルカーソルが移動し、カーソルが見えなくなったり IME 入力の位置がずれたりする問題を修正
- `/skills` や `/mcp` などスクロールバー付きの一覧が、スクロールバーが表示されえない非フルスクリーンモードで 2 列分狭く表示されていた問題を修正
- 長くリバインドされたキーでエージェントパネルのフッターが 2 行に折り返される問題と、「Esc to collapse」ヒントがリバインドされた collapse キーを無視する問題を修正
- `keybindings.json` で stop-all-agents のショートカットが未割り当ての場合に、`/tasks` ダイアログのフッターで ` · ` の区切りが二重に表示される問題を修正
- Claude がバージョンに 60 文字を超えるラベルを付けた場合に artifact の公開が失敗していた問題を修正。ラベルが短縮されるようになった
- スクリーンリーダーモード、引用リスト、非常に長いリストで、リスト項目内（直接または引用内）で始まるコードブロック先頭の空行が失われていた問題を修正
- PDF のページ読み込みエラーメッセージを修正: アクセント記号付きや非ラテン文字を含むパスが読みやすく表示されるようになり、「password」や「invalid」のような名前のフォルダによってエラーの原因が誤って表示されることがなくなった
- vim モードの `>>` が空行をインデントする問題、行より長いカウントを指定した `r` がテキストを変更してしまう問題、`2J` が 1 行多く結合してしまう問題、最終行でのカウント指定（`2dd`、`2>>`）がずれたり削除されたりする問題を修正
- vim モードのカーソル位置を修正: `dd`、`dj`、`dG` や行全体の `p`/`P` の後は先頭の非空白文字に移動し、`yy` はカーソルを移動しなくなり、絵文字の後の Esc でカーソルが絵文字の中に残らなくなった
- vim モードで `x`、`s`、`p`、`d`、`c` を `.` で繰り返す際にカウント指定が無視されていた問題、および行全体の `p`/`P`、`o`、`O`、`J`、`>>`、`<<` が上の行が折り返されている場合に誤った行に作用していた問題を修正
- vim モードで、履歴から呼び出した、またはキューから戻したプロンプトの末尾を超えた位置にノーマルモードでカーソルが残り、`x` が何も行わなかった問題を修正
- コンパクションされたことがないものも含め、非常に大きなセッションの再開にかかる時間を改善
- Windows でトランスクリプトファイルが読み込めずセッションを再開できない場合（EBADF）のエラー表示を改善。考えられる原因と対処法が表示されるようになった
- Claude Desktop の不明なモデルエラーで、別のモデルへの切り替えを提案するように改善
- 権限プロンプトでの特殊な Unicode の表示を改善
- `/artifacts` を改善: タイトルが 1 列に揃い、詳細が単語の途中で切れず丸ごと省略されるようになり、一覧が PgUp/PgDn、Home/End、マウスホイール、クリックに対応した
- `claude-api` スキルを更新: 出力前の拒否に関する課金について How refusals are billed のドキュメントへのリンクを追加し、ストリーム途中の拒否は通常料金で課金され、出力前の拒否はレート制限にカウントされることを明記
- `claude-api` スキルを更新し、Managed Agents のリソースをバージョン管理されたファイルとして保持するために `ant apply` を使うことを推奨するようにした
- テレメトリがオフの場合、Anthropic API への直接接続では auto mode がデフォルトでサーバー側の分類器を使用するように変更（`CLAUDE_CODE_AUTO_MODE_SERVER=0` でオプトアウト可能）
- 管理設定または `--settings` で `allowUnsandboxedCommands: false`、あるいは管理設定で `allowManagedDomainsOnly: true` が設定されている場合、`sandbox.excludedCommands` がプロジェクト設定とローカル設定のエントリを無視するように変更
- プロジェクト設定とローカル設定が、`CLAUDE_CODE_ENABLE_TELEMETRY` や `OTEL_LOG_*` のようなエクスポートを有効化・エンドポイント設定・内容取得を行う OpenTelemetry 変数を無視するように変更
- Windows/WSL の管理設定を変更し、管理者ポリシー（HKLM、`managed-settings.json`）が存在するが無効または読み込めない場合に、ユーザーが書き込み可能な HKCU と WSL の `/etc/claude-code` が適用されないようにした
- `Skill(anthropic-skills:*)` と `Skill(claude-ai:*)` の許可ルールが、claude.ai から同期されたスキルのみを対象とし、同名を使うだけのプラグインや他のスキルは対象外になるように変更
- `anthropic-skills` または `claude-ai` 名前空間のスキルフォルダ、コマンドファイル、ワークフローコマンドが読み込まれなくなるように変更。同名のプラグインは引き続き読み込まれるが、同期されたスキルとの名前の競合が発生する
- `anthropic-skills` または `claude-ai` という名前で構成された MCP サーバーが、スキルやプロンプトを一切表示しないように変更（ツール自体は引き続き動作する）。一覧に表示させたい場合は MCP 設定でサーバー名を変更すること
- `/effort` とプロンプト入力欄の `ultracode` の見た目を、通常のスタイル（波紋・枠の装飾・キーワードのきらめきなし）に変更し、dynamic-workflows のスピナーのヒントを削除
- 起動バナーの Clawd マスコットの足が、体の角の下に位置するように変更
- [VSCode] 長い返信がストリームに追いつけなくなる問題を修正: パネルが更新のたびに返信全体を再パースしなくなった
- [VSCode] 入力欄が十分に高くスクロール可能な場合、ディクテーションのマイクボタンがメッセージ入力欄のスクロールバーを覆ってしまう問題を修正
- [VSCode] このコンピュータで開始した Remote Control セッションが、セッション一覧の Web エントリから開けなかった問題を修正。他の場所で実行中でない限りローカルの会話が開くようになった
- [VSCode] 拡張機能ホストの再起動後にエディタタブのサインイン画面が無言でハングしていた問題を修正。「stopped responding」通知が表示されるようになった
- [クラウドセッション] Settings › Connectors › GitHub に Claude GitHub App のステータス（アプリがインストールされアカウントに到達可能かどうか、接続・インストール・再接続の手順）を追加
- [クラウドセッション] リポジトリが GitHub 以外の Git サーバーでホストされているクラウドセッションのリポジトリメニューに「Open repository」と「Open compare page」のリンクを追加
- [クラウドセッション] Slack から開始したものを含め、すでにリポジトリが設定されている実行中のクラウドセッションに、フォークのアップストリームなど別の GitHub オーナーのリポジトリをアタッチできる機能を追加
- [クラウドセッション] 30 分単位のタイムゾーン（インドなど）で、1 時間ごとのルーチンの次回実行時刻が 30 分ずれて表示されていた問題を修正
- [クラウドセッション] 過去のセッションで多数のチェックインリマインダーがスケジュールされているアカウントで、Routines ページとサイドバーの Scheduled 一覧の読み込み速度を改善
- [Claude Tag] Enterprise Grid の組織全体インストールで、1 つのワークスペース用に保存された自動参加チャンネルパターンが無視されていた問題を修正。Claude が一致する新しいチャンネルに参加するようになった
- [Claude Tag] 1 組織の 2 つのワークスペースで共有されているチャンネルで、そのチャンネルの Claude Tag バージョンがもう一方のワークスペースから保存されていた場合に Claude が応答しなかった問題を修正
- [Claude Tag] GitHub Enterprise Server のリポジトリでのセッション用の旧 Claude in Slack アプリの進捗カードを修正: リポジトリ名が表示され、動作する Create PR ボタンが提供されるようになった
- [Claude Tag] モデルが廃止された Slack スレッドが毎回別のモデルにフォールバックし、応答が遅くなりフォールバック通知が毎回表示されていた問題を修正。スレッドが動作するモデルに移行するようになった
- [Claude Tag] Claude が Slack にアップロードするファイルにテーブルを含むキャプションを付けられなかった問題を修正。キャプションが返信と同じ書式でレンダリングされるようになった
- [Claude Tag] Slack の Agents & tools ビューで、Claude のスレッドが名前ではなく最初のメッセージの下に表示されることがあった問題を修正。後からの名前変更でも一覧が更新されるようになった
- [Claude Tag] アクセスバンドルの Repositories タブから GitHub organization の許可を削除する際、その GitHub organization が切断済みだと保存に失敗していた問題を修正
- [Claude Tag] アプリが追加されていない Grid ワークスペースと共有されているチャンネルで、Claude が常に「Couldn't check this channel just now」と返信していた問題を修正。どのワークスペースにアプリが必要かが通知に表示されるようになった
- [Claude Tag] セッションのクラウドワーカーが再起動した後、Claude の返信フッターのコストとトークンの合計が実際より大幅に高く表示されていた問題を修正
- [Claude Tag] Claude が Slack の返信でプラン・テーブル・詳細に使う枠付きカードを、狭い幅ではなくデフォルトで幅広に表示するように変更
- [Claude Tag] 新しく接続された Slack ワークスペースが、接続時点のデフォルトモデルを保持し続けるのではなく、現在のデフォルトモデルに従うように変更
