---
version: "2.1.273"
date: 2026-09-15T18:06:34+00:00
---

- LLM ゲートウェイ向けに `x-claude-code-request-class`、`x-claude-code-agent-type`、`x-claude-code-prev-tool-durations`、`x-claude-code-compaction`、`x-claude-code-context-compacted` リクエストヘッダーを追加。`CLAUDE_CODE_GATEWAY_HINT_HEADERS=1` でオプトイン
- MCP サーバーがセッション中に切断され、自動再接続が諦めた場合に `/mcp` を案内する通知を追加
- `claude --remote-control` または `/remote-control` で開始したセッションを、Claude アプリからフォークできるように追加。フォークはコンピュータ上のバックグラウンドセッションとして実行される
- `permissions.blockReadsOutsideWorkingDirectories` で、権限チェッカーが完全に解析できない Bash コマンドがプロンプトをスキップしていた問題と、バイパスモードでサブシェルが危険な `rm` を隠していた問題を修正
- claude.ai から同期されたスキルが、組織が Skills を無効化した後も使用可能なままだった問題を修正。今後は復元可能なゴミ箱に移動する
- MDM や `managed-settings.json` で設定した `allowManagedMcpServersOnly`、`deniedMcpServers`、`disableClaudeAiConnectors` が、サーバー管理の設定も存在する場合に無視されていた問題を修正
- Bedrock、Vertex、Foundry での 401/403 エラーと、Claude アプリゲートウェイの 403 エラーで `/login` の実行を案内するように修正。メッセージは更新すべき認証情報の名前、またはゲートウェイ管理者への案内を示すようになった
- `/login`、`/upgrade`、`/extra-usage` が会話の以前の思考内容を破棄し、次のリクエストでプロンプトキャッシュ全体の再構築を強制していた問題を修正
- クラウドまたは Remote Control セッションで Artifact ツールがチャットに添付したファイルをアップロードした際、auto モードが承認待ちで停止していた問題を修正
- リポジトリの `.git` ディレクトリが削除または移動された後、長時間実行中のセッションがスタブの `.git/info/exclude` を再作成していた問題を修正
- シェルモード中に先頭で入力した `!` がメインプロンプトで無視されていた問題を修正し、`! grep …` のような否定コマンドを入力できるように
- macOS の Read が、ドラッグ&ドロップしたスクリーンショットやシステムが別パスとして報告するファイルを「symlink resolution changed after permission was checked」として拒否していた問題を修正
- `permissions.blockReadsOutsideWorkingDirectories`: リポジトリの設定で選択されたメモリディレクトリが、プロンプトへの読み込み、想起、インデックス化、メモリ抽出に使用されないように修正
- 最終のストリーミング応答にトークン使用量が含まれていない、またはモデル ID がない場合に、サブエージェントとバックグラウンドエージェントが失敗として報告され、結果が届かなかった問題を修正
- コンテキストメーターと auto-compact が advisor ツールのターンを実際のコンテキストサイズの約 2 倍としてカウントしており、auto-compact が実際のウィンドウの約半分で発動していた問題を修正
- 既に作業を終えてエージェントパネルに表示されなくなったエージェントチームのチームメイトが原因で `/tui` が再起動を拒否していた問題を修正
- `.claude/scheduled_tasks.json` が新しい worktree など別のフォルダにコピーされた後、保存済みのスケジュールタスクが誤ったセッションで実行されていた問題を修正
- サブエージェントが実行中にバックグラウンドに移動した場合（例: `CLAUDE_AUTO_BACKGROUND_TASKS` による）、SDK と `--output-format stream-json` の出力がサブエージェントの残りのメッセージと最終レポートを破棄していた問題を修正
- `/install-github-app` が SAML シングルサインオンのブロックを「admin permissions required」と誤報していた問題を修正
- Claude Desktop、VS Code、JetBrains のセッションに接続した Remote Control クライアントが、セッションのコンテキストウィンドウ使用状況を要求した際に拒否されていた問題を修正
- 「Running PreCompact hooks…」のようなコンパクション状況表示行で、スピナーが二重の省略記号（「……」）を表示していた問題を修正
- Artifacts の読み取りや公開後に frontend-design プラグインを誤って提案するスピナーのヒントを修正
- 2.1.268 で導入された、権限チェッカーが解析できない Bash 行（`eval`、`env -C`）に対して Read と Edit の拒否ルールをチェックする変更を差し戻し。`time -p make build` のようなコマンドは拒否されず再度プロンプトが表示されるように
- 長時間セッションの応答性を改善: フックの進行状況とサブエージェントのアクティビティが、更新のたびに会話全体を再処理しなくなった
- Artifact ツールが提供していないファイル種別を公開しようとした際のエラーを改善: どの種別が提供されるか、代わりに何をすべきかが伝えられ、ターミナルには一行のプレーンなメッセージが表示される
- Artifact ツールのページ読み取りで、公開権限を持つ全員に対し、artifact サービスがそのページに対して持つ機能とデータベースルールを提示するように改善
- artifact のデータベース書き込みを改善: 更新時にドキュメント全体を書き換えずに単一フィールドを削除できるように
- artifact の公開を改善: claude.ai への到達後に接続が切れた公開が、失敗や重複バージョンの作成ではなく安全に再送されるように
- IP 許可リスト、アプリのインストールの停止、SAML シングルサインオンによるクラウドセッションの GitHub エラーで、汎用的なインストール案内ではなく原因を表示するように改善
- `/autofix-pr` を改善: `gh pr view` が失敗した場合、汎用的な終了コード行ではなく gh 自体のエラー（サインイン、SAML、レート制限）を表示するように
- `/autofix-pr` を改善: PR に対する GitHub webhook の配信が設定できなかった理由（例: リンクされた GitHub アカウントがない）を、汎用的な警告ではなく表示するように
- `/web-setup` のエラーを改善: 拒否された GitHub トークンには考えられる理由と対処法を、接続失敗には設定済みのプロキシまたは TLS 証明書の問題を表示するように
- セッション内の SSL 証明書およびプロキシ接続エラーを改善し、エラーコードと対処法（例: 信頼されていない社内 CA に対する `NODE_EXTRA_CA_CERTS`）を示すように
- クラウドセッションが Claude のログイン期限切れまたは失効により作成できない場合のエラーを改善: `/login` の実行を案内するように
- MCP サーバーのサインインがセッション中に期限切れになった場合のエラーメッセージを改善し、再認証方法（`/mcp`）を示すように
- Bedrock、Vertex、Foundry での auto モードを、当面はローカル分類器をデフォルトで使用するように変更。`CLAUDE_CODE_AUTO_MODE_SERVER=1` でプラットフォームのサーバー側分類器を使用可能
- `OTEL_LOG_TOOL_DETAILS=1` で、コストとトークンのメトリクスに実際のエージェント名、スキル名、プラグイン名、MCP サーバー名も含めるように変更
- Claude アカウントでのサインインが、claude.ai のプラグインへのアクセスも要求するように変更
- `/bug` と `/feedback` のレポートに、最後の API リクエストからモデルの動作に関するパラメータ（モデル、システムプロンプト、ツール）のみを含めるように変更し、リクエストメタデータと `CLAUDE_CODE_EXTRA_BODY` フィールドは除外するように
- [VSCode] プロダクトフィードバックを無効化している組織で、「Report a problem」が表示され続け、`/bug` と `/feedback` がレポートフォームを開いていた問題を修正
- [VSCode] Windows で、完了したターンの後に赤い「Claude Code process exited with code 4294967295」バナーが表示されていた問題を修正
- Windows: `--add-dir` でマップされたネットワークドライブが追加された場合の UNC パスに対するネットワークパス権限チェックを改善
- [Claude Code on the web] 管理者がコネクタを削除して再追加した後、ルーチンが組織のコネクタへのアクセスを失い、古いコネクタを呼び出し続けていた問題を修正
- [Claude Code on the web] 組織設定からのセルフホスト環境の作成が、サーバーエラーで失敗し中途半端な環境を残すことがあった問題を修正
- [Claude Code on the web] 管理者の「Share cloud sessions」設定を、Claude Code ページから Data and privacy 配下に移動し、Data and privacy の管理者も管理できるように変更
- [Claude Code on the web] New routine ページや Edit routine ダイアログが、入力したルーチン名、プロンプト、編集内容を破棄する前に「Discard unsaved changes?」の確認を追加
- [Claude Code on the web] クラウド環境を持たない新規ユーザーが Mac と Windows で見ていた全画面のデスクトップアプリダウンロード画面を削除し、セットアップに直接進むように
- [Claude Code on the web] ルーチン詳細ページを改善: パンくずにメニューと名前変更、オン/オフスイッチと Run を上部に配置し、実行履歴をルーチン設定の隣に表示
- [Claude Tag] Enterprise Grid が切断されたがワークスペースの一つが接続されたままだった場合、アプリの再インストール後にしばらくして Claude が沈黙していた問題を修正
- [Claude Tag] 組織共有のプライベート Slack チャンネルで設定したスケジュールタスクが、静かに投稿されなくなっていた問題を修正。作成されたスレッドで実行し続けるように
- [Claude Tag] Claude が作業中に古い Slack スレッドで返信すると、作業がプッシュされずに失われ、最初からやり直されることがあった問題を修正
- [Claude Tag] アカウントトークンの更新直後に、Claude が誤った「couldn't find a Claude Code environment」通知とともにメッセージを取りこぼすことがあった問題を修正
- [Claude Tag] Budgets、Savings Plans、WAF Classic、Import/Export などリージョンのないエンドポイントを AWS 接続が拒否していた問題を修正。Global Accelerator のリクエストも正しく署名されるように
- [Claude Tag] AWS 接続の失敗を改善: ホスト名にリージョンがないなどリクエストに署名できない場合、単なるエラーではなく理由と対処法を示すように
- [Claude Tag] トークンタイプを小文字で返すプロバイダーで OAuth のクライアントクレデンシャルと JWT ベアラー接続が失敗していた問題を修正。リクエストは標準の Bearer スキームを送信するように
- [Claude Tag] Enterprise Grid の共有チャンネル、Claude が未使用のチャンネル、レガシーなプライベートチャンネルでチャンネルマネージャーの追加が拒否されていた問題を修正
- [Claude Tag] Claude が、会話が依存しているインシデントチャンネルなど関連する公開チャンネルを、依頼されなくても自ら監視し始めるように変更
- [Claude Tag] 管理者の Memory ページが、Claude が自ら設定した Slack チャンネルに保存済みメモリがあってもリスト表示していなかった問題を修正。管理者はそのメモリを開いて編集・削除できるように
- [Code Review] 「Additional findings」を含む以前のレビューがある PR にベースブランチをマージすると全体の再レビューがトリガーされていた問題を修正。これらのプッシュは今後より軽いフォローアップレビューになる
- [Code Review] @メンション、複数行にまたがるコードスパン、バッククォートで囲まれた HTML タグにより REVIEW.md 全体が無視されていた問題を修正。変更されたファイルにリンクする行のみが保留されるように
- 提案される修正で、変更する動作に他のコードが依存している場合に、その修正が維持すべき内容を示すように改善
- 2 番目の影響箇所を指すレビューコメントが、途切れたスタブではなく完全な文でその箇所の問題を示すように改善
- [Code Review] `/ultrareview --post` を修正し、GitHub エラー後のリトライで findings コメントがちょうど1回だけ投稿されるように（以前は投稿されない、または重複して投稿されることがあった）。コメントにはレビュー対象のコミットが明記されるように
- [Code Review] オーナー名またはリポジトリ名に大文字を含む GitHub リポジトリで、空またはコード内容が同一のプッシュが再レビューされていた問題を修正。これらのプッシュはスキップされるように
