---
version: "2.1.154"
date: 2026-05-28T15:40:50+00:00
---

- SDK: `Workflow` ツールの `args` フィールドのドキュメントを明確化 — 配列やオブジェクトは JSON エンコードされた文字列ではなく、実際の JSON 値として渡す必要がある（文字列化されたリストはスクリプト内の `args.filter`/`args.map` を破壊する）
- SDK: `Bash` ツール出力に `gitOperation` 構造化フィールドを追加。コマンド内で検出された git/gh 操作（commit/push/merge/rebase/PR）をクライアント向けに分類して提供し、stdout の再パースが不要になる
