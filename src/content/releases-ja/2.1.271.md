---
version: "2.1.271"
date: 2026-09-14T19:45:19+00:00
highlights:
  - "`/desktop` コマンドを追加"
---

- Claude Code Remote セッション(クラウドおよびセルフホストランナー)に fast mode を追加: ホスト側の fast mode 設定、またはセッション内で入力した `/fast` が、組織で許可されている範囲で適用されるようになった
- フルスクリーンモードの `/config` パネルにマウス操作を追加: ホイールで設定リストをスクロールでき、設定値のクリックで値を変更でき、ポインタ下の行がハイライトされるようになった
- `claude self-hosted-runner --drain-marker-file <path>` を追加: SIGTERM による drain 時にこのファイルが存在すると、ランナーはサーバーに対して host drain として終了を報告する(テレメトリ用途のみ)
- サンドボックス付き auto mode の Bash・PowerShell・Monitor に、コマンドごとの `allowed_domains` を追加: コマンドが必要とするホストがそのコマンドと共にレビューされ、そのコマンド専用に開放される。それ以外のホストは拒否される
- agent frontmatter と `--agents` の JSON に `omitClaudeMd` を追加: カスタム・プラグインのサブエージェントを user・project・local の CLAUDE.md なしで実行できるようになった(managed のポリシーファイルは引き続き読み込まれる)
- `claude plugin install` と `claude plugin update` に `--accept-command <sha256>` を追加: `-y` の代わりに、直前の `--json` 実行が表示したコマンドそのものだけを承認できるようになった
- `modelPricing` の managed 設定と Claude apps gateway の `pricing` ブロックで、社内チャージバック向けの上乗せ料金用に 1 を超え最大 10 までの `multiplier` をサポート
- Bedrock・Vertex AI・Foundry・LLM gateway のユーザーに Claude デスクトップアプリを案内するスピナーヒントを追加。claude.ai のデスクトップアプリのヒントは、アプリのダウンロードを提案する `/desktop` を案内するようになった
- アカウント・組織・API キーを切り替えた後に、キャッシュされた組織ポリシーが再利用されてしまう問題、およびセッション途中で認証情報が変わった場合にポリシーが1時間ごとのチェックまで更新されない問題を修正
- 起動後や途中で組織ポリシーの読み込みが完了した際に、ツールとコマンドの一覧が更新されない問題を修正
- 読み込みまたはパースに失敗したエンタープライズの `managed-mcp.json` が無視されていた問題を修正: 現在は MCP の排他制御を維持し(user・project・プラグインのサーバーは読み込まれない)、起動時に警告するようになった
- `ANTHROPIC_UNIX_SOCKET` で設定したサードパーティのローカルプロキシ経由で組織ポリシーが取得され、拒否されていた問題を修正: 他のカスタムゲートウェイと同様に扱われるようになった(Remote Control を含む)
- セッションのワーカーが再起動した後にワークフローまたはエージェントの承認が適用された場合、クラウドセッションがすべてのサブエージェントのツール呼び出しを("updatedInput … failed schema validation" で)拒否していた問題を修正
- 組織が fast mode を無効にしている場合に、`/fast off` が fast mode をオフにする代わりに "Fast mode unavailable" と応答していた問題を修正
- `CLAUDE_CODE_SKIP_FAST_MODE_ORG_CHECK` で起動したセッションが、API に fast mode を拒否された後も毎ターン fast リクエストを送り続けていた問題を修正: 拒否が維持され、理由が表示されるようになった
- `CLAUDE_CODE_RETRY_WATCHDOG` 下の fast mode が、使用クレジット上限でターンを失敗させたり、過負荷時に fast 速度でリトライしたりしていた問題を修正: 標準速度へのフォールバックが正しく行われるようになった
- `fmt`・`column` などのコマンドが読み取るファイルが、チェッカーが認識しないオプションの後に続く場合に Bash のパーミッションチェックから漏れていた問題を修正
- コマンドのパターンやオプション値内にワイルドカードがある場合(例: `grep -v dir/* file`)に、Bash のパーミッションチェックがそのワイルドカードが展開するファイルをスキップしていた問題を修正
- シェル変数宣言のフラグによって実行されるコマンドが偽装できてしまう問題を Bash のパーミッションチェックで修正
- ディレクトリ移動を2回行う・サブシェルを使う・`cd`+`git` を連結するといった Bash コマンドが、bypass モードおよび auto モードの `permissions.blockReadsOutsideWorkingDirectories` 下でプロンプトをスキップしてしまう問題を修正
- サンドボックス化されたコマンドの起動に失敗した後、古い `.git/config.lock` がセッションの残りの間、`git checkout -b`・`git push -u`・`git config` を壊してしまう問題を修正(Linux)
- macOS でシステムのファイルイベントサービスが飽和している場合に、セッション外で行われた設定ファイルの変更が検知されない問題を修正: ウォッチャーがポーリングにフォールバックするようになった
- ツールがすべて MCP サーバー由来である `claude -p` セッションを resume すると "At least one tool must have defer_loading=false" で失敗していた問題を修正
- LLM gateway が非ストリーミングの応答を `text/plain` として返した場合に、"API returned an empty or malformed response" でターンが失敗していた問題を修正
- MCP サーバーが `list_changed` 通知を高頻度で送り続けた場合に、CPU 使用率が高止まりしツール一覧リクエストが繰り返される問題を修正
- MCP OAuth のクライアント登録の扱いを修正: 同意を拒否すると新規登録が強制される問題、別のリダイレクト URI 用の登録が再利用される問題、同時書き込みによって有効な登録が削除されたり不一致な登録が残ったりする問題を修正
- Claude がフルネーム `mcp__server__tool` ではなく MCP ツールの素の名前を選んだ場合に、tool search が一致なしを返していた問題を修正
- Ctrl+O が保留中の MCP サーバー再接続をキャンセルしてしまう問題、およびトランスクリプトビューを開いた状態で Remote Control から送られた `/mcp` が失敗する問題を修正
- ToolSearch が利用できない場合でも、Claude in Chrome のプロンプトがモデルに ToolSearch 経由でのツール読み込みを指示していた問題を修正
- 受信側セッションのパーミッションモードのポリシーによって保留されたセッション間メッセージが、痕跡を残さず消えていた問題を修正: ヘッドレスの送信元には配信通知が届くようになり、`SendMessage` の結果はメッセージが読まれたことを意味しなくなった
- 会話が compaction された後も実行中だったバックグラウンドコマンド(watch タスクや dev サーバーなど)を、Claude が二重に起動してしまう問題を修正
- 会話が実際に実行していたモデルに戻した際に、`/model` がキャッシュ喪失の警告を表示していた問題を修正
- `/cd` の後、`/reload-skills` がスラッシュメニューと食い違うスキル数を報告していた問題を修正
- 背の低いターミナルのフルスクリーンモードで、`/resume` と `/continue` がセッションを1〜2件しか表示しない問題を修正
- `/resume` と `/teleport` が以前の会話のファイル読み取り追跡を引き継いでしまい、resume した会話が一度も読んでいないファイルを Claude が編集できてしまう問題を修正
- resume したセッションのモデルファミリーが設定済みのデフォルトモデルと異なる場合に、`--resume` が 1M コンテキストウィンドウ(`[1m]`)を落としてしまう問題を修正
- `/artifacts` で添付したアーティファクトが `--resume` 後にセッションから消えてしまう問題を修正
- バックグラウンドセッション(`claude --bg`、`claude agents`)が、公開したアーティファクトの他所での再公開を監視していなかった問題を修正
- inode 0 を返す仮想ドライブ(Windows ドライブとしてマウントされた暗号化ボールトなど)から、2つ目以降のカスタムエージェント・スラッシュコマンド・output style が読み込まれない問題を修正
- ホストの設定ディレクトリが 64 MiB を超えると、セルフホストランナーのセッションがホスト設定(設定・スキル・プラグイン・MCP サーバー)を静かに失っていた問題を修正: `--host-config-snapshot disk|memory` を追加
- claude.ai からサインアウトした後もそこから同期されたスキルがディスクに残り続けていた問題を修正: `cleanupPeriodDays` 内で更新されなかったコピーは、次回起動時に復元可能なゴミ箱へ移動するようになった
- 自分のアカウント種別で利用できない、またはセッションで無効化されているコマンドをスピナーヒントが提案していた問題を修正
- `/add-dir` のパス入力を修正: 左右矢印キーでカーソルを移動できるようになり、Enter はハイライトされた補完候補ではなく入力したパスのみを追加するようになった
- メインのプロンプト以外のテキストフィールドで、先頭の `!` が入力の末尾に移動してしまう問題を修正(`!foo` が `foo!` になっていた)
- 継承されたオブジェクトのプロパティ名(`__proto__` や `constructor` など)と同名の hook マッチャーがある場合に、インタラクティブな `/hooks` メニューがクラッシュする問題を修正
- 背景色を失った枠の中のテキストが、古い背景色を保持し続けるフルスクリーン描画の不具合を修正
- アタッチしたバックグラウンドセッションで、st の Delete と rxvt-unicode の Alt+矢印キーが動作しない問題を修正
- Claude Code の終了時・サスペンド時・起動直後のエディタ起動時に、ターミナルの機能クエリへの応答(`^[[?1;2c`)がシェルプロンプトやエディタに表示されてしまう問題を修正
- ターミナル描画のパフォーマンスを改善: 大きな diff や長いトランスクリプトの描画が速くなり、遅いフレームが減少
- 毎回の起動時に行っていた組み込みモデルデータの冗長な検証をスキップすることで、起動時間をわずかに改善
- hook のフィードバックを改善: SessionStart・UserPromptSubmit・PreToolUse・SessionEnd の hook が実行中であることをスピナーが経過時間とともに表示し、SessionStart hook 待ちのプロンプトは Esc でキャンセルできるようになった
- 長い思考中のスピナー表示を改善: 45秒後には「deep in thought」と表示され、出力トークン上限からの復帰中は「picking the thought back up」と表示されるようになった
- 使用量上限に達した際に、影響を受けたエージェントを打ち切るのではなく、動的ワークフローを一時停止し上限リセット後に自動再開するよう改善
- 不安定なネットワークでセットアップが失敗した際に、Remote Control が claude.ai に残す空セッションを減らすよう改善
- クラウドセッションでブラウザに到達できない場合の Claude in Chrome のメッセージを改善: インストールを提案する前に、コンピュータがスリープ中の可能性があることを伝えるようになった
- `claude mcp serve` を改善: 実行中のツール呼び出しが30秒ごとに進捗更新を送るようになり、クライアント側で実行中であることが分かるようになったほか、何も出力しない長時間コマンドがアイドルタイムアウトで中断されなくなった
- Foundry および AWS 上の Claude Platform のセッションを改善: 会話の途中で接続が完了した `alwaysLoad` の MCP サーバーが、tool search の往復なしで次のターンから使用できるようになった
- アーティファクトとして公開された Markdown ファイルを改善: タイトルヘッダー・ドキュメント用タイポグラフィ・シンタックスハイライト付きコードを備えたスタイル付きドキュメントページとして描画されるようになった
- Artifact ツールの公開エラーを改善: ファイルなしでの公開はまずページをファイルに書き出すよう案内し、非対応のファイル形式は favicon 不足より先に報告されるようになった
- ページが持つ contract バージョンにない機能を宣言した場合の Artifact ツールのエラーを改善: サポートされている全機能を列挙し、より新しい contract バージョンにその機能がある場合はその旨を示すようになった
- アーティファクトの監視を改善: 他所での再公開を監視できる公開済みアーティファクト数の上限が、5件から10件に増加
- pdfinfo がページ数を数えられない場合、PDF の @-mention はファイルサイズから推測したページ数の代わりに「ページ数不明」と表示するよう改善
- `/mobile` を改善: claude.ai/mobile への単一の QR コードを表示し、スマートフォンに応じた適切なアプリストアを開くようになった
- auto mode を変更: スキルやスラッシュコマンドのインライン `!` シェルコマンドは、分類器ではなくデフォルトモードのパーミッションルールに従うようになった。どのルールにも該当しないコマンドは、レビュー済みのツール呼び出しとして実行される
- auto mode を変更: サブエージェントは、最後のメッセージが事後的にレビューされる代わりに、専用の hand-back 呼び出しを通じて呼び出し元に報告するようになり、この呼び出しが安全性分類器によってレビューされる
- Monitor の watch を変更: タイムアウトなしの `persistent` オプションを廃止し、常に締切(最長30分、単発の `-p` 実行では10分)を持つようにして、Claude に再設定を通知するようになった
- プロンプト内の IDE 選択インジケータを変更: 複数行のプロンプトを圧迫しない、テキストと一緒に折り返される `[⧉ …]` のピル表示になった。Backspace で削除すると選択範囲を含めずに済む
- 動的ワークフローのデフォルトサイズを Pro プランでは small に変更し、medium サイズの目安を15から10エージェントに引き下げ
- Claude apps gateway・Bedrock・Vertex AI・Foundry のセッションを変更: セッションが使用しない claude.ai の残存ログインを更新しなくなった
- 同梱の `claude-api` スキルを更新: ストリーミングのカスタムツールで `eager_input_streaming` を有効化し、`user.define_outcome` で成果物指向の Managed Agents の作業を開始するようになった
- [VSCode] Attach Open File 設定を追加: オフにすると開いているファイルがメッセージに追加されなくなる(選択したテキストは引き続き添付される)
- [VSCode] Hooks・Permission rules ダイアログが、実際には失敗した保存を成功と報告していた問題、およびプラグイン専用ポリシーロック下で Hooks ダイアログが空白になったり保存エラーでカラーコードが表示されたりする問題を修正
- [VSCode] Hooks ダイアログの保存を修正: 置き換え時に hook が重複しなくなり、ヘッダー名を別の大文字小文字で入力し直してもシークレットが保持され、保存が完了する前に settings.local.json が gitignore されるようになった
- [VSCode] ワークスペースが Windows のマップされたネットワークドライブや SUBST ドライブ上にある場合に、セッション履歴が現在のセッションしか表示しない問題を修正
- [VSCode] Open もチェックされている場合に、Active フィルタが開いているアイドルセッションを隠してしまう問題を修正
- [VSCode] セッション一覧が更新された際に、新しいチャットが直前のチャットに切り替わってしまう問題を修正
- [VSCode] `environmentVariables` 設定で `CLAUDE_CONFIG_DIR` を変更した後、開いているタブとサイドバーがウィンドウ再読み込みまで古い設定フォルダを保持し続けていた問題を修正
- [VSCode] 拡張機能が git・ripgrep・サインイン状態チェックなどのバックグラウンドコマンドを実行する際に、Windows でコンソールウィンドウがちらつく問題を修正
- [VSCode] プロンプトキャッシュ時計のホバーテキストが遅延してしか表示されない問題、および auto-compact アイコンがポップアップの横でブラウザ自体のツールチップを表示してしまう問題を修正
- [VSCode] Hooks ダイアログを改善: 設定ファイル自体が原因で保存が拒否された場合、"Open settings file" ボタンと "Copy error" の背後にある理由を示すポップアップが開くようになった
- [VSCode] トグルスイッチの ON 状態の色を、Claude オレンジからエディタテーマのボタン色に変更
- [Claude Code on the web] プロセスが終了した後もセッションが生きているように見える状態から応答までに約10分かかることがあった問題を修正: メッセージを送信すると即座にセッションが再起動するようになった
- [Claude Code on the web] claude.ai/code の Routines ページを、Yours と Templates タブおよび実行状況を示す2カラムの routine カードを持つ新しいレイアウトに変更し、カレンダービューを削除
- [Claude Code on the web] 管理者設定の Cloud environments エディタに、環境ダイアログ(claude.ai/code)と同じ許可ドメインリストを持つ Custom network access オプションを追加
- [Claude Code on the web] Cloud environments の管理者ページを改善: Claude Tag と Claude Code のデフォルト環境を表示し、変更用リンクを設け、推奨される種類を示すようになった
- [Claude Tag] 会話の大部分がスレッド内である場合、アクティブなままのチャンネルで Claude が約1時間ごとに作業コンテキストを失っていた問題を修正: スレッドでの活動によりリセットされなくなった
- [Claude Tag] プルリクエストの監視を依頼されたスレッドが、Claude の再起動後は CI 失敗・コメント・レビューを受け取らなくなっていた問題を修正
- [Claude Tag] Claude がすでに返信していたスレッドの最初のメッセージを削除しても、そこでの Claude の作業が終了しない問題を修正: 返信のないメッセージが削除された場合と同様に停止するようになった
- [Claude Tag] 別の bot やアシスタント宛ての以前の指示のせいで、Claude が投稿を控えてしまう問題を修正: Claude 宛ての指示のみが Claude を拘束するようになり、不明な場合は確認するようになった
- [Claude Tag] 忙しいチャンネルに参加した際に Claude が投稿する reply-mode カードが、単に賑やかまたは大規模なチャンネルに対して「自動投稿が多く見える」と表示していた問題を修正: カードが実際の理由を示すようになった
- [Claude Tag] Claude Tag 管理者設定の Environment ピッカーを改善: 選択肢に Anthropic ホストか自己ホストかのラベルが付き、その環境の編集や新規作成へのリンクが表示されるようになった
- [Code Review] PR ごとに1回レビューするリポジトリで、レビュー待ち中に新しいコミットが届くとレビューが行われないことがあった問題を修正: 依頼されたコミットを確実にレビューするようになった
- [Code Review] GitHub がレビュー作成成功時にエラーを報告した場合に、Code Review が同じ指摘を2〜3回投稿してしまうことがあった問題を修正
- [Code Review] 後続のプッシュで指摘のアンカー行が移動した場合に、フォローアップレビューが既に解決済みのセキュリティ指摘を再投稿してしまう問題を修正
- [Code Review] 完了済みの /ultrareview クラウドセッションを Claude アプリで再度開くと、意図せずレビュー全体が最初からやり直されてしまう問題を修正
- Windows: セッションの一時出力パスが260文字に達すると、PowerShell コマンドが出力なしで "Exit code 1" として失敗する問題を修正
