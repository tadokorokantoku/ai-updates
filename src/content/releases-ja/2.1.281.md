---
version: "2.1.281"
date: 2026-09-23T17:01:17+00:00
---

- `desktop` ポリシーブロックに、`blockReadsOutsideWorkingDirectories` や `disableBypassPermissionsMode` を含む、より新しい Claude Desktop 用のキーに対する Claude apps gateway サポートを追加
- Claude apps gateway の Bedrock アップストリームに `assume_role` を追加: gateway は STS 経由でアシュームした IAM ロールとして Bedrock を呼び出せるようになり、必要なら別の AWS アカウントで、開発者ごとに 1 セッションを分けることも可能
- Claude apps gateway の Bedrock アップストリームに `guardrail: {id, version}` を追加し、それらを経由するすべてのリクエストに Amazon Bedrock のガードレールを適用できるように(すべての Bedrock アップストリームに設定するか、まったく設定しないかのどちらか)
- Claude apps gateway の設定に `telemetry.resource_attributes` を追加し、Claude Desktop と `/login` セッションのテレメトリに固定ラベルを付けられるように
- `settings.json` に `"attribution": false` を追加し、コミットと PR の attribution をすべて非表示にできるように。古い CLI バージョンはこれを保持する設定ファイルをスキップするため、複数バージョン間で共有するファイルではオブジェクト形式を維持すること
- 2026-07-28 プロトコル接続に MCP の URL モード elicitation を追加し、サーバーが Claude Code にブラウザベースのフローを開くよう依頼できるように。サーバー側に完了を確認する手段がない場合、画面に待機ダイアログが残らないように
- `claude plugin validate` に MCP サーバーのチェックを追加: 読み込み時に無言でドロップされる `.mcp.json` のエントリ、未宣言の `${user_config.*}` 参照、安全でない URL を報告するように
- `/insights` に auto mode の推奨を追加し、直近のセッションで auto mode がどれだけの許可プロンプトを処理できたはずかを推定するように
- フルスクリーンモードの `/skills`、`/mcp`、`/plugin` の Installed リストにスクロールバーを追加。`/workflows` に既にあるものと同様に、マウスがリスト上にある間表示され、クリックまたはドラッグできる
- API リクエストのリトライ中にセッションを終了させることがあった("unrecoverable interface error")クラッシュを修正
- モデルがパースできないツール呼び出しと出力上限による切り詰めを交互に返したとき、`--max-turns` を無視して無限にリトライし続けてしまうターンを修正
- 再開されたセッションが以前のターンを形を変えて再送信してしまう問題を修正(並列ツール呼び出しのターン、サーバーがまだ再接続中の MCP ツール呼び出しの入力、または読み込み中のターンが中断された tool-search の結果など)。これにより API が会話の以前の推論を破棄してしまうことがあった
- 非常に大きなセッションを再開したとき、直近の数メッセージしか復元されないことがある問題を修正
- 保留中の許可プロンプトの最中に再起動後に再開されたセッションが、以前と異なる履歴を送信してしまい、その時点からプロンプトキャッシュが壊れる問題を修正
- ツール呼び出しの最中に終了したセッションの再開を修正: Claude は今後その呼び出しを認識し、結果が不明であることを伝えられるように。手動での再開でも隠れた "Continue" メッセージが追加されなくなった
- API がもはや読み込めなくなった以前の advisor の結果を持つセッションが、毎ターン 1 リクエストずつ失敗し、以前の推論を繰り返し失ってしまう問題を修正。履歴は今後一度だけ修復される
- tool search がオフの状態で(例えばプロキシやゲートウェイの背後で)MCP サーバーが会話の途中で切断される、または再開後にまだ接続中のときに、プロンプトキャッシュが失われる問題を修正
- ストリームをきれいに閉じるプロキシやゲートウェイによって短く打ち切られたレスポンスが、警告なしで完了として表示されてしまう問題、および重複したストリームイベントでツール呼び出しが二重に実行されてしまう問題を修正
- プロキシがレスポンスの途中でストリームイベントをドロップしたときに "Content block not found" で失敗する問題を修正。部分的なレスポンスは今後保持され、web 検索も既に届いた結果を保持するように
- ストリームの最終イベントより前に接続が切れたとき、空の完了レスポンスが 2 回リクエストされてしまう問題を修正
- プロキシが末尾に usage のみのフレームを送信したときに stop reason が失われる問題を修正
- `CLAUDE_CODE_RETRY_WATCHDOG` セッションが、429/529 の待機が連続した後の最初の 5xx や接続切断で失敗してしまう問題、および 5xx の長い `Retry-After` で無制限かつ無言でスリープしてしまう問題を修正
- サーバーが `Retry-After: 0` を送信したとき、fast mode がレート制限されたリクエストを連続でリトライしてしまう問題を修正
- 過大なサイズの画像を返すツールが、兄弟のツール呼び出しを未応答かつ実行中のままにしてしまう、またはターンが最終メッセージなしで終了してしまう問題を修正
- モデルが過度に長い名前でツールを呼び出した後、会話が "tool_use.name: String should have at most 200 characters" で永続的にスタックしてしまう問題を修正
- Claude Code が自身のメモリ使用量を読み取れないとき(例えばファイルディスクリプタが枯渇したとき)、ツール呼び出しが "Failed to get memory usage" で失敗する、または実行後に失敗として報告されてしまう問題を修正
- 以前のアシスタントメッセージがプレーン文字列のコンテンツを持っていたとき、`--input-format stream-json` セッション(Agent SDK、VS Code 拡張機能)とスケジュールされたクラウドセッションが毎ターンエラーで失敗する問題を修正
- 起動元のディレクトリがセッション中に削除されたとき、非対話型セッション(`-p`、Agent SDK)が次のターンで失敗する問題を修正
- ホストが handshake の途中で応答を止めたとき、ホスト側(SDK)の MCP サーバーを持つヘッドレスセッションが最初のメッセージでスタックする問題を修正。リモートセッションは今後最大でも数秒待つように
- MCP サーバーやプラグインが設定されていないとき、対話型の起動が managed settings のネットワークリクエスト(通常約 80ms、ネットワークに到達できない場合は 17 秒以上)を待ってしまう問題を修正
- 3MB を超える PDF を読み込む、または @ メンションしたときに、応答までに最大 2 分の遅延が発生する問題を修正
- PDF の特定ページの読み込みが中断されたとき、そのページのレンダリングが最大 2 分間実行され続けてしまう問題を修正
- 承認前に、macOS の `/.vol`、`/.nofollow`、`/.resolve`(ネットワークマウントに到達しうる)配下のパスを許可プロンプトや添付ファイルのチェックが読み取ってしまう問題を修正
- `rm -rf "$(pwd)"` のようにコマンド置換の出力のみがターゲットとなる再帰的な `rm` が、auto mode や `--dangerously-skip-permissions` モードで無確認のまま実行されてしまう問題を修正。今後は Bash の allow ルールがあっても確認を求めるように(`CLAUDE_CODE_DISABLE_SUBSTITUTION_RM_PROMPT=1` で無効化可能)
- NUL バイトを含む許可ルールがワイルドカードマッチに展開されてしまう問題を修正。そのようなルールは今後何にもマッチしないように
- sandbox の `excludedCommands` エントリが `git rev-parse --git-dir`、シェルの組み込みコマンドのような名前のプログラム、`[WIP]` や `#` 行を含むコミットメッセージにマッチしない問題を修正
- `CLAUDE_CODE_TMPDIR` が設定されているとき、サンドボックス化された Bash コマンドが `$TMPDIR` に書き込めない問題を修正
- `claude --bg` が、ワークスペースの信頼プロンプトを通過していないディレクトリでバックグラウンドセッションを開始し、そのプロジェクトフックを実行してしまう問題を修正。今後は先に信頼を確認し、対話的に実行されていない場合は終了するように
- `--setting-sources`(および SDK の `settingSources`)が、起動されたセッション(teammate、`/bg`、`claude agents` セッション、`--worktree --tmux`)に引き継がれない問題を修正。今後は親の制限で開始されるように
- Read、Write、Edit、NotebookEdit を修正: null バイトを含むファイルパスは、ターン全体を終了させる代わりに、明確なエラーでそのツール呼び出しのみを失敗させるように
- 同じ値を持つ 2 つの異なるパラメータ名でファイルパスやコンテンツを二重に渡す呼び出しを Write が拒否してしまう問題を修正
- 作業ディレクトリ内の `--add-dir` ディレクトリからの CLAUDE.md やルールファイルが、ヘッドレスセッションと SDK セッションでモデルに二重送信されてしまう問題を修正
- 許可プロンプトと sandbox のネットワークアクセスプロンプトが重なって両方に回答されたとき、リモートセッションが古いプロンプトのまま "needs approval" に留まってしまう問題を修正
- ワーカーの再起動直前に完了したバックグラウンドエージェントについて、クラウドセッションが Claude に伝えない問題を修正
- リモートセッションでスケジュールされた routine や通知のターンが、最初のツール呼び出しまでターン開始通知(新しく利用可能になったツール、MCP の変更、日付、todo)を受け取らない問題を修正
- 配信が失敗したとき、スケジュールされたタスクや `/loop` の wakeup が毎秒再度発火してしまい、ターンの終わりに Claude Code が終了してしまうことがある問題を修正
- 組織のポリシーがまだ読み込まれていないだけなのに、Remote Control が "disabled by your organization's policy" と報告してしまう問題を修正。今後はフェッチをリトライし、確認できなかった旨を伝えるように
- Claude Desktop、claude.ai、モバイルアプリから開くために `claude remote-control` が起動する Remote Control セッションから、Artifact ツールが欠落する問題を修正
- ログインキーチェーンがロックされていたとき(例えばスリープ復帰直後)、macOS の認証情報の書き込みが保存済みの MCP OAuth トークンを失う、またはキーチェーンのエントリを削除してしまう問題を修正
- Claude Code が終了する、またはリフレッシュがタイムアウトしたときに、`gcpAuthRefresh`/`awsAuthRefresh` のログインプロセスが実行されたまま残ってしまう(Windows ではローカルホストのコールバックポートを保持したまま)問題を修正
- 別の Claude Code プロセスからログインした後、セッションに "Not logged in · Run /login" のフッターと claude.ai コネクタの欠落が残ってしまう問題を修正
- ブロッキングイベント(PreToolUse など)の `mcp_tool` フックが、その MCP サーバーがまだ接続中の間スキップされてしまう問題を修正。今後は MCP の接続タイムアウトまで待つように
- プラグインや claude.ai コネクタと設定済みサーバーが URL の綴り方(ホスト名の大文字小文字、デフォルトポート、末尾のスラッシュ)を違えて記載しているとき、同じ MCP サーバーが二重に接続されてしまう問題を修正
- `MCP_CONNECTION_NONBLOCKING=0` が、`MCP_CONNECT_TIMEOUT_MS` を尊重せずに claude.ai コネクタを 1 秒で諦めてしまう問題を修正
- `--channels` のプラグインエントリが、インストール済みプラグインのマーケットプレイスのみと照合されてしまう問題を修正。今後はインストール済みプラグインの名前もエントリと一致する必要がある
- `.claude-plugin/marketplace.json` も持つプラグインのフォルダに対して `--plugin-dir` を指定すると、そのフォルダ内のプラグインの代わりに空のプラグインが 1 つ読み込まれてしまう問題を修正
- `claude plugin uninstall` が、有効化されていないプロジェクトスコープのプラグインの削除を拒否し、"enabled at project scope" と表示してしまう問題を修正(`claude plugin disable` は既に無効と表示する)
- `--scope` を省略したとき、`claude plugin update` がプロジェクトスコープのプラグインで失敗する問題を修正。今後は user と決めつけず、プラグインが実際にインストールされているスコープを解決するように
- `claude plugin validate` が、plugin.json 内の `privacyPolicyUrl`、`supportUrl` などのリスティングメタデータキーを未知のフィールドとして報告してしまう問題を修正
- リモートに到達できず `CLAUDE_CODE_PLUGIN_KEEP_MARKETPLACE_ON_FAILURE` が既存のクローンを保持したときに、`known_marketplaces.json` がそのマーケットプレイスを更新済みとして記録してしまう問題を修正
- `/plugin` の Errors タブが、最後のエラーが解決された後も確認表示を出さない問題を修正
- 最初のアンインストールや更新がまだ実行中のときに Enter が再度押されると、`/plugin` が同じプラグインの 2 回目のアンインストールや更新を開始してしまう問題を修正
- "Add marketplace?" の質問が表示された瞬間に、`/plugin` がマーケットプレイスのソースを確認している間押しっぱなしだった `y` がそれを読める前にマーケットプレイスを追加してしまう問題を修正
- ポインタが No にある状態で `/permissions` の削除やディレクトリ削除の確認画面で `1` が Yes に応答してしまい、`1` を押しっぱなしにするとワークスペースディレクトリが次々に削除されてしまう問題を修正
- Alt+T と `/config` が、thinking をオフにできないモデルでもオフにする選択肢を提示してしまう問題を修正。今後はそこでは thinking がオンのままとなり、スイッチの代わりに理由が一行で表示される
- `/context` の合計が、直前の応答以降に追加されたメッセージを含めていない問題を修正。今後は各カテゴリと一致し、ステータスラインより高い値を読み取れるように
- API が選んだモデルを拒否したとき、`/model` が生の API エラー JSON とリクエスト ID を表示してしまう問題を修正。今後はサーバーのメッセージを表示し、モデルは変更されなかったと伝えるように
- プロキシのエラーページ(429 や 502 ページなど)からの HTML エラーページのマークアップがそのまま表示されてしまう、または HTTP ステータスが欠落してしまう問題、およびサーバーのエラーテキストが改行で終わっているときにエラーメッセージが 2 行目に折り返されてしまう問題を修正
- /feedback、/bug、/share が、送信中にキャンセルした後もレポートを送信し続けてしまう問題を修正
- ダイアログが開いている間に Remote Control の Stop が届いた後、/feedback、/bug、/share が毎回 "Couldn't send feedback" で失敗する問題を修正
- 実行中の IDE が一覧に表示されているにもかかわらず、`/ide` が "No available IDEs detected" と表示してしまう問題を修正
- `/setup-bedrock` や `/setup-vertex` が新しい設定を適用するために Claude Code を再起動するとき、ターミナルが壊れた状態(クラッシュや文字化けした入力)のまま残ってしまう問題を修正
- `~/.claude.json` の `respectGitignore` や `copyFullResponse` が `null` を保持しているとき、`/config` が終了してしまう問題を修正
- Claude が選択式の質問をしている間、/rename によるセッション名がプロンプトから消えてしまう問題を修正。並べて表示されたセッションを識別できるように
- VS Code や Remote Control からのプロンプト、および展開されたペーストのプレースホルダーで、1 行のペーストが送信メッセージ内で複数行に分かれて表示されてしまう問題を修正
- Claude が作業中にキューに入れられたメッセージが、書き込まれた時点の IDE 選択を失う、または変更してしまう問題、およびキューに入れられたメッセージがその選択を表示しない問題を修正
- Shift+Tab を素早く 2 回押すと、誤った許可モードになってしまう問題を修正
- `/memory`、`/hooks`、`/mcp`(サーバーのサインイン画面を含む)、`/export`、`/copy`、`/theme`、および `/teleport` の未コミット変更確認とログインプロンプト(Esc でも終了してしまっていた)といった残りのダイアログやピッカーで、Ctrl+C や Ctrl+D を 2 回押すとダイアログを閉じる代わりに Claude Code が終了してしまう問題を修正
- 1 回の入力バーストで届くキー(Remote Control 経由など、矢印キーに続いて Enter、`x`、`s` など)が直前の選択に対して作用してしまう問題を修正: `/effort` とモデルピッカーの古い effort レベル、`/skills` の直前にハイライトされていた行、プロンプト下のバックグラウンドタスク行、MCP サーバーのプロンプト、`/install-github-app`
- `/install-github-app` が "Skip workflow update" を選んだ後もワークフローを更新してしまう問題、Enter を繰り返し押すとセットアップが 2 回実行されてしまう問題、リポジトリが検出されなかったときにリポジトリステップで ↑ を押すと入力済みのリポジトリ名がブロックされてしまう問題を修正
- vim モードを修正: `dj`/`dk`/`dG`/`dgg` とその `c`/`y` 形式が行の一部にしか作用しない問題、`1G` が最終行に移動してしまう問題、`d0`/`c0`/`y0` が何もしない問題、`.` で挿入を繰り返した後にカーソルが 1 文字ずれる問題、`!` で始まる行での `o`/`p` がシェルモードに切り替わってしまう問題
- vim モードの `cw` を修正: スペース、空行、単語の最後の文字、1 文字の単語に対して実行すると次の単語まで変更してしまう問題、ヒンディー語やベンガル語などのスクリプトで単語移動が単語の途中で止まってしまう問題、`!` で始まるテキストを挿入する `.`、`p`、`P` がシェルモードに切り替わる、テキストを失う、または誤った文字を編集してしまう問題
- 単独のキーとして入力されたアクセント記号の後、プロンプトのカーソルが 1 文字余分に進んでしまう問題を修正
- スクリーンリーダーモード、引用されたリスト、長いリストで、箇条書きの次の行から始まるテキストを持つリスト項目の上に余分な空行が入ってしまう問題を修正
- 単純な数字の箇条書き(`- 316.` など)が文字やローマ数字、誤った数字として表示されてしまう問題を修正
- エージェントパネルのフッターヒントが `keybindings.json` で再割り当てされたキーを無視してしまう問題、および stop-all-agents のショートカットが未割り当てのときに余分な ` · ` が表示されてしまう問題を修正
- 既に表示中のエージェントに対してエージェントパネルのフッターが "Enter to view" と "x to stop" を提示してしまう問題(x がその入力欄に入力されてしまう)、main が既に表示されているときに main の行で "Enter to view" が表示されてしまう問題を修正
- エージェントパネルの行をマウスでクリックすると、キーボードのカーソルが以前選択されていた行に残ってしまう問題を修正
- Esc が、選択中のエージェントパネル行の選択解除ではなく、実行中のターンを中断してしまう問題を修正
- フルスクリーンモードで、ダイアログのリスト(`/skills` など)で PgUp と PgDn が何もしない問題を修正
- `/heapdump` のサマリーが、JS ヒープスナップショット内のメモリを native と表示してしまう問題を修正
- Bash の edit-diff スナップショットディレクトリが一時フォルダに溜まってしまう問題を修正。放棄されたものは今後すぐに削除され、残りは Claude Code の終了時に削除される
- リストが開いている間に新しい実行が開始されると、/workflows がポインタを別の実行に移動させてしまう、`x` がその実行を停止してしまう問題を修正
- タブ付きダイアログ(`/config`、`/plugin`、`/permissions`)で、色がオフ(`NO_COLOR`)のときにタブバーにフォーカスがある間、選択中のタブがハイライト表示されない問題を修正
- `/plugin` の Installed リストの上でのマウスホイールが、リストではなく背後のペインをスクロールしてしまう問題を修正
- フルスクリーンモードでスクロールやフィルタでマウスから離れた後も、ホバーハイライトがリストの行に残ってしまう問題を修正
- `/remote-control` メニューなどの長いリスト行が、狭いターミナルで 2 行目に折り返されてしまう問題を修正。今後は … で切り詰められる
- `/hooks` と `/mcp` の詳細ビューが、狭いターミナルで長い値を下の行の上に重ねて表示してしまう問題を修正
- `/plugin` のスキルの状態オプションなどのリストが、スクリーンリーダーモードで数字入力による回答に対応していない問題を修正
- Windows: `$TMPDIR/…` に書き込む Bash コマンドが "Permission denied" で失敗する問題を修正
- Windows: 同時に更新される Claude Code セッションが互いの `claude.exe` バックアップを削除してしまい、`claude.exe` が残らなくなることがある競合状態を修正
- Claude Desktop のサインインと使用量上限のエラーメッセージを改善し、ターミナルコマンドではなくアプリを案内するように
- 起動時を改善: managed settings とポリシーのフェッチが、決して成功しないリクエストをリトライしなくなった
- 対話型の起動時間を改善: git の読み取り、起動時のテレメトリ、Bedrock/Vertex のモデルアップグレードチェックが、最初のフレームより前に実行されなくなった
- 多くのファイルを読み込む長いセッションの再開時間を改善。復元されたファイルキャッシュが読み込まれた時点のファイルと一致するように
- compact 済みの非常に長いセッションの再開時間を改善。Agent SDK や Claude Desktop 経由で特に顕著
- 1 つの非常に大きな最初のプロンプトが支配的なセッションでの "Prompt is too long" からの復旧を改善: そのプロンプトはサマリーから除外される代わりに、単独でサマライズされるように
- 新しいプロセスでセッションを再開した後の auto mode を改善: permission classifier が以前のプロンプトキャッシュを書き直す代わりに再利用できるように
- auto mode の拒否メッセージを改善し、Claude が拒否を正確なコマンドだけでなく結果全体を対象とみなすように
- 危険な rm のチェックを改善し、シェル変数に続くトップレベルのディレクトリ名、作業ディレクトリから導出された変数、バックスラッシュのみのターゲットに対する削除もフラグするように
- macOS 上の sandbox のガイダンスを改善: ローカルの開発サーバーがポートをバインドできないとき、Claude は今後 `sandbox.network.allowLocalBinding` を案内するように
- `--agents` を改善し、インラインの JSON に加えて(`-p` と併用時)JSON ファイルへのパスも受け付けるように、また空の `prompt` を許可するように
- `/batch` を改善し、WorktreeCreate フックがエージェントの worktree を提供する場合には、git リポジトリ内でなくても実行できるように
- プラグインのフック失敗エラーを改善し、問題のプラグイン名を示すように。またシェル形式のフックが `${CLAUDE_PLUGIN_ROOT}` をクォートしていない場合(スペースを含むプラグインパスで壊れる)、`claude plugin validate` の警告を追加
- `/` メニュー、`/skills`、`/context`、`/plugin` の Installed リストを改善し、他のコマンドがその短縮名を使用していない場合、claude.ai から同期されたスキルを `anthropic-skills:<name>` ではなく短縮名で表示するように
- scope ステップの出力から未使用の必須フィールドを削除することで、長いリサーチブリーフでの `/deep-research` の信頼性を改善
- 公開された artifact ページの文章を改善: バンドルされた artifact-design スキルが、簡潔で直接的な文章を Claude に求めるように
- 低速な接続での artifact 公開を改善: 大きなページのアップロードが今後圧縮して送信されるように
- 大きな CLAUDE.md の起動時通知を改善し、指示ファイルを合算してカウントするように。多数の中規模ファイルや @-import も検出されるように
- デバッグログを改善し、セッションの起動環境が既に設定しているために無視された settings の `env` 変数の名前を示すように
- `/permissions` や `/usage` などのタブ付きダイアログでのキーボード操作を改善: ↑/↓ でタブ行とコンテンツ間でフォーカスを移動でき、リストはフォーカスがある間のみキーに反応するように
- `/help` と `/sandbox` を改善: タブのリスト内から ←/→ と Tab でタブを切り替えられるように。`/help` の空の Custom commands タブで ↓ を押してもキーが Esc まで固まらなくなった
- `/install-github-app`、`/desktop`、`/permissions` の auto mode 環境プロンプト、`/plugin` の "Add marketplace?" と "Run this command?" プロンプトを改善: 標準のダイアログフレームとキーヒントを使用するようになり、他のダイアログと同様に Ctrl+C や Ctrl+D を 2 回目でキャンセルできるように
- `/workflows` と `/mcp` のリストを改善: 他のリストと同様にページング(PgUp/PgDn、Home/End)でき、j/k とマウスにも対応。矢印キーは `select:previous`/`select:next` の再割り当てに従い、`/workflows` の `x` はポインタのある実行を停止するように
- `/plugin` のプラグインとマーケットプレイスの詳細メニュー、`/remote-control` の接続済みメニューを改善: Home/End と行のクリックに対応するように
- プロンプト下のバックグラウンドワークフロー行を改善: 名前、進捗バー、広いターミナルでのエージェント数、経過時間、合計トークン数、大規模ワークフローの警告を表示するように
- /plugin の Installed リストを改善: 行が各セクションを通じて列(status、name、type、details)に揃うように
- `/skills` を改善: 各行がスキル名から始まり、✔ または ◯ 単独でオン/オフを表示し、狭いターミナルでも 1 行に収まるように
- 狭いリスト行(`/skills`、`/workflows`、`/feedback`)を改善: 名前は最初の詳細情報の横で 20 桁を保持し、詳細情報は全体表示されるかまったく表示されないかのどちらかになるように
- `/diff` を改善: スクロールバーで変更されたファイルの長いリストのどこにいるかを示し、長いパスがその行を折り返さなくなった
- `/hooks` を改善: フックの詳細画面が、常に settings.json を指すのではなく、そのフックの種類と変更場所を示すように。フック無効化、safe mode、managed-hooks-only の通知はそれぞれ何が起きているかを一文で明示するように
- `/mcp` のスクリーンリーダー出力を改善: 無効化されたサーバーが "pending" ではなく "off" として読み上げられるように
- Remote Control の確認を改善: ターミナルウィンドウがフォーカスを取り戻した直後、その選択肢が一時的に再度非アクティブになり、切り替え中に押されたキーが回答として扱われないように
- send now(ctrl+enter または ctrl+x ctrl+s)を変更し、ターンをキャンセルする代わりに実行中のツールをバックグラウンドに移すように
- classifier のレビューがサーバー側で実行される場合、auto mode を変更し、読み取り専用かつサンドボックス化されたシェルコマンドもそのレビューを待ち、フラグされた場合はブロックされるように
- `CLAUDE_CODE_AUTO_MODE_SERVER` を変更し、Anthropic API への直接接続でも適用されるように: `0` はサーバー側の auto mode classifier をオプトアウトし(その場合ローカルの classifier が使用量にカウントされる)、`1` はオプトインする
- `--dangerously-skip-permissions` と auto mode における危険な `rm` プロンプトを変更し、応答を 2 分間待った後、書き換えのヒント付きでコマンドを拒否するように。これにより無人セッションは動作を継続できる(`CLAUDE_CODE_DISABLE_DANGEROUS_RM_TIMEOUT=1` でこの動作をオフにできる)
- `managedMcpServers` エントリの `envHelper` パスが `\??\` や `/??/` で始まる場合、Claude apps gateway が起動を拒否するように変更。これは現行の Claude Desktop が実行を拒否するパス形式
- セルフホストランナーを変更し、システムプロンプトをコマンドライン文字列ではなくプライベートファイルとして Claude Code に渡すように。これにより大きなプロンプトが起動を失敗させなくなった。`--system-prompt` や `--append-system-prompt` を追加するラッパーや `command` フックは、`--system-prompt-file` や `--append-system-prompt-file` に切り替える必要がある
- キューに入れられたメッセージを変更し、スピナーの下ではなく会話の上に表示されるように
- プロンプト下のセッション artifact リンクを変更し、`/artifacts` を開く 1 つのフッターピル(`⧉ name` または `⧉ N`)にまとめた。`/artifacts` は今後このセッションの artifact を先頭に表示する
- Artifact ツールを変更し、Claude が artifact ページで unpkg.com からスクリプトを読み込めるように
- フルスクリーンモード(`/config` を含む)でのリスト行のホバーを変更し、フォーカスされた行の横に 2 つ目の ❯ ポインタを描く代わりに、その行を色づけするように
- /mcp を変更: 各サーバーの行はステータスアイコンと名前から始まり、状態を一度だけ示すように。狭いターミナルでは名前を短縮する前に "managed" などの末尾の情報を落とすように
- /workflows を変更: 各実行の行はステータスアイコンと経過時間から始まるように。狭いターミナルでは実行の名前と時間を保持し、エージェント数とトークン数を先に落とすように
- Remote Control の添付ファイルダウンロードを変更し、接続を再利用し、セッション内で既にダウンロード済みのファイルをスキップするように
- MCP のリソースリスト(リソースリストツールと @ メンションの候補)を変更し、MCP Apps の UI リソースをスキップするように。URI での個別読み取りは引き続き機能する
- `claude plugin uninstall --json` と /plugin ダイアログを変更し、プラグインのフォルダが他のインストール済みプラグインに使用されている、またはインストール記録が読み取れないために残る場合、そのデータが保持された旨を伝えるように
- バックグラウンドタスクリスト(`/tasks`)を変更: 実行中の `/ultrareview` で `x` を押すと、レビューの停止前に確認を求めるように
- コマンドメニューと `/help` に残っていた "(removed)" の `/agents` エントリを削除。`/agents` と入力すると引き続きウィザードの移動先を説明する
- [VSCode] auto mode が課金対象の classifier リクエストにフォールバックしたとき、VS Code と JetBrains のパネルに Continue/Stop プロンプトを追加し、応答できない警告行を置き換えるように
- [VSCode] メッセージのない Web セッションを開くと、再開できない空のローカルコピーが保存されてしまう問題を修正。エラーは今後どこで続行できるかを示す
- [VSCode] サーバーが履歴の返却に失敗した、一部の読み込みに失敗した、またはネットワークのサインインページがその代わりに応答したときに、claude.ai/code のセッションがエラーなしで空または会話の一部だけの状態で開いてしまう問題を修正。今後はエラーが表示され、再度開くことができる
- [VSCode] 拡張機能のホストが再起動した後、エディタタブ内の会話が無言でハングしてしまう問題を修正。今後はタブがセッションリストから再度開くよう案内する
- [VSCode] チャットパネルの複数選択の質問に、Claude がオプションプレビューを添付してしまう問題を修正(質問カードには表示されない)
- [VSCode] セッションマネージャーのコスト・使用量ブロックが、狭いサイドバーでテキストの途中で折り返されてしまう問題、およびアカウント切り替え後に以前のログインの合計を表示してしまう問題を修正
- [Claude Code on the web] クラウドセッションのコンポーザーのモデルメニューに Fast mode の切り替えを追加。プランに fast mode が含まれ、選択中のモデルが対応している場合に表示される
- [Claude Code on the web] GitHub セットアップのヒントに設定へのショートカットを追加し、リポジトリピッカーに "Troubleshoot GitHub connection" リンクを追加。どちらも GitHub 接続ページを開く
- [Claude Code on the web] プルリクエストが draft に変換されたことをトリガーとする GitHub トリガーの routine が一切発火しない問題を修正。今後はプルリクエストが draft に変換されると実行を開始するように
- [Claude Code on the web] GitHub にホストされていないリポジトリのクラウドセッションに、決して機能しない Create PR ボタンが表示されてしまう問題を修正。今後はそこでは非表示になる
- [Claude Code on the web] claude.ai/code の GitHub セットアップヒントが、ピッカーが開いている間リポジトリピッカーの検索ボックスと行を覆ってしまう問題を修正。今後はピッカーが閉じるまで脇に避けるように
- [Claude Code on the web] クラウドセッションがファイルを開けないときに表示されるファイルカードを改善: ファイルが存在しないのか、セッションの権限設定が読み取りをブロックしているのかを示すように
- [Claude Tag] 誰かが Stop を押した後、誰が Claude の応答を止めたかを示し、続けるには @Claude とメンションするよう伝える短い行を Slack のスレッドに追加
- [Claude Tag] スレッド内の返信に Claude が永続的に応答しなくなることがある問題を修正。影響を受けたチャンネルは今後、次の新しいメッセージで自動的に回復する
- [Claude Tag] check-in が発火した、またはバックグラウンドタスクが終了したときなど、Slack で Stop を押した後に Claude が停止したリクエストを再開してしまう問題を修正。応答中に送られたメッセージは今後読み取られる
- [Claude Tag] セットアップスクリプトの失敗などでセッションがタスクの途中でクラッシュした後、Slack の返信が何分も遅れて届く、または一切届かない問題を修正。今後は数分以内に自動的に再起動する
- [Claude Tag] セッションの設定が大きすぎて起動できないとき、Claude が Slack で自動再起動を約束した後に一般的なエラーで失敗してしまう問題を修正。今後はスレッドが理由と再試行方法を示す
- [Claude Tag] 非常に長い Slack スレッドを修正: Claude が数週間前のメッセージと照らし合わせて無言で返信を控えてしまうことがある問題、スレッドの深いところでの再起動が直近のコンテキストを失ってしまう問題
- [Claude Tag] Enterprise Grid の組織全体共有からシングルワークスペースに移動された Slack チャンネルで、Claude がすべてのメンションに "Couldn't check this channel just now" と応答してしまう問題を修正
- [Claude Tag] 主にワークスペース間で共有されたチャンネルを通じてアクセスされる非常に大規模な Enterprise Grid ワークスペースで、静かな時間の後に再び "Couldn't check this channel" となってしまう問題を修正
- [Claude Tag] 組織の inference hook にブロックされた Slack リクエストが汎用的なリトライ通知を表示してしまう問題を修正。今後はスレッドがフックの拒否メッセージを表示し、Claude はリトライしないように
- [Claude Tag] Claude が Slack で組織が使用できないモデルへの切り替えを提示してしまう問題を修正。今後は切り替えが実際に受け入れるモデルのみを一覧表示・提示するように
- [Claude Tag] Claude Tag の AWS 接続経由で送信したとき、DynamoDB と Kinesis のアカウントベースエンドポイントへのリクエストが認証に失敗する問題を修正
- [Claude Tag] Claude Tag 管理者設定の Plugins セクションが組織管理者に対して読み込みに失敗し、接続済みプラグインを生の ID として一覧表示してしまう問題を修正。今後は正しく読み込まれ、各プラグインの名前が表示される
- [Claude Tag] Slack のスレッドで尋ねられたときに Claude が示す routine のリストを変更し、デフォルトでチャンネル内のすべての routine ではなく、そのスレッド自体のスケジュールされたタスクを表示するように
- [Code Review] レビュー対象のコミットが、失敗したレビューのリトライ中に force-push で消えてしまい、プッシュごとにレビューする設定でないリポジトリでプルリクエストが一切レビューされない問題を修正
