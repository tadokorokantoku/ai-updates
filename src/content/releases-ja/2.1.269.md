---
version: "2.1.269"
date: 2026-09-11T18:12:49+00:00
---

- `claude plugin eval` を追加: プラグインの eval スイートを Claude Code に対して実行し、スコア付けされた再現可能な結果(JSON + HTML レポート)を得られるようになった。詳細は `claude plugin eval --help` を参照
- `/output-style [name]` を追加: output style の一覧表示と切り替えができるようになった。Remote Control 経由やクラウド・その他のヘッドレスセッションでも利用可能
- Bash ツールがファイル編集を扱う場合に、Bash コマンドが変更したファイルの diff を Bash ツールの結果に追加(`bashEditDiffEnabled` 設定)
- `OTEL_METRICS_INCLUDE_REPOSITORY` を追加: OpenTelemetry のメトリクスとイベントに `vcs.*` リポジトリ属性を付与できるようになった。`OTEL_LOG_TOOL_DETAILS` を設定すると commit イベントに `vcs.ref.head.*` が付与される
- `CLAUDE_CODE_GATEWAY_MODEL_DISCOVERY_TIMEOUT_MS` を追加: LLM gateway の `/v1/models` discovery タイムアウト(デフォルト3秒)を延長できるようになった
- 自分のプロンプト・1行の作業要約・レスポンスだけを表示するビューとして `/focus` を提案するスピナーのヒントを追加
- `CLAUDE_CODE_WORKFLOW_MAX_CONCURRENT_AGENTS`(1〜256)を追加: 推論律速な fan-out 向けに、Workflow ツールの1回の実行あたりの同時エージェント数上限を引き上げられるようになった
- 出力トークン上限でレスポンスが打ち切られ自動的に再開された場合、その次のターンでプロンプトキャッシュが部分的に無効化されていた問題を修正
- Claude の思考途中で中断してからセッションを再開すると、それ以前のコンテキストの再送信方法が変わり、プロンプトキャッシュの再利用が損なわれることがあった問題を修正
- kitty プロトコルのターミナルで F1/F2/F4 が動作しない問題、st で Delete が動作しない問題、rxvt-unicode で Alt+矢印キーが Escape として扱われる問題、WezTerm で Shift+記号キーが Shift なしのキーとして入力される問題を修正(2.1.247 で発生したリグレッション)
- バックグラウンドエージェントがまだ実行中であるにもかかわらず、リモートおよびヘッドレスセッションが「waiting for your input」と報告していた問題を修正(以前の動作に戻すには `CLAUDE_CODE_BG_TASKS_REPORT_RUNNING=0` を設定)
- 一部のターミナルで、機能クエリへのターミナルの応答(`^[[?1;2c`)が起動時に余分なテキストとして表示されていた問題を修正
- ターミナルのリサイズ後、フルスクリーンモードでトランスクリプトの上端または下端の行が空白になる問題を修正
- `!` で始まる deny・ask パーミッションルールが、それを書き込んだ設定ソースの範囲を超えて適用されていた問題を修正。このようなルールは今後そのソース内でのみ適用され、単独の `!` による否定は無視されるようになった
- compaction 後に Claude に伝えられる git status を修正: セッション開始時点のものではなく、現在の status になった
- リモートセッションの再開時に、同期されたプラグインの MCP サーバーが接続されない問題を修正
- ヘッドレスセッションの再開時、ターン途中でモデルが切り替えられたりリクエストがリトライされたりした場合に、そのターンの返信が失われる問題を修正
- 作業再開時に、バックグラウンドタスクのディスク上の記録に含まれるターミナルのエスケープコード・改行・過大なテキストが、タスク一覧やタスク通知にそのまま届いてしまう問題を修正
- CMYK JPEG 画像が「cannot decode」で添付に失敗していた問題を修正: 他の JPEG と同様に変換・リサイズされるようになった
- スキームなしで設定された gRPC テレメトリエンドポイントについて、managed settings の承認ダイアログが collector 名を表示していなかった問題を修正
- プラグインの `headersHelper` の同意プロンプトが、別のホストと誤読されかねない URL パスを表示していた問題を修正
- `@` で始まるフォルダ名を含む相対 Windows パスが、プラグインのエラーで `[redacted URL]` として表示されていた問題を修正
- ターミナルのネイティブカーソルが有効な場合に、パーミッションルール・auto モードルール・ディレクトリ追加・セッション名変更・フィードバックレビューの各テキスト欄でカーソルが表示されない問題を修正
- `/fork` の受信票を1秒未満の間隔で連続クリックすると、現在のツールの完了待ちの間、セッションがすぐにバックグラウンド化されない問題を修正
- `shutdown` パラメータを拒否するプラグインの LSP サーバー(rust-analyzer など)が、セッション終了時に起動したまま残る問題を修正: `shutdown` が失敗した場合でも `exit` が送信されるようになった
- attribution リマインダーが、コミットやプルリクエストの attribution を禁止する CLAUDE.md やメモリのルールを上書きしていた問題を修正。managed settings で設定された行は引き続き適用される
- 日本語・中国語・タイ語など単語間にスペースを置かない言語のテキストで、プロンプト提案が破棄されていた問題を修正
- synchronized output に対応していないバージョンの GNOME Terminal と Konsole で、ターミナル名から対応していると誤って判定されていた問題を修正
- `--output-format stream-json` の結果の `permission_denials` が、パス範囲指定の deny ルールでブロックされた Read・Edit・Write の呼び出しを含めていなかった問題を修正
- SDK や desktop app 経由で実行されたセッションが、他のセッションのエージェント一覧で unknown ステータスとして表示される問題を修正
- アカウントがデフォルトの Opus モデルに到達できない Bedrock、Vertex、Foundry、gateway のデプロイで `/insights` が失敗していた問題を修正: 代わりにセッションのモデルを使用するようになった
- 別の Claude Code プロセスが同時にログインを更新した場合に、セッションで組織のポリシー上限が読み込まれない問題を修正
- Bedrock、Vertex、gateway を使う Claude Desktop セッションで、ターン終了通知の「Claude が何を必要としているか」という文脈的なテキストが表示されない問題を修正
- 更新された設定でサーバー URL のクエリパラメータの順序だけが変わった場合に、MCP サーバーが再接続してしまう問題を修正
- 名前や説明に改行を含む、またはターミナル幅を超えるバックグラウンドエージェントを表示する際、プロンプトボックスの上枠が余分な行に分割されてしまう問題を修正
- auto-compaction が要約できる完全な過去のやり取りを持たない場合に、セッションが「Prompt is too long」で永久に止まってしまう問題を修正(主に非常に大きなプロンプトを持つ Agent SDK セッションで発生)
- API エラー・ネットワーク切断・トークン上限の後に `/goal` の実行が黙って停止してしまう問題を修正: バックオフを伴うリトライを行うか、一時停止してその理由(usage limit のリセット待ちを含む)を伝えるようになった
- クラウドセッションでのプロンプトキャッシュミスを修正: 最初のリクエストの前にサーバー設定を少し待つようになった
- `/btw` の回答に架空のツール呼び出しと出力が含まれることがあった問題を修正: side question にそれらを書かないよう指示し、それでも現れた場合は実行されていないものとしてフラグが付くようになった
- `CLAUDE_CODE_RESUME_INTERRUPTED_TURN` が、6時間以上前(`CLAUDE_CODE_RESUME_INTERRUPTED_TURN_MAX_AGE_MS` 設定時はそれより古い場合)に API エラーで失敗したターンを再実行してしまう問題を修正
- managed settings 経由で有効化された組織プラグインが、ヘッドレスセッションおよび Claude Desktop(Desktop がこの CLI バージョンを同梱後)で読み込まれない問題を修正。次回のセッションから読み込まれるようになる
- セッション用に展開されたプラグインアーカイブが他のローカルユーザーから読み取り可能だった問題、展開されたファイルがアーカイブ由来の world-writable ビットを保持していた問題、再展開後も古いファイルが残っていた問題を修正
- `Edit()` の deny ルールと書き込みパスチェックが、Bash の `tee` コマンドが書き込むファイルに適用されていなかった問題を修正。`Bash(tee:*)` の allow ルールは作業ディレクトリ外の書き込み先をカバーしなくなった
- 低速な接続(ssh、ブラウザターミナル)経由での起動時に、`22c` のような余分な文字やターミナルのカラー・バージョン応答がプロンプトに入力されてしまう問題を修正
- rxvt-unicode でフルスクリーンを終了・再突入した後、ターミナルのブロックカーソルがインターフェースの下に表示される問題を修正
- rxvt-unicode のフルスクリーンモードで外部エディタから戻った後、カーソルブロックが表示されたままになる問題を修正
- フルスクリーンモード以外で外部エディタ(Ctrl+G)から戻った後、インターフェースが二重に描画される問題を修正
- Konsole で外部エディタから戻った後、インターフェースが二重に描画される問題を修正
- Windows: バックグラウンドに送られた PowerShell ツールコマンドが、Claude Code の終了時に停止してしまう問題を修正
- `/diff` パネルを改善: 最初にローディング状態を表示する代わりに、一度に完全にレンダリングされた状態で開くようになった
- 日本語・中国語・韓国語のテキストに対するプロンプト提案のフィルタリングを改善: 複数の文字体系が混在する提案や単語単体の提案は保持され、英語と同様にメタ的・評価的なテキストは除外されるようになった
- Skill ツールの「Unknown skill」エラーを改善: 素の名前がちょうど1つのプラグインスキルに一致する場合、そのプラグインスキルのフルネームを表示するようになった
- SSH 経由および未認識のターミナルでのキーボードサポートを改善: kitty キーボードクエリに応答するターミナル(foot、Alacritty 0.16+ など)で Shift+Enter と Ctrl+Shift のショートカットが使えるようになった
- 長時間セッションでの応答性を改善: トランスクリプトの更新時に、折りたたまれたツール使用の要約を構築するために会話全体を再処理しなくなった
- テレメトリを無効化したファーストパーティセッションを改善: 会話の途中で接続が完了した `alwaysLoad` MCP サーバーが、tool-search の往復なしで次のターンから利用できるようになった
- `/ultrareview --post` を変更: finding が届いた時点で PR コメントを直接投稿してそのリンクを表示するようになり、投稿のために2つ目のクラウドセッションを起動しなくなった
- セッションのスクラッチパッドに保存するアーティファクトのデータベース読み取りを変更: working-folder の承認待ちで止まらなくなった
- クラウドセッションで claude.ai から同期されたスキルの名前を変更: Claude Desktop に合わせて `anthropic-skills:<name>` という名前になった。他に使用しているものがなければ素の名前も引き続き使用可能
- [VSCode] agent map を追加: フッターの「N agents」ピルから、セッションのサブエージェントをエージェントごとのカード・Stop agent・読み取り専用トランスクリプトとともにマップ表示できるようになった
- [VSCode] コマンドメニューに Hooks ダイアログを追加: hooks の閲覧と、user・project・local 設定での追加・編集・削除ができるようになった。managed・プラグイン・セッションの hooks は引き続き読み取り専用
- [VSCode] Focus view のツール呼び出しグループの下に、実行中のサブエージェントのライブ進捗行を追加
- [VSCode] パーミッションルールを一覧表示し、user・project・local 設定での追加・削除ができる Permission rules ダイアログを追加。起動オプション・セッション限定・managed のルールは引き続き読み取り専用
- [VSCode] Switch account 画面に Cancel ボタンを追加: 現在のアカウントのままセッションに戻れるようになった
- [VSCode] Focus view で、スケジュールされたタスクなどの配信されたプレーンテキストプロンプトによって開始されたターンが、直前のターンの一部として表示されていた問題を修正
- [VSCode] パネルが狭い場合に、フッターのプロンプトキャッシュ時計が分表示を隠していた問題を修正
- [VSCode] 設定ファイルまたは `environmentVariables` 設定で `CLAUDE_CONFIG_DIR` が指定されている場合に、セッション一覧がデフォルトフォルダのセッションを保持し続けていた問題を修正
- [VSCode] 読み込みが遅れて完了した plan preview が、コメント欄を隠したり古い plan を表示したりすることがあった問題を修正
- [VSCode] Claude タブが閉じられた後も plan preview がコメントを受け付けてしまい、それがどこにも反映されない問題を修正
- [VSCode] 最後の返信の後に compact され、その後閉じられたセッションのプロンプトキャッシュ時計と再オープン通知を修正: 再度開いた際に cold と表示されるようになった
- [VSCode] Remote Control が有効な状態で拡張機能内でセッション名を変更しても、claude.ai/code 上では古い名前のままだった問題を修正
- [VSCode] ターミナルから設定がデフォルトにリセットされた後も、「Enable Remote Control for all sessions」トグルが直前の位置のままだった問題を修正
- [VSCode] ウィンドウリロード後に復元された Claude タブが、クリックされるまでセッション一覧で開いているとみなされない問題と、その行をクリックすると2つ目のタブが開いてしまう問題を修正
- [VSCode] 同じアカウントで再度サインインした際、Switch account によってタブが閉じたはずの usage-limit 警告を忘れてしまう問題を修正
- [VSCode] 長いターンの最中にセッション名を変更した場合、ウィンドウリロード後にその名前が生成された名前で上書きされてしまう問題を修正
- [VSCode] アカウントがモデル別の週次上限を失った後も、サイドバーの usage meter にその古い行が残ってしまう問題を修正
- [VSCode] 新しい chat view の起動中にキーボードショートカットで送信された @-mention が、キー入力からかなり時間が経ってから入力欄に挿入されてしまう稀なケースを修正
- [VSCode] Claude サイドバーを開いた直後に Account & usage ヘッダーが少し遅れて表示されると、セッション一覧が下にジャンプしてしまう問題を修正
- [VSCode] ユーザー以外の相手に向けて書かれるドキュメントやメッセージを改善: Claude がその読み手向けに文章を書き、返信の冒頭でその読み手を明示するようになった
- [VSCode] スラッシュコマンドメニュー、@-mention メニュー、output-style ピッカー、Send/Stop ボタン、パーミッションおよび質問カード、オンボーディングチェックリストでのスクリーンリーダーおよびキーボードのアクセシビリティを改善
- [VSCode] メッセージ入力欄の current-file チップを変更: Hide トグルの代わりに X で削除できるようになった
- [VSCode] セッションタブの右クリックメニューとエディタタイトルバーの「...」メニューから Claude Code の項目を削除: そのメニューを開いたタブに対しては操作できていなかったため
- [Claude Code on the web] クラウドセッションで、Claude が読む前にキューに入れたメッセージを取り戻せる機能を追加: キューから削除するか、Esc または上矢印キーを押すとテキストがメッセージ欄に戻る
- [Claude Code on the web] Claude Code が使用できるモデルを制限している組織で、クラウドセッションでの `/model default` により以降のすべてのメッセージが失敗し続けていた問題を修正
- [Claude Code on the web] 一時的なサーバーエラーの後、一度限りのスケジュール済みルーティンが2回実行されてしまうことがあった問題を修正
- [Claude Code on the web] サブエージェントを使用するルーティンの実行が、早く終了したものとして扱われることがあった問題を修正: 実際の失敗後のリトライがスキップされたり、重複した実行が開始されたりすることがあった
- [Claude Code on the web] Claude がリポジトリのサブフォルダで作業していた場合に、クラウドセッションのトランスクリプト内のファイルリンクが GitHub の404を開いてしまう問題を修正
- [Claude Code on the web] Cloud environments の管理ページを変更: 到達できないことがある Show more コントロールの背後で各テーブルを5行に制限する代わりに、すべての環境を一覧表示するようになった
- [Claude Code on the web] Free プランのユーザー向けの claude.ai/code を変更: 先に進む手段のない「Disabled by org admin」ページの代わりに、アップグレードへの導線がある plans ページを開くようになった
- [Claude Tag] admin settings で GitHub インストールに対する Connect all または Disconnect の前に確認ダイアログを追加: 組織全体に及ぶ意図しない変更を防ぐため
- [Claude Tag] Slack が一時的にレート制限をかけたことで失敗通知が失われ、失敗したターンの後にスレッドが静かになってしまうことがあった問題を修正: 通知はリトライされるようになった
- [Claude Tag] 組織で有効化されていないモデルへの切り替えを Claude が受け入れ、代わりのモデルで黙って回答していた問題を修正: 今後は切り替えを拒否し、管理者が有効化できることを伝えるようになった
- [Claude Tag] Claude が同じメッセージにファイルを添付すると、テーブルが生のパイプ区切りテキストとして投稿されていた問題を修正: テーブルは通常の返信として投稿され、ファイルはその後にシンプルなキャプション付きで続くようになった
- [Claude Tag] Claude が有効になっていないチャンネルのトップレベルで `@Claude !restart` を実行すると、無関係な会話が始まってしまう問題を修正: 今後は再起動するものがないことを本人にのみ伝えるようになった
- [Claude Tag] Slack access settings のプラグイン行が、無効化する手段のないラベルなしの生 ID を表示していた問題を修正: 名前と、それを管理する bundle へのリンクを表示するようになった
- [Claude Tag] Slack から開始されたセッションの共有セッションバナーと Share ダイアログが、組織全体がリンクを開けると表示していた問題を修正: 今後は Slack チャンネルの対象者を明示するようになった
- [Claude Tag] admin settings ページとその Slack チャンネルピッカーの読み込み時間を改善: チャンネル数が多い組織や複数のワークスペースを接続している組織で特に顕著
- [Claude Tag] Slack チャンネルでのスケジュール済みルーティンを改善: 常に新しいトップレベルのチャンネルメッセージを投稿する代わりに、既存のスレッドに返信できるようになった
- [Claude Tag] Claude のライブ進捗チェックリストのタイムスタンプを改善: 固定の UTC 時刻の代わりに、各読み手のローカル時刻と最終更新からの経過時間を表示するようになった
