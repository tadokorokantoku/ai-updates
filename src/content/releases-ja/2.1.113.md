---
version: "2.1.113"
date: 2026-04-17T19:34:36+00:00
---

- CLI がバンドルされた JavaScript の代わりにネイティブ Claude Code バイナリを起動するように変更 (プラットフォーム別のオプショナル依存経由)
- より広範な `allowedDomains` ワイルドカードが許可する場合でも特定のドメインをブロックする `sandbox.network.deniedDomains` 設定を追加
- フルスクリーンモード: 可視範囲を超えて選択を拡張する際、Shift+↑/↓ でビューポートがスクロールするように
- `Ctrl+A` と `Ctrl+E` がマルチライン入力で現在の論理行の先頭/末尾に移動するように (readline 動作に一致)
- Windows: `Ctrl+Backspace` が前の単語を削除するように
- レスポンスと bash 出力内の長い URL が行をまたいで折り返されてもクリック可能に (OSC 8 ハイパーリンク対応ターミナルで)
- `/loop` を改善: Esc で保留中のウェイクアップをキャンセル可能に、ウェイクアップが明確化のため "Claude resuming /loop wakeup" と表示
- `/extra-usage` が Remote Control (モバイル/ウェブ) クライアントから動作するように
- Remote Control クライアントから `@` ファイル自動補完候補をクエリ可能に
- `/ultrareview` を改善: 並列チェックによる起動の高速化、起動ダイアログでの diffstat 表示、アニメーション付き起動状態
- ストリーム途中で停止したサブエージェントが無音でハングせず、10 分後に明確なエラーで失敗するように
- Bash tool: 最初の行がコメントの複数行コマンドがトランスクリプトに完全なコマンドを表示するように、UI なりすましベクターを解消
- `cd` が no-op の場合、`cd <current-directory> && git …` を実行しても権限プロンプトが発生しないように
- Security: macOS で `/private/{etc,var,tmp,home}` パスが `Bash(rm:*)` 許可ルール下で危険な削除対象として扱われるように
- Security: Bash deny ルールが `env`/`sudo`/`watch`/`ionice`/`setsid` などの exec ラッパーでラップされたコマンドにもマッチするように
- Security: `Bash(find:*)` 許可ルールが `find -exec`/`-delete` を自動承認しないように
- MCP 同時呼び出しのタイムアウト処理で、あるツール呼び出しのメッセージが別の呼び出しのウォッチドッグを無音で無効化する問題を修正
- Cmd-backspace / `Ctrl+U` が再びカーソルから行頭まで削除するように修正
- セル内にパイプ文字を含むインラインコードスパンがある場合に Markdown テーブルが壊れる問題を修正
- プロンプトで未送信テキストを作成中にセッション recap が自動発火する問題を修正
- `/copy` "Full response" が GitHub、Notion、Slack に貼り付ける際に Markdown テーブルの列を揃えない問題を修正
- 実行中のサブエージェント表示時に入力されたメッセージがそのトランスクリプトから非表示になり親 AI に誤帰属される問題を修正
- Bash `dangerouslyDisableSandbox` が権限プロンプトなしにサンドボックス外でコマンドを実行する問題を修正
- `/effort auto` の確認 — ステータスバーラベルに合わせて "Effort level set to max" と表示するように修正
- "copied N chars" トーストが絵文字や他の複数コードユニット文字を過大にカウントする問題を修正
- Windows で `/insights` が `EBUSY` でクラッシュする問題を修正
- 終了確認ダイアログがワンショットのスケジュールタスクを繰り返しとして誤ラベルする問題を修正 — カウントダウン表示に
- フルスクリーンモードでスラッシュ/@ 補完メニューがプロンプト境界に密着しない問題を修正
- `CLAUDE_CODE_EXTRA_BODY` `output_config.effort` が、effort をサポートしないモデルや Vertex AI のサブエージェント呼び出しで 400 エラーを引き起こす問題を修正
- `NO_COLOR` 設定時にプロンプトカーソルが消える問題を修正
- 貼り付けた MCP ツール名が description にマッチする兄弟ではなく実際のツールを浮かび上がらせるよう `ToolSearch` のランキングを修正
- 再開したロングコンテキストセッションのコンパクトが "Extra usage is required for long context requests" で失敗する問題を修正
- インストール済みプラグインと依存バージョンが衝突しても `plugin install` が成功する問題を修正 — `range-conflict` を報告するように
- "Refine with Ultraplan" がリモートセッション URL をトランスクリプトに表示しない問題を修正
- 処理に失敗した SDK 画像コンテンツブロックがセッションをクラッシュさせる問題を修正 — テキストプレースホルダーにフォールバック
- Remote Control セッションがサブエージェントトランスクリプトをストリーミングしない問題を修正
- Claude Code 終了時に Remote Control セッションがアーカイブされない問題を修正
- Bedrock Application Inference Profile ARN 経由で Opus 4.7 を使用する際に `thinking.type.enabled is not supported` 400 エラーが発生する問題を修正
