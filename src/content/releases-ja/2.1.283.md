---
version: "2.1.283"
date: 2026-09-25T18:46:11+00:00
---

- LLM ゲートウェイが 1 つのユーザープロンプトを処理するリクエストをグループ化できるよう、ゲートウェイヒントヘッダーに `x-claude-code-prompt-id` を追加。`CLAUDE_CODE_GATEWAY_HINT_HEADERS=1` で有効化
- 管理設定 `availableModelsMatch` を追加: `"exact"` を指定すると、`availableModels` のエントリはそこに記載されたモデルバージョンのみを許可するようになり、新しいリリースは一覧に追加されるまでブロックされ続ける
- 特定のモデルをブロックする管理設定 `deniedModels` を追加。`availableModels` で許可されていてもブロックされる
- `OTEL_LOG_TOOL_CONTENT=1` の場合、`tool.output` OpenTelemetry span イベントに MCP ツール、WebFetch、WebSearch の出力を追加
- CLAUDE.md ファイル、スキル、エージェント、コマンドを古いモデル向けに書かれたプロンプトパターンについて監査する `/doctor prompt-audit`（`/checkup prompt-audit` としても利用可能）を追加
- フルスクリーンモードで他のセッションからの省略されたメッセージをクリックして展開できる機能を追加
- stream-json の `system/init` の `plugin_errors` の `--plugin-dir` 読み込み失敗エントリに `path` を追加し、読み込めなかったディレクトリ名を表示
- Claude apps gateway 設定にオプトインの `load_test_mode` ブロックを追加: リクエストは構築・署名されるが上流には送信されず、クライアントには定型応答が返されるため、デプロイメントの負荷テストが行える
- Claude apps gateway に Amazon Bedrock の Mantle エンドポイント向けの upstream プロバイダ `mantle` を追加
- SDK セッションで、ターンが早期終了した際に遅延ツール呼び出しや完了済みツール結果が失われる問題、ワーカー再起動後に保留中の承認プロンプトが失われる問題、非ストリーミングフォールバックの `result.usage` が失われる問題を修正
- 長時間実行中のツール呼び出しがバックグラウンドに移動すると MCP の進捗通知が破棄されていた問題を修正。バックグラウンドタスクが最新の進捗を表示するようになった
- セッションが起動中の stdio MCP サーバーを、起動途中のまま終了させてしまっていた問題を修正
- ステートレスなリモート MCP サーバー（再デプロイ中のプロキシなど）からの一時的な HTTP 404 により、そのサーバーが接続済みと表示されたままセッションの残りで使用不能になっていた問題を修正
- 有効な URL のない MCP サーバーへのサインインが不明瞭な SDK エラーで失敗していた問題を修正。`/mcp` はそのようなサーバーに Authenticate を提示しなくなった
- テレメトリが無効な場合に、週次の Fable 上限が `/usage` と VS Code の使用状況メーターに表示されていなかった問題を修正
- id に日付や `-v1:0` サフィックスが付いている場合に、プレーンな id が拒否されるケースで `/model` が Sonnet 4.6 や Sonnet 5 に `[1m]` を付けて受け入れてしまっていた問題を修正
- `ANTHROPIC_DEFAULT_HAIKU_MODEL` で別のモデルが固定されている場合に、`/model` ピッカーがハードコードされた Haiku のバージョンと価格を表示していた問題を修正
- モデルフォールバック中に開始した dynamic workflows が、設定されたモデルへの再試行ではなく全エージェントをフォールバックモデルで実行していた問題を修正
- Haiku がセッションのメインモデルである場合に `DISABLE_PROMPT_CACHING_HAIKU` が効かなかった問題を修正
- `claude plugin validate` が、Claude Code がインストールできないプラグイン名やマーケットプレイス名を受け入れ可能と表示していた問題を修正。`marketplace.json` 内のそのような名前は検証に失敗するようになった
- `outputStyles`、`themes`、`monitors`、`lspServers` のパスが存在しない、またはプラグインディレクトリ外を指しているプラグインを `claude plugin validate` が通過させていた問題を修正
- `plugin.json` で MCP サーバーを宣言しているプラグインについて `claude plugin details` が MCP サーバー数を 0 と表示していた問題を修正
- `claude plugin marketplace remove` が、そのマーケットプレイスと共にアンインストールしたプラグインを表示していなかった問題を修正。今は一覧表示される
- 大文字小文字のみが異なる id を持つインストール済みの 2 つのプラグインのうち、指定していない方が、そのスコープに `enabledPlugins` のエントリがない場合にオプションとシークレットごと `claude plugin uninstall` で削除されてしまっていた問題を修正
- バージョンを宣言しないプラグインで、キャッシュされたファイルが見つからない場合に、インストール済みのバージョンではなく取得元の最新コミットへ無言で復元されてしまっていた問題を修正
- ホームディレクトリや設定ディレクトリが移動された後（バインドマウントされた devcontainer など）、ユーザーがインストールしたプラグインとマーケットプレイスが「cache-miss」で読み込みに失敗していた問題を修正
- `installed_plugins.json` が無効なプラグイン id のもとにレコードを保持している場合にプラグインが一切表示されなかった問題を修正。そのようなファイルも再度読み込まれるようになった
- このバージョンが読み込めないレコードを `installed_plugins.json` が保持している場合に、書き換えられてレコードが失われていた問題を修正。`claude plugin` コマンドが該当レコード名と復旧方法を表示するようになった
- スクリーンリーダーモードの権限ダイアログが、引用符付きのコマンドやパスをダイアログ自体のテキストであるかのように読み上げていた問題を修正
- `/context` が MCP サーバーの instructions をカウントしていなかった問題を修正。今は独自の行として表示され、合計にも加算される
- Warp ターミナルで Markdown のリンクがクリック可能なハイパーリンクではなくプレーンテキストとして表示されていた問題を修正
- サンドボックス内などでユーザーまたはローカルの設定ファイルを書き込めなかった場合に、`claude mcp add`、`add-json`、`remove` が成功と報告していた問題を修正
- クラウドセッションでの返信の最初の単語が、Claude が書き進めるにつれてストリーミングされず、遅れて表示されることがあった問題を修正
- Claude 組み込みのキーバインドガイドで、コードの組み合わせのタイムアウトが 3 秒ではなく 1 秒と表示されていた問題、`cmd` を `meta` のエイリアスと呼んでいたためにほとんどのターミナルが送信しない `cmd+` ショートカットが生成されうる問題を修正
- `keybindings.json` が `ctl+k` のようなスペルミスの修飾キーを無言で受け入れていた問題を修正。デバッグログに警告が出て修正方法が提案されるようになった
- `keybindings.json` で `footer:openSelected` をリバインドまたは解除した後も、フッターのヒントが「Enter to view」のままだった問題を修正
- 連続して素早く入力されたキー（先行入力、キーリピート、ssh や tmux 経由でのバースト入力）が古い状態に対して処理されることがあった問題を修正
- CA 証明書が `GIT_CONFIG_COUNT` 環境変数のペアとして git に渡された場合に、worktree のチェックアウト（Git LFS のダウンロードなど）が証明書検証に失敗していた問題を修正
- サンドボックス化された `git` が、サンドボックスプロキシのログイン情報を保存するよう credential helper に要求し、「failed to store」と表示されていた問題を修正
- ネストした値の一つが無効な場合に、管理対象の `sandbox` 設定全体が無視されていた問題を修正。無効な値は今はフェイルクローズし、残りの設定は引き続き適用される
- Claude Code が git リポジトリのサブディレクトリで起動された場合に、Claude 自身の auto-memory ノートへの編集が機密ファイルへの書き込みとしてブロックされていた問題を修正
- `DISABLE_TELEMETRY` または `DO_NOT_TRACK` でテレメトリがオフの場合に、有料プランで Remote Control が利用できなかった問題を修正
- 狭いターミナルで `/remote-control` メニューの QR コードのヒントが単語の途中で切れていた問題を修正
- vim モードの `.` が Shift+Enter の改行を欠落させていた問題、カーソルがアクセント付き文字の中に残ってしまっていた問題、最終行での `3J` やビジュアルモードの `J` の後に古い変更が繰り返されていた問題を修正
- vim モードのカーソル位置を修正: ノーマルモードで 10,000 文字を超えるプロンプトを履歴から呼び出してもカーソルが末尾を超えて残らなくなり、`V` の後の `p` が先頭の非空白文字に移動するようになった
- vim モードの `J` が Vim と異なる間隔（`)` の前やタブの後にスペースが入るなど）で行を結合していた問題、および最終行での `3J` やビジュアルモードの `J` が Vim のようにカーソルを移動させなかった問題を修正
- Windows: PowerShell ツールが `cmd /c rd`、`rmdir`、`del`、`erase` によって `Remove-Item` が拒否するドライブのルートやホームフォルダなどを削除できてしまっていた問題を修正
- `/mcp` のツール一覧を改善: 一度に表示されるツールが増え、ページキーとマウスでスクロールでき、組織がブロックしたツールに警告アイコンが表示されるようになった
- MCP ツールの結果を改善: MCP ツールが返す画像がファイルにも保存されるようになり、Bash、Read などの他のツールから開けるようになった
- `/tasks` を改善: 各行にステータスアイコン、名前、全情報が表示され、タスクが多い場合もタイトルとキーのヒントが画面に残るようになり、一覧にページングキー、マウスホイール、クリックが対応した
- `/help`、`/hooks`、`/copy`、`/chrome`、`/memory`、`/ide`、`/release-notes`、`/rewind`、`/diff`、`/remote-env`、`/plugin` などのピッカーの一覧をページキー、マウスホイール、クリックに対応させて改善
- `/skills` や `/artifacts` など検索ボックスを伴う一覧で、検索ボックスがキー入力を受け取っている間はポインタを薄く表示し、ハイライトされるポインタが一つだけになるよう改善
- compaction のスピナーを改善: タイマーが compaction の開始時から始まり、パーセンテージバーの代わりに要約のトークン数をストリーミング中にカウントするようになった
- MCP サーバーへのサインイン後に表示されるブラウザページを改善: 中央揃えのレイアウト、ダークモード、新しいアートワークに刷新
- 読み込みに失敗したプラグインに属するスキルが呼び出された際の Skill ツールの応答を改善し、スキルがアンインストールされたと伝えるのではなく、プラグインが読み込めなかったと Claude に伝えるようになった
- Claude Code の設定に対する `prompt-audit` を改善: 古くなったパス、古くなったコマンド、矛盾する指示ファイルがレポートの先頭に表示されるようになり、Claude Code がドキュメント化している thinking キーワードは維持されるようになった
- 全く読み込めない `installed_plugins.json` からの復旧を改善: 再構築される前にその内容が隣接するファイルに保存され、`claude plugin list` がそのファイル名を表示するようになった
- artifact データベースの読み込みを改善: 完全な 1 ページを返す order 付きクエリが、それが 1 ページ分であることと残りの読み込み方法を表示するようになった
- 最初の返信のレイテンシを改善: セッションの最初の返信の最後に実行されていたパターンコンパイル処理が、返信のストリーミング中に実行されるようになった
- 事前接続済みの API 接続を再利用することで、最初のリクエストのレイテンシを改善
- 起動を改善: `claude -p` と Claude Code Remote はインタラクティブ UI を読み込まなくなり、auto-mode の分類器のルールと Artifact ツールは起動時ではなく初回使用時に読み込まれるようになった
- Artifact ツールの機能がまだ判明していない claude.ai アカウント（初回実行時など）の起動を改善: プロンプトが確認のために最大 1.5 秒待つことがなくなり、必要な場合は最初のメッセージ側で待機するようになった
- サードパーティプロバイダやテレメトリがオフのインタラクティブセッションで、権限モードが未設定の場合は auto mode で開始するように変更。`permissions.defaultMode` は引き続き優先される
- `/ultrareview` の起動ダイアログを変更し、ローカルブランチのレビューでは追跡対象ファイルの未コミットの変更もアップロードされうる旨を表示するようにした
- `/model` ピッカーの Opus 行とデフォルトモデル名から、Opus がすでに 100 万トークンコンテキストウィンドウを持つ場合の「(1M context)」表記を削除。ウィンドウ自体は変更なし
- ターミナルのプロンプト提案を変更し、20 回連続で使われないと表示頻度が下がるようにした。1 つ使うと再び表示されるようになる
- `--system-prompt` と `--append-system-prompt` が、テキストと `-file` 形式の両方を同時に受け付けるように変更。ファイルのテキストが先に来る
- `Skill(anthropic-skills:<name>)` の deny ルールが、Claude Desktop がプラグインとして配信する同名スキルもブロックするように変更。`Skill(skill:<name>)` の deny がスキルのエイリアスと表示名にもマッチするように変更
- `/rewind` と `/diff` の一覧が、他のすべての一覧と同じキーバインドアクション（`select:*`）で移動するように変更。`messageSelector:*`/`diff:*` のリバインドも引き続き機能する
- `/workflows` の実行一覧を他の一覧と同様のサイズにするように変更: インラインでターミナルの半分の高さになり、プロンプトが下に表示される場合もタイトルが画面に残るようになった
- git がインストールされている場合、`claude plugin eval` が git 2.31 以降を要求するように変更。古い git での実行はバージョン名を示すメッセージとともに拒否される
- artifact の監視を変更: 自動的に有効化された（自分で要求していない）監視は、3.5 時間操作がないと終了するようになった。公開または再度監視することで再度有効化される
- セルフホストランナー: lifecycle hooks の git を変更し、リポジトリの Git LFS `pre-push` フックをスキップし、書き込み可能なシステムの `core.hooksPath` を無視し、`--configure-git` なしではコミットに署名しないようにした
- セルフホストランナー: Anthropic 管理の git 配下での `GIT_SSL_CAINFO` と `GIT_SSL_NO_VERIFY` を変更: ランナー自身の git は常に Anthropic の git ルートを検証するようになり、警告行がどこに何が適用されるかを示すようになった
- 2.1.282 での `claude-ai` という名前の予約を取り消し: その名前のスキル、コマンド、ワークフロー、MCP サーバーのスキルとプロンプトが再び読み込まれるようになり、`Skill(claude-ai:*)` ルールは通常の前方一致ルールに戻った
- [VSCode] 自動での権限モード切り替えが失敗した後もセッションが auto または bypass モードで動き続けているのに、権限モードインジケータが Default と表示されていた問題を修正。切り替えが成功するまで再試行されるようになった
- [VSCode] Web からテレポートされたセッションで、Claude が作業中に送信されたメッセージが失われていた問題を修正
- [VSCode] 古いバージョンが空のローカルコピーを保存していた場合に、Web セッションがセッション一覧から隠され、代わりに空のチャットが開いてしまっていた問題を修正
- [VSCode] 再度開いたセッションで、自動継続などターン途中に Claude が受け取ったメッセージの位置でターンが分割されてしまっていた問題を修正
- [VSCode] 再読み込みしたセッションで、巻き戻し済みのターンや compaction 以前の行だけが表示されていた問題を修正
- [VSCode] 狭いパネルでフッターの agents ピルのアイコンが中央からずれ、ステータスドットが端に寄って表示されていた問題を修正
- [VSCode] 改行で終わる行を長いプロンプトに貼り付けた後、チャット入力のテキストがカーソルと選択範囲より少し下に表示されていた問題を修正
- [クラウドセッション] 実行中のクラウドセッションへのリポジトリ追加を改善: GitHub アカウントが読み取りはできるが push はできないプライベートリポジトリも、読み取り用としてアタッチできるようになった
- [クラウドセッション] サーバー側の再起動から復旧した後、クラウドセッションが重複コメントの投稿や push など、すでに完了しているステップを再実行してしまうことがあった問題を修正
- [クラウドセッション] 新しいルーチンのスケジュールがデフォルトで時報の数分後に設定されるように変更。ちょうど時報に設定されたルーチンは数分遅れて開始する場合がある旨の注記も追加
- [Claude Tag] Claude の Slack 検索を、追加済みの公開チャンネルに限定する管理設定「Channels Claude can search」を追加。組織、ワークスペース、チャンネル単位で設定可能
- [Claude Tag] Claude アカウント接続後に表示されるページに Back to Slack ボタンを追加し、開始したスレッドに戻れるようにした
- [Claude Tag] チャンネルがアタッチルール経由でアクセスバンドルを取得している場合に、そのチャンネルの設定ページにコネクタやプラグインが一切表示されていなかった問題を修正。ルールでアタッチされたバンドルも表示されるようになった
- [Claude Tag] 選択されたリポジトリの大きな一覧に限定された GitHub App インストールで、アクセスバンドルのリポジトリ検索がリポジトリを見つけられなかった問題を修正
- [Claude Tag] 新しいメッセージが返信の途中に割り込むと、Claude が同じ返信を 2 回投稿することがあった問題を修正
- [Claude Tag] Slack Connect の共有後などで Slack チャンネル ID が変更された古いプライベートチャンネルで、チャンネルルーチンが動作しなくなっていた問題を修正
- [Claude Tag] 「Channel only」に設定されたチャンネルで、ゲスト以外のメンバーが参加し Slack のメンバーシップ確認が遅れると、会話がすべて終了してしまっていた問題を修正
- [Code Review] GitHub がプルリクエストを返せなかった際に「@claude review」のリクエストが応答しなくなっていた問題を修正。リクエストは一度再試行され、それでも失敗した場合はコメントで説明されるようになった
- [Code Review] 何も検証されないまま時間制限で停止したレビューの課金を修正。未完了として表示され、課金されず、一度再試行されるようになった
