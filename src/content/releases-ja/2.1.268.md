---
version: "2.1.268"
date: 2026-09-10T18:41:11+00:00
---

- Claude apps gateway に追加: `gateway.yaml` で `pricing:` を設定すると、サインイン済みの Claude Code クライアントが managed settings 経由で同じレートを受け取るようになり、`/cost` とテレメトリが spend meter と一致する
- `access_control.allow_cidrs` が空の場合にゲートウェイの起動時警告を追加し、パブリックアドレスからリクエストが初めて届いた際の一度限りの警告を追加
- `gatewayInternalNetworks` managed setting を追加: 管理者が組織自身のパブリック IPv4 ブロックから Claude apps gateway への `/login` を許可できるようになった
- `claude self-hosted-runner --remove-session-state`(デフォルト無効)を追加: セッション終了時に `<base-dir>/_sessions/` 配下の各セッション専用ディレクトリを削除する
- `claude auth status --json` の出力に `configDirectory` を追加
- `claude plugin install`、`uninstall`、`update`、`enable`、`disable` に `--json` を追加し、`claude plugin list --json` の各行に `errorDetails`/`noteDetails` を追加
- 公開したアーティファクトにブラウザタブアイコンを追加。Claude が各ページに合わせて選択する
- `ANTHROPIC_BASE_URL` を使うサードパーティの Anthropic 互換エンドポイントで、2.1.265 以降すべてのターンが HTTP 400 で失敗していた問題を修正: Artifact ツールの入力スキーマ内の正規表現をそれらのエンドポイントが拒否していた
- レスポンスを開いたまま完了させないサーバーに対して WebFetch が無限にハングしていた問題を修正。300秒後にフェッチが失敗するようになった。`CLAUDE_CODE_WEBFETCH_DEADLINE_MS` でデッドラインを上書き可能(0 で無効化)
- 信頼していないフォルダ内の同名エージェントファイルから、再起動されたインプロセスのチームメイトがツールやシステムプロンプトを引き継いでしまう問題を修正
- 持続的な高 CPU 使用率を修正: 長時間アイドルなセッションでのビジーループが CPU コアを占有しなくなり、セッション要約中の高速なターミナルフォーカス通知が CPU を高止まりさせなくなった
- MCP ツール呼び出し後に Claude が「your message came through empty」と返信することがあった問題を修正
- シンボリックリンクされたディレクトリ(macOS の `/etc`、`/tmp`、`/var`、Linux の `/bin`)に対する deny・ask パーミッションルールが、実体のパスで指定された場合に適用されない問題を修正。シンボリックリンクパスの表記で書かれた deny ルールを Bash コマンドが無視する問題も修正
- パーミッションチェッカーが解析できない `env -C`、`eval` などのコマンドが同じ行にある場合、Read や Edit の deny ルールが適用されないことがある問題を修正
- プラグインやマーケットプレイスのエラーが git ソース URL からトークンやパスワードを表示していた問題を修正
- `/mcp`・`/plugin` のサーバー詳細、`claude mcp list`/`get`、MCP ログインエラーが、MCP 設定内の `${VAR}` プレースホルダーから解決されたシークレットを表示していた問題を修正
- `excludeDynamicSections` を使う SDK セッションでプロンプトキャッシュと extended thinking がセッション途中で壊れていた問題を修正: 最初のメッセージがリクエストごとに再レンダリングされなくなった
- キャッシュされたモデルアクセス拒否が古くなっていた場合、再起動後や Desktop の Code タブで、利用権限があるユーザーにモデルが制限されていると表示されていた問題を修正
- 別の Claude Code プロセスが古いモデルアクセスエントリを更新した際、実行中のセッションが黙って組織のデフォルトモデルに切り替わってしまう問題を修正
- Fable モデルでの長コンテキスト 429 が、Pro・Team プランでの 1M コンテキストメッセージの代わりに usage-credits の同意プロンプトを表示していた問題を修正
- プロファイル経由のワークロードアイデンティティフェデレーション(claude-code-action が設定する構成)で、同じプロファイルを共有するプロセスが実行中に `401 … jti reused` で失敗することがあった問題を修正
- ローカルコールバックのポート範囲がバインドできない場合に、MCP サーバーの OAuth サインインが「No available ports for OAuth redirect」で失敗していた問題を修正
- `$` を含むテキストを `/compact` や auto-compact が生成する会話要約が損なっていた問題を修正
- `/compact` で終了した会話を再開する際、復元されたファイルのメモが再開ごとに順序が変わっていた問題を修正。今後は毎回同じ順序で読み込まれる
- SDK のプロンプト提案、サイドクエスチョン、`/rename` が compaction 前の会話を送信していた問題を修正
- 上矢印キーで以前のプロンプトを呼び出して編集した後、`@` ファイルや `/` コマンドのサジェストが表示されない問題を修正
- `claude agents`: 自然な速さで ← を連打してエージェント一覧に戻る操作が、1秒以上間を置くまで無視されていた問題を修正
- `claude agents` のセッション削除で、worktree が削除できない場合に処理が止まってしまう問題を修正。原因と次に取るべき対応がメッセージに表示されるようになり、git worktree の場合は再度 ctrl+x でディレクトリを強制削除できる
- エージェントパネルのバックグラウンドエージェント・ワークフローの行が、テキストに改行を含む場合に何行にも広がってしまう問題を修正
- 組織の managed settings で MCP allowlist が設定されている場合に、Claude in Slack セッションが Slack ツールを失っていた問題を修正
- ナビゲーション URL がスキームを持つがホストが解析できない場合に、Claude in Chrome がホスト「https」の許可を求めていた問題を修正
- 現在のタスクのラベルが長い場合にスピナーが複数行に折り返されていた問題を修正。ラベルと「Next:」タスク行が1ターミナル行内に収まるようになった
- ターミナルのネイティブカーソルが有効な場合に、`/bug` と `/feedback` の説明欄でカーソルが表示されない問題を修正
- `claude remote-control` が提供する Remote Control セッションが、`ListAgents` でセッションタイトルではなく生成された名前を表示していた問題を修正
- プラグインローダーが許容しているにもかかわらず、ディレクトリ名がドット2つで始まるプラグインパスを `claude plugin validate` が拒否していた問題を修正
- チェックできなかったデフォルトの monitors ファイルやルートの SKILL.md を、プラグインが黙ってスキップしていた問題を修正
- localhost などドットのないホスト名に対する WebFetch のエラーを修正: URL が拒否される理由を説明し、curl の利用を提案するようになった
- `--print` モードで PermissionRequest フックが発火しない問題を修正
- ヘッドレス(`-p`)実行でポリシーヘルパーの警告が出力されない問題を修正
- `/resume` が `/fork` のバックグラウンドセッションを自身の `⑂` フォーク名ではなく親セッションの名前で一覧表示していた問題を修正
- サインアウト時に `/remote-control` などの claude.ai 連携コマンドが Claude for Enterprise への移行メッセージを表示していた問題を修正。`/login` を提案するようになった
- `CLAUDE_CODE_SESSIONEND_HOOKS_TIMEOUT_MS` が、フックごとの `timeout` を持たない SessionEnd フックの時間を延長していなかった問題を修正(1.5秒でキャンセルされ続けていた)
- GitHub アカウントが未接続の場合に `/autofix-pr` などのクラウドセッションコマンドが、リトライや Claude GitHub App のインストールを案内していた問題を修正。`/web-setup` または web 接続ページを案内するようになった
- `/teleport` や `/remote-env` などのクラウドセッションコマンドが、組織のポリシーで無効化されている場合に「Unknown command」と答える代わりに、その理由を説明するよう修正
- Bash サンドボックスの説明が閉じ込めの強度を過大に説明していた問題を修正: ファイルシステム分離が無効な場合に実施されないパスリストを示さなくなり、strict モードでコマンドが決してサンドボックス外で実行されないと主張しなくなった
- フルスクリーンモードを改善: プロンプト行の追加・削除(Shift+Enter)が、表示中のトランスクリプトを再レンダリングする代わりに文字入力と同じ速さで再描画されるようになった
- `--continue`/`--resume` を改善: SessionStart フックを待たずに会話がすぐに表示されるようになり、最初のメッセージでトランスクリプト全体を再読み込みしなくなった
- ツール呼び出しが多いターンでの応答性を改善: 非表示のツールバッチごとのリマインダーのためにトランスクリプトを再描画しなくなった
- `.claude/workflows/` スクリプトを持つプロジェクトでの起動時間を改善: 一覧表示時に各スクリプトをパースしなくなった
- auto モードの拒否を改善: Claude が受け取るメッセージに、どのルールがブロックしたかが明記され、確認を求めて停止する前に安全な方法を試すか関係のない作業を終えるよう促すようになった
- Claude in Chrome を改善: 長いページの読み取りが、ファイルに保存して読み返す代わりにインラインで保持されるようになった
- MEMORY.md の切り詰め警告を改善: 何行が切り詰められ、どこから切り詰められたかを表示するようになった
- アーティファクトのターミナルパーミッションプロンプトを改善: 確認事項の質問を先頭に表示するようになった
- プロンプトフッターを改善: エディタや `/diff` の選択範囲がプロンプト入力内に表示されるようになり、フルスクリーンモードではフッターではなくヘッダーに Remote Control のステータスが表示されるようになった
- 「Usage credits required for 1M context」メッセージを改善: セッション中に usage credits を有効にした場合、Claude Code を再起動後に反映されることを説明するようになった
- `/plugin` を改善: プラグインのインストール・有効化・無効化がメニューを閉じた時点で反映されるようになり、その後 `/reload-plugins` が不要になった
- Bedrock、Vertex、Foundry のシステムプロンプトを変更: ファーストパーティセッションと同様に、環境・モデル・設定の詳細を添付ファイルとして渡すようになった
- Bedrock、Vertex、Foundry のセッションを変更: ファーストパーティセッションと同様に、会話を通じてツールリストをバイト単位で安定させる(遅れて接続するツールはリストを書き換えずに deferred で読み込む)ようになった
- タスク管理ツール(TaskCreate/Get/Update/List、TodoWrite)の提供を変更: Claude 3.x、Opus 4.0–4.7、Sonnet 4.0–4.6、Haiku 4.5 でのみ提供されるようになった。他のモデルで使うには `CLAUDE_CODE_ENABLE_TODO_TOOLS=1` を設定する
- ターミナルのアーティファクトデータ編集パーミッションプロンプトを変更: ドキュメント数とアーティファクトを開けるユーザーを表示するカード形式になった
- すべての承認をスキップするよう設定されたローカル Cowork セッションを変更: Artifact ツールが、セッションのフォルダ外のローカルファイルやシンボリックリンクの先にあるファイルを、確認なしに読み取る代わりに拒否するようになった
- プレーンな `WebFetch` の deny・ask ルールが Artifact ツールの読み取り・更新に適用されないよう変更。ブロックやゲートするには `Artifact` ルール(または `WebFetch(domain:claude.ai)`)を使用する
- 「N MCP servers need authentication」の起動時通知を変更: 起動ごとに表示する代わりに、各サーバーについて一度だけアナウンスするようになった
- [VSCode] `CLAUDE_CONFIG_DIR` が設定ファイルまたは `environmentVariables` 設定で指定されている場合の、セッション一覧・設定トグル・チャットタブの問題を修正
- [VSCode] ログイン・ログアウト・アカウント切り替え後の数秒間、開いているタブでモデルピル・モデルピッカー・コマンドメニューが空白になる問題を修正
- [VSCode] `~/.claude/settings.json` で指定されたモデルをプロジェクトまたはローカル設定が上書きしている場合、新規タブやリロード直後の会話で Auto がモードピッカーから消える問題を修正
- [VSCode] SessionStart フックが設定されている場合、ウィンドウのリロード後にセッション名が最後のプロンプトに戻ってしまう問題を修正
- [VSCode] ウィンドウ内の別タブの Claude プロセスが既に起動している場合、フッターのモデルピルと Remote Control ピルが新しいタブの Claude プロセス起動を待ってしまう問題を修正
- [VSCode] セッションタブの起動がその設定読み込みから0.5秒以上遅れて届いた場合、2つ目の Claude プロセスが完全な起動処理を実行してしまう問題を修正
- [VSCode] セッション一覧からのセッション再開が `claudeCode.preferredLocation: "sidebar"` を無視していた(常にパネルで開いていた)問題を修正。プログラムによるオープンがこの設定を "panel" にリセットしてしまう問題も修正
- [VSCode] Windows の問題を修正: WSL がインストールされていないマシンで WSL インストールプロンプトが表示されなくなり、WSL インストール時の Windows ファイルに対する IDE 診断が正しく返されるようになった
- [VSCode] `CLAUDE_CONFIG_DIR` が設定経由で指定されている場合、カスタムスタイルビルダーが CLI が読み取らないフォルダに User レベルのスタイルを保存していた問題を修正
- [VSCode] always-allow パーミッションルールの保存先を変更する左右矢印キーのサポートを追加(キーボード・スクリーンリーダー利用者向け)
- [VSCode] 会話内の最新メッセージにキーボードフォーカスを移動する「Claude Code: Focus last message」コマンドを追加(キーボード・スクリーンリーダー利用者向け)
- [VSCode] Manage plugins ダイアログを変更: インストール・有効化・無効化・アンインストールが再起動なしで開いているセッションに反映されるようになった
- [VSCode] 一部のアーティファクトパーミッションプロンプトを変更: ターミナルと同様に「don't ask again」の選択肢を省略するようになった
- [Claude Code on the web] 約6時間を超えて実行されるクラウドセッションが、永続化セッションフォルダに保存したファイルを静かに失っていた問題を修正。保存内容が最大1日間保持されるようになった
- [Claude Code on the web] 管理者がモデルのエフォートを制限している組織で、ルーティンがセッションを再開する場合やエフォート未設定でセッションを開始する場合に「Invalid effort level」エラーが出ていた問題を修正
- [Claude Code on the web] コネクタを持たない会話からのルーティン作成を改善: 確認のみで終わらず、コネクタがないこととその追加方法を Claude が説明するようになった
- [Claude Tag] 一時的な読み込み失敗の後、管理設定ページがローディングスケルトンのままハングする、または空白になる問題を修正。読み込みに失敗したセクションに Retry ボタンが表示されるようになった
- [Claude Tag] Slack チャンネルの configure ページから組織の Claude in Slack 管理設定に戻るリンクを追加
- [Claude Tag] Slack 管理者がチャンネルを別のワークスペースに移動した後、Slack Enterprise Grid のチャンネルが Claude の設定(リポジトリ、環境、アクセス)を失っていた問題を修正
- [Claude Tag] Claude がブロックされた操作を説明する方法を改善: パーミッションチェック、事前確認するという自身の判断、アクセス不足のいずれが原因で止まったかを示すようになった
- [Claude Tag] 応答速度を改善: Claude が読み取り専用の複数の確認作業(Slack 検索、スレッド読み取り、人物検索)を順番にではなく同時に実行するようになった
- [Claude Tag] 比較のフォーマットを改善: 文章程度の長さの比較が、横スクロールが必要な幅広テーブルの代わりにリストで表示されるようになり、長いテーブルセルは折り返されるようになった
- [Claude Tag] 専用セッションを持つスレッドでの `@Claude !restart` が、矛盾する「このスレッドはチャンネルセッションが処理します」という通知も一緒に投稿することがあった問題を修正
- [Claude Tag] Claude アカウントが Slack ワークスペースとは別の組織に属している場合のメッセージを改善: ワークスペースを自分の組織に接続する方法を説明するようになった
- [Claude Tag] 角括弧で囲まれた URL を含む Markdown リンクが、Slack でクリック可能なリンクではなく括弧付きの文字列そのままで表示されていた問題を修正
- [Claude Tag] ゲストが Claude を使用できるチャンネルでのワークスペースゲストによるトップレベル @mention が、回答の代わりに「Slack アカウントが接続されていません」という返信になることがあった問題を修正
- [Claude Tag] チャンネルの長時間稼働セッションが会話の途中で新しいセッションに置き換わってしまう問題を修正。定期リフレッシュはチャンネルとそのスレッドが落ち着くまで待つようになった
- [Claude Tag] チャンネル設定カードを複数回クリックした場合、変更が既に適用済みであっても提案元セッションに「拒否されました」と伝わっていた問題を修正。結果は一度だけ送信されるようになった
- [Claude Tag] パブリックチャンネルでのメモリを変更: チャンネルごとに独自のメモを保持するようになり、Claude は他のパブリックチャンネルで保存したメモを参照しなくなった。ワークスペースのメモは引き続き共有される
- [Code Review] フォローアップレビューの未解決findings一覧に注記を追加: finding のスレッドに返信するだけでなく解決済みにしないと、以降のレビューで未解決としてカウントされ続けることを明記
- [Code Review] finding を検証するエージェントの一つが途中で失敗した場合に、レビューが未完了として終わることがあった問題を修正。今後はそのエージェントを差し替えてレビューを完了させる
- [Code Review] 実行中のレビューの後に待機していた push トリガーのレビューが、プルリクエストがドラフトに変更された後も投稿されていた問題を修正
- [Code Review] PR が、CLAUDE.md が挙げるファイルと名前だけが一致するルートファイル(例: README.md)を編集した場合に、レビューがそのディレクトリの CLAUDE.md の規約を無視していた問題を修正
