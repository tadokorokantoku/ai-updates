---
version: "2.1.31"
date: 2026-02-04T00:43:28+00:00
---

- 終了時にセッション再開のヒントを追加。後から会話を続ける方法を表示
- チェックボックス選択で日本語 IME からの全角 (zenkaku) スペース入力をサポート
- PDF too large エラーでセッションが永続的にロックし、新しい会話を開始し直す必要が生じる不具合を修正
- sandbox モード有効時に bash コマンドが "Read-only file system" エラーで誤って失敗を報告する不具合を修正
- `~/.claude.json` のプロジェクト設定にデフォルトフィールドが欠けている場合に plan モード突入後セッションが使用不能になるクラッシュを修正
- streaming API パスで `temperatureOverride` が黙って無視され、設定されたオーバーライドに関わらず全ての streaming リクエストがデフォルト temperature (1) を使っていた不具合を修正
- null params を拒否する厳格な言語サーバーとの LSP shutdown/exit 互換性を修正
- bash 代替 (`cat`, `sed`, `grep`, `find`) ではなく専用ツール (Read, Edit, Glob, Grep) の使用をより明確に誘導するようシステムプロンプトを改善し、不要な bash コマンド使用を削減
- PDF およびリクエストサイズのエラーメッセージを実際の上限 (100 ページ、20MB) を示すよう改善
- streaming 中にスピナーが出現/消滅する際の端末レイアウトのジッタを低減
- サードパーティプロバイダ (Bedrock, Vertex, Foundry) ユーザー向けのモデルセレクタから誤解を招く Anthropic API の価格表示を削除
