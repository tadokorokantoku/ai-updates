---
version: "2.1.277"
date: 2026-09-18T16:22:26+00:00
---

- AGENTS.md サポートを追加: CLAUDE.md がないプロジェクトでは Claude Code が代わりに AGENTS.md を読み込む。`/config` の「Project instructions」で変更可能（Bedrock、Vertex、Foundry では未対応）
- 唯一の egress がフォワードプロキシである Claude apps ゲートウェイ向けに `CLAUDE_GATEWAY_PROXY_IS_EGRESS_BOUNDARY=1` を追加: すべてのアウトバウンドリクエストが、ホスト名をローカルで解決する代わりにプロキシに渡すように
- プロバイダの前段で自分が動かすプロキシに静的ヘッダーを送るため、Claude apps ゲートウェイのアップストリームにオプションの `headers:` マップを追加
- `/tasks` などのパネルを開いている間にバックグラウンドタスクが完了すると、更新が待機中であることを示す行を追加
- 内部エラー後に結果なしでハングすることがあった `claude -p` と Agent SDK セッションを修正。エラーを報告して終了コード 1 で終了するように
- 以前のアシスタントターンが他のコンテンツと並んで空のテキストブロックを保持していた場合に、`--resume` 後も含めすべてのリクエストが "text content blocks must be non-empty" で失敗する問題を修正
- 同じマシン上で古い Claude Code ビルド（IDE 拡張機能にバンドルされた CLI など）が現在のものと一緒に動作しているときに、予期せずログアウトされる問題を修正
- `~/.claude.json` に不正な形式の `customApiKeyResponses` 値があるとき、`ANTHROPIC_API_KEY` ユーザーのインタラクティブな起動がハングしたりエラーを表示したりする問題を修正
- プロキシが無効なバージョンを返した場合に、更新チェックが 30 分ごとにエラーになる問題、および最小・最大バージョンが設定されているときに `claude update` がハングする問題を修正。不正な形式の `minimumVersion` は無視されるように
- バージョン確認が失敗したときに、winget または apk で管理されたインストールで `claude update` が "up to date" と報告する問題を修正
- セッションや他のプログラムが使用中のプラグインバージョンを再インストールすると `claude plugin install` が失敗してインストール済みのコピーを壊すことがある問題を修正。変更されていないコピーはそのまま残るように
- プロセス、メモリ、ファイルハンドルが不足して検索を開始できなかったときに、Grep と Glob が「一致なし」と報告する問題を修正。今後はその旨のエラーを返すように
- 対象パスが既存のディレクトリであるとき、Write ツールが無言で権限拒否としてターンを終了する問題を修正。今後は明確なエラーを報告するように
- Edit ツールが、エスケープされたバックスラッシュの後に続く `uXXXX` テキストを `\uXXXX` エスケープとして扱う問題を修正。これにより非 ASCII 文字の編集でエスケープされたバックスラッシュシーケンスが書き換えられることがあった
- 非 ASCII テキストを含む非常に大きな編集がファイルに一致しなかったとき、Edit ツールが "String not found in file" の代わりに "Invalid regular expression: regular expression too large" を報告する問題を修正
- ツール呼び出しのファイルパスにエスケープシーケンスとして書かれた `\^@` が含まれるとき、ターンが "Path contains null bytes" で早期終了する問題を修正。エスケープされた制御文字はリテラルテキストのまま保持されるように
- プラグインの LSP サーバーが終了したり stdin を閉じたりしたときに、バックグラウンドセッション（`claude --bg`）が終了する問題を修正
- `~/.claude.json` に不正な形式の `claudeAiMcpEverConnected` 値があるとき、`/mcp` または `/plugin manage` を開くとクラッシュ（"Type error"）する問題を修正
- `~/.claude.json` に不正な形式の `theme` 値があるとき、起動時にクラッシュする問題を修正
- プロンプトにターミナルのカラーコードを含むテキスト（履歴から呼び出したプロンプトや外部エディタから読み込んだテキストなど）が含まれているとき、クラッシュ（"unrecoverable interface error"）する問題を修正
- 保存された履歴にプレーンな文字列として格納されたアシスタントメッセージを持つセッションを再開するときのクラッシュを修正
- 遅いまたは高負荷のマシンで、最初のスピナー表示時に "Claude Code exited after an unrecoverable interface error" で終了することがある問題を修正
- 内部レンダリングエラーの後、画面がセッションの残り時間中更新を停止することがある稀なケースを修正
- Windows で、Claude が返信した直後に "Out of memory" のようなエラーでターンが停止し、その返信のツール呼び出しが実行されないことがある稀なケースを修正
- SessionStart フックが出力を表示したときに、`/clear` 後に継続されたセッション（restart、`--continue`、`--resume`）が最初のメッセージの一部を欠落させ、プロンプトキャッシュが完全にミスする問題を修正
- ターン中に届いた他のエージェント（サブエージェントの SendMessage など）からのメッセージが、届いた場所ではなく "Ran N shell commands" 行の下に表示される問題を修正
- フルスクリーンの `/resume` ピッカーやプロンプトエリアを覆う他のパネルでテキストをドラッグ選択した後に「コピーしました」通知が表示されない問題を修正
- サンドボックスが有効な状態でサンドボックス外で実行される Bash コマンドで `$TMPDIR` が空に展開される問題を修正
- Cowork クラウドセッションの WebFetch と WebSearch が、フェッチ予算の使い切りや管理者ポリシーなどリクエストが拒否された理由を Claude に伝えない問題を修正
- プロキシが設定されているとき、Claude apps ゲートウェイのテレメトリリレーが `NO_PROXY` に記載されたコレクターのホスト名やドメインを無視する問題を修正
- 不正な形式の `strictKnownMarketplaces` または `blockedMarketplaces` エントリが 1 つあるだけでエンタープライズマーケットプレイスポリシー全体を無言で無効化する問題を修正
- 自動更新が失敗すると `~/.cache/claude/staging` に大きなステージング済みダウンロードが残る問題を修正
- `/plugin` の Installed タブで、失敗したプラグイン更新のエラーなどのメッセージからターミナル制御文字が取り除かれない問題を修正
- スキルまたはレガシーコマンドが `constructor` や `toString` のような組み込み Object プロパティのような名前のとき、`/plugin` → Installed と `/skills` がクラッシュする問題を修正
- 複数選択でのすべてのインストールが失敗したときに `/plugin` がメッセージなしで閉じる問題を修正
- アンインストールされたプラグインが `/plugin` Installed に "failed to load" 行として再表示され、Remove でその行がクリアされない問題を修正
- 公式マーケットプレイスからのプラグインが `installed_plugins.json` にコミットなしで記録される問題、および固定コミットのプラグインを更新した後も `installed_plugins.json` が古いコミットを保持し続ける問題を修正
- プラグインのリロードプレビューが、終了するまでプレビューされたプラグインアーカイブのすべてのコピーを展開したまま保持する問題、およびダウンロードが失敗したときにリロードがフォールバックするキャッシュ済みの `--plugin-url` アーカイブを上書きする問題を修正
- `~/.claude.json` に不正な形式のプレースホルダーレコードがあるとき、Remote Control のセッション管理が失敗する問題を修正
- 取り消された claude.ai ログイン後のエラーが期限切れの Anthropic プロファイルのせいにする問題を修正。今後は `/login` を先頭に表示
- キーリピートや非常に高速な入力の間に、`claude agents` のディスパッチ入力でタイプまたはペーストしたテキストが時々乱れる問題を修正
- 保存されたトランスクリプトが正しい形式でないフックリストを含む stop フックの要約を含むとき、セッション再開時のクラッシュ（"unrecoverable interface error"）を修正
- `keybindings.json` が Chat コンテキストで Enter を（例えば `chat:queueSubmit` に）再バインドしているとき、選択されたエージェントパネル行で Enter を押しても何も起きない問題を修正
- 作業フォルダのパスが長い（約 120 文字以上）とき、Windows での PDF ページ読み込みが失敗する問題を修正
- ヘッドレスな resume（`claude -p --resume`、SDK、VS Code 拡張機能のウィンドウリロード）がセッションのコストと使用量の合計をゼロから開始する問題を修正。ヘッドレスセッションは終了時に合計を保存するように
- `.claude/skills` が未追跡のとき、`--worktree` セッションでメインリポジトリのプロジェクトスキルが読み込まれない問題を修正
- `sandbox.excludedCommands` のグロブが、一部だけが一致した場合でも複合 Bash コマンド全体をサンドボックスから除外する問題を修正。今後はすべての部分が一致する必要があるように
- 再開されたサブエージェントとチームメイトが、読み込んでいた MCP ツール定義を再レンダリングし、そのエージェントのプロンプトキャッシュを壊す問題を修正
- レート制限された artifact の publish が Claude にリトライを停止するよう伝える問題を修正。今後は何も publish されなかったことと、いつ同じ publish を再送すべきかが Claude に伝えられるように
- 会話の以前に記録された添付ファイルが resume や再起動後に再レンダリングされ、extended thinking がドロップされプロンプトキャッシュがミスする問題を修正
- サーバーが API キーの作成を拒否したとき、Console のサインインが "Request failed with status code 400" とだけ表示する問題を修正。今後はサーバーのメッセージを表示するように
- Claude がまだ作業中のときに入力されたメッセージが、モデルに無視されることがある問題を修正
- SDK およびヘッドレス（`-p`）使用時のセッション起動を改善: 最初のターンがディレクトリごとの CLAUDE.md 検索を待たなくなった
- Claude apps ゲートウェイのループバックエラーメッセージを改善し、`CLAUDE_GATEWAY_ALLOW_LOOPBACK` という名前を明示するように
- `/plugin` Installed を改善: プラグインとは別に表示される MCP サーバーが、どのプラグインに属しているかを表示するように
- 既にインストール済みのプラグインに対する `claude plugin install` を改善: マーケットプレイスがより新しいバージョンを提供しているときにその旨を表示し、`claude plugin update` コマンドを案内するように
- ロゴ下のスタートアップ通知オーバーフロー行を改善: "+N more · /status" の代わりに "N more notices hidden" と表示するように
- プロンプト処理を改善: プロンプト内の不可視な Unicode 書式文字やタグ文字が除去され、送信前にレビュー用としてクリーンなプロンプトが表示されるように
- レビュー対象が何もないときの `/ultrareview` を改善: メッセージがどのケースに該当するかを示し、最新のコミットをレビューするコマンドを提示し、新しいリポジトリの最初のコミットは全体がレビューされるように
- そのツールが利用可能なとき、Claude が claude.ai の artifact リンクを WebFetch ではなく Artifact ツールで読むよう、artifact リンク処理を改善
- 危険な rm の権限プロンプトを改善: フラグされた rm コマンドを明示し、`${VAR:?}` ガードを提案することで、ヘッドレス実行がリカバリーできるように
- Artifact ツールの権限プロンプトを改善: 文をより短く、ページと artifact をタイトルまたはファイル名で表示し、リンクをテキストの後に列挙するように
- Anthropic API 上で Fable が `/model` に常に表示されるように変更。組織の設定で無効化されている場合のみグレーアウトされる
- Bedrock、Vertex、Foundry での Bash サンドボックスの説明をファーストパーティの文言に変更。サンドボックスをタスクに与えられたものの境界として位置づける
- リポジトリにベースブランチや共有履歴がない場合、非インタラクティブセッションでの `/ultrareview` が拒否するように変更
- サブエージェントの結果が、サブエージェント出力であることを示すヘッダーの下にインデントされて main agent に届くように変更。これにより、サブエージェントの結果内のテキストがセッション自身の指示として通ることができなくなった
- Bedrock、Vertex、Foundry でのワークフロースクリプトの計算された `agent()` プロンプトが、スクリプト作成者によるテキストとしてフレーム付けされてサブエージェントに届くように変更。安全性分類器がそれをユーザーとして読み取らないように
- SDK や IDE の外で起動された `claude -p` 実行からのバックグラウンド Haiku 自動タイトルリクエストを削除
- 非推奨の TaskOutput ツールを削除。Claude は代わりに Read でバックグラウンドタスクの出力ファイルを読み、`taskOutputMaxChars` 設定と `TASK_MAX_OUTPUT_LENGTH` はもう効果を持たない
- [VSCode] パネルメニューに Sign out 行を追加し、タイプコマンドメニューに `/logout` を追加
- [VSCode] バックグラウンドシェルや他の実行中タスクをエージェントマップに追加。それぞれに Stop があり、それを開くタイプコマンド `/tasks` を追加
- [VSCode] 応答に Copy response ボタンを追加し、タイプコマンド `/copy` を追加
- [VSCode] 非アクティブなセッションが自動的にアーカイブされたときのワンタイム通知と、Archived sessions グループでの "Unarchive all" アクションを追加
- [VSCode] プラン上限が適用されない場合（Vertex、Bedrock、Foundry、API キー）に、Account & usage ダイアログとセッションマネージャーにセッションのコストとトークン使用量を追加
- [VSCode] "General config" メニュー行が設定を開く代わりに `/config` の使用方法テキストを表示していた問題を修正し、タイプされた `/mcp`、`/hooks`、`/memory`、`/rewind` などのコマンドがそれぞれのダイアログを開くように修正
- [VSCode] `/effort` で保存済みのレベルを持つモデルで、effort スライダーのレベルが後のセッションに永続化されない問題を修正
- [VSCode] 保存されたモデル設定が "Sonnet" のように大文字小文字の異なるエイリアスであるとき、既に使用されたパネルで開かれた会話のモードピッカーから Auto が欠落する問題を修正
- [VSCode] `/fast` がデフォルトとして fast mode を保存せず、拡張機能が Claude Code を再起動すると失われる問題を修正
- [Claude Code on the web] Team および Enterprise プランの environment ピッカーに Personal と Organization セクションを追加。管理者は個人の environment を組織と共有できるように
- [Claude Code on the web] Team および Enterprise プランで、組織の environment が Code タブから読み取り専用のサマリーとして開くように変更。編集は Admin settings → Cloud environments で行う
- [Claude Code on the web] Custom network access でドメインを指定せずに保存したクラウド environment が無言で Trusted に戻ってしまう問題を修正。ダイアログは少なくとも 1 つのドメインを要求するように
- [Claude Code on the web] "Web" と表示されていた管理者向け Claude Code 設定を "Cloud sessions" に変更し、その下の冗長な読み取り専用 Mobile 行を削除
- [Claude Tag] Enterprise Grid の組織全体インストール上の Slack チャンネルで作成された routine が、実行時にワークスペース内の他の公開チャンネルを読み取れない問題を修正
- [Claude Tag] Claude Tag アクセスバンドルの credential プリセットにある "Learn more" リンクが、汎用の API リファレンスではなく各ベンダーの credential 設定ページを開くように修正
- [Claude Tag] Claude Tag アクセスバンドルの Pylon credential プリセットを変更し、管理者が Pylon の EU ホストを指定できるように
- [Claude Tag] Claude Tag アクセスバンドルの Google Cloud credential フォームを修正: 拒否されたキーファイルは理由を表示し、website と scopes はロックされたままになり、拒否されたローテーションは貼り付けたキーを保持するように
- [Claude Tag] Claude Tag 管理者設定のネットワークイベントログが、AWS signing、クライアント証明書、カスタム CA を使う接続経由のリクエストで応答ステータスを表示しない問題を修正
