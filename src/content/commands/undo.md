---
name: "/undo"
introducedIn: "2.1.108"
description: "`/undo` is now an alias for `/rewind`"
category: slash
---

# /undo

`/undo` is now an alias for `/rewind`

## 関連リリース

### v2.1.108

- `/undo` is now an alias for `/rewind`
