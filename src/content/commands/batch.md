---
name: "/batch"
introducedIn: "2.1.63"
description: "Added `/simplify` and `/batch` bundled slash commands"
category: slash
---

# /batch

Added `/simplify` and `/batch` bundled slash commands

## 関連リリース

### v2.1.63

- Added `/simplify` and `/batch` bundled slash commands
