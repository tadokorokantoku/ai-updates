---
name: "/routines"
introducedIn: "2.1.136"
description: "Manage scheduled cloud agents (routines) that execute on a cron schedule via the claude.ai API"
category: slash
---

# /routines

Manage scheduled remote Claude Code agents (routines) via the claude.ai API. Create, list, update, and run cloud agents on a schedule. Auth is handled in-process — the token never reaches the shell.
