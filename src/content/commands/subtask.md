---
name: "/subtask"
introducedIn: "2.1.212"
description: "Launch an in-session subagent to work on a task; this is what `/fork` used to launch before `/fork` was changed to create a new background session instead"
category: slash
---

# /subtask

Launch an in-session subagent to work on a task; this is what `/fork` used to launch before `/fork` was changed to create a new background session instead.

Not available in coordinator sessions — use `/branch` instead there.
