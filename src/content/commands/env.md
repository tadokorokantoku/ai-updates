---
name: "/env"
introducedIn: "2.1.89"
description: "`/env` now applies to PowerShell tool commands (previously only affected Bash)"
category: slash
---

# /env

`/env` now applies to PowerShell tool commands (previously only affected Bash)

## 関連リリース

### v2.1.89

- `/env` now applies to PowerShell tool commands (previously only affected Bash)
