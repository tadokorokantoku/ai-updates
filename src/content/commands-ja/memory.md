---
name: "/memory"
introducedIn: "1.0.94"
description: "Claude が有用なコンテキストを自動的に auto-memory に保存。/memory で管理できる"
category: slash
---

# /memory

Claude が有用なコンテキストを自動的に auto-memory に保存。`/memory` で管理できる

## 関連リリース

### v2.1.59

- Claude が有用なコンテキストを自動的に auto-memory に保存するように。`/memory` で管理できる
### v1.0.94

- `/memory` コマンドがインポートされた全メモリファイルの直接編集に対応
