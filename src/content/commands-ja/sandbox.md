---
name: "/sandbox"
introducedIn: "2.0.12"
description: "`/sandbox` コマンドの UI を改善。依存関係が不足している場合にステータスとインストール手順を表示"
category: slash
---

# /sandbox

`/sandbox` コマンドの UI を改善。依存関係が不足している場合にステータスとインストール手順を表示

## 関連リリース

### v2.1.80

- `/sandbox` のタブ切り替えが Tab キーや矢印キーに応答しない問題を修正
### v2.1.78

- `/sandbox` の Dependencies タブが macOS で macOS 固有の情報ではなく Linux 用の前提条件を表示する問題を修正
### v2.1.20

- `/sandbox` コマンドの UI を改善。依存関係が不足している場合にステータスとインストール手順を表示
