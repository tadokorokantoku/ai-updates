---
name: "/env"
introducedIn: "2.1.89"
description: "`/env` が PowerShell tool コマンドにも適用されるように（従来は Bash のみ）"
category: slash
---

# /env

`/env` が PowerShell tool コマンドにも適用されるように（従来は Bash のみ）

## 関連リリース

### v2.1.89

- `/env` が PowerShell tool コマンドにも適用されるように（従来は Bash のみ）
