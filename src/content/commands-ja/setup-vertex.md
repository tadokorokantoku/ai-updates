---
name: "/setup-vertex"
introducedIn: "2.1.111"
description: "`/setup-vertex` と `/setup-bedrock` を改善。`CLAUDE_CONFIG_DIR` が設定されている場合に実際の `settings.json` パスを表示、再実行時に既存のピンからモデル候補を初期化、サポート対象モデルには「with 1M conte..."
category: slash
---

# /setup-vertex

`/setup-vertex` と `/setup-bedrock` を改善。`CLAUDE_CONFIG_DIR` が設定されている場合に実際の `settings.json` パスを表示、再実行時に既存のピンからモデル候補を初期化、サポート対象モデルには「with 1M conte...

## 関連リリース

### v2.1.111

- `/setup-vertex` と `/setup-bedrock` を改善。`CLAUDE_CONFIG_DIR` が設定されている場合に実際の `settings.json` パスを表示、再実行時に既存のピンからモデル候補を初期化、サポート対象モデルには「with 1M context」オプションを提供
