---
name: "/color"
introducedIn: "2.1.70"
description: "`/color` コマンドでデフォルト色に戻す手段がなかった問題を修正 — `/color default`、`/color gray`、`/color reset`、`/color none` でデフォルトに復元できるように"
category: slash
---

# /color

`/color` コマンドでデフォルト色に戻す手段がなかった問題を修正 — `/color default`、`/color gray`、`/color reset`、`/color none` でデフォルトに復元できるように

## 関連リリース

### v2.1.75

- 全ユーザー向けに、セッションのプロンプトバー色を設定する `/color` コマンドを追加
### v2.1.70

- `/color` コマンドでデフォルト色に戻す手段がなかった問題を修正 — `/color default`、`/color gray`、`/color reset`、`/color none` でデフォルトに復元できるように
