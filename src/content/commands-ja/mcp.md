---
name: "/mcp"
introducedIn: "2.1.70"
description: "[VSCode] ネイティブの MCP サーバー管理ダイアログを追加 — チャットパネルで `/mcp` を使うと、ターミナルに切り替えずにサーバーの有効化/無効化、再接続、OAuth 認証管理が可能"
category: slash
---

# /mcp

[VSCode] ネイティブの MCP サーバー管理ダイアログを追加 — チャットパネルで `/mcp` を使うと、ターミナルに切り替えずにサーバーの有効化/無効化、再接続、OAuth 認証管理が可能

## 関連リリース

### v2.1.101

- `headersHelper` で設定された MCP サーバーに対して `/mcp` メニューが OAuth 固有のアクションを提示する問題を修正。代わりに Reconnect を提示し、ヘルパースクリプトを再呼び出し
### v2.1.77

- エージェント実行中に `/mcp` や同様のダイアログを開いた際の入力デッドロックを修正
### v2.1.70

- [VSCode] ネイティブの MCP サーバー管理ダイアログを追加 — チャットパネルで `/mcp` を使うと、ターミナルに切り替えずにサーバーの有効化/無効化、再接続、OAuth 認証管理が可能
