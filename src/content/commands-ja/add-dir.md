---
name: "/add-dir"
introducedIn: "1.0.23"
description: "`/add-dir` コマンドにチルダ (`~`) 展開のサポートを追加"
category: slash
---

# /add-dir

`/add-dir` コマンドにチルダ (`~`) 展開のサポートを追加。

## 関連リリース

### v2.1.47

- statusline JSON の `workspace` セクションに `added_dirs` を追加。`/add-dir` で追加したディレクトリを外部スクリプトから参照できるようになった (anthropics/claude-code#26096)
### v1.0.42

- `/add-dir` コマンドにチルダ (`~`) 展開のサポートを追加
