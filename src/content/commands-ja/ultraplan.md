---
name: "/ultraplan"
introducedIn: "2.1.83"
description: "`/ultraplan` などのリモートセッション機能が、ウェブでのセットアップを要求せずにデフォルトのクラウド環境を自動作成するように"
category: slash
---

# /ultraplan

`/ultraplan` などのリモートセッション機能が、ウェブでのセットアップを要求せずにデフォルトのクラウド環境を自動作成するように

## 関連リリース

### v2.1.101

- `/ultraplan` などのリモートセッション機能が、ウェブでのセットアップを要求せずにデフォルトのクラウド環境を自動作成するように
