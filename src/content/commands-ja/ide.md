---
name: "/ide"
introducedIn: "0.2.104"
description: "拡張機能を自動インストールする際に `/ide` が `onInstall is not defined` でクラッシュする問題を修正"
category: slash
---

# /ide

拡張機能を自動インストールする際に `/ide` が `onInstall is not defined` でクラッシュする問題を修正

## 関連リリース

### v2.1.73

- 拡張機能を自動インストールする際に `/ide` が `onInstall is not defined` でクラッシュする問題を修正
