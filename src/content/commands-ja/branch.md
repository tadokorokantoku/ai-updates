---
name: "/branch"
introducedIn: "2.1.77"
description: "`/fork` を `/branch` にリネーム（`/fork` はエイリアスとして引き続き利用可能）"
category: slash
---

# /branch

`/fork` を `/branch` にリネーム（`/fork` はエイリアスとして引き続き利用可能）

## 関連リリース

### v2.1.105

- `/resume`、`--worktree`、`/branch` 実行後の終了時に "Resume this session with..." ヒントが表示されない問題を修正
### v2.1.77

- `/fork` を `/branch` にリネーム（`/fork` はエイリアスとして引き続き利用可能）
