---
name: "/permissions"
introducedIn: "2.0.67"
description: "`/permissions` コマンドに検索機能を追加。`/` キーボードショートカットでツール名によるルールのフィルタリングが可能に"
category: slash
---

# /permissions

`/permissions` コマンドに検索機能を追加。`/` キーボードショートカットでツール名によるルールのフィルタリングが可能に

## 関連リリース

### v2.1.89

- Auto モード: 拒否されたコマンドに通知を表示し、`/permissions` → Recent タブに表示。`r` キーで再試行可能
### v2.1.80

- `/permissions` を改善。リスト内から Tab キーや矢印キーでタブ切り替えが可能に
### v2.1.79

- `/permissions` の左右矢印キーによるタブ移動を修正
### v2.1.71

- `/permissions` の Workspace タブで、末尾スラッシュの有無により同じディレクトリが重複表示される問題を修正
### v2.0.67

- `/permissions` コマンドに検索機能を追加。`/` キーボードショートカットでツール名によるルールのフィルタリングが可能に
- `/permissions` でパーミッションルール削除後にスクロール位置がリセットされる問題を修正
