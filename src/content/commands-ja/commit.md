---
name: "/commit"
introducedIn: "2.1.51"
description: "カスタム `/commit` コマンドを持たないユーザーに対して、Claude が存在しない `commit` skill を呼び出して \"Unknown skill: commit\" と表示する問題を修正"
category: slash
---

# /commit

カスタム `/commit` コマンドを持たないユーザーに対して、Claude が存在しない `commit` skill を呼び出して "Unknown skill: commit" と表示する問題を修正

## 関連リリース

### v2.1.111

- カスタム `/commit` コマンドを持たないユーザーに対して、Claude が存在しない `commit` skill を呼び出して "Unknown skill: commit" と表示する問題を修正
